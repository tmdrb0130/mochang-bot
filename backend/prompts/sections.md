---
# user 프롬프트에 들어가는 섹션 머리말 모음. 코드(assemble.py, rag/pipeline.py)가 key 로 읽는다.
# 문구를 바꾸려면 여기만 고친다.
facts: "[지원자가 확인한 사실 — 그대로 사실로 써도 됨]"
unknowns: "[지원자가 아직 모른다고 한 것 — 확정된 것처럼 쓰지 말고 '가정'·'계획'으로 표현. 멘토 도움(Q4-2)에서 다룰 것]"
references: "[웹 참고자료 — 아래 사실만 인용 가능. 인용할 때 '출처: 매체/기관, 연도' 를 문장에 붙일 것. 목록 밖의 수치·통계는 쓰지 않는다]"
extend_current: "이미 작성된 글:"
---
이 파일의 본문은 쓰지 않는다. frontmatter 의 값만 사용.
