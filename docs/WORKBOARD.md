# 작업 상태판 (WORKBOARD) — 2026-09-02

> 실시간 조율 문서. 규칙은 `docs/TEAMWORK.md`.
> 각 세션은 **자기 로그 절에만** 쓴다. 배정표·조율 필요 절은 세션1이 관리한다.

## 배정표

| # | 작업 | 담당 | 상태 | 근거 |
|---|---|---|---|---|
| 1 | **V1 — 프론트를 작업 큐 API 로 전환** | 세션2 | ✅ 완료 (`b4839a1`+`dae680c`, 08:57 배포) | PROGRESS "다음 순서" 1순위. V5·V6·V9 가 같이 풀림 |
| 2 | **V4 — 사용자별 동시 작업 제한** | 세션3 | ✅ 완료 (`1bb3ad0`) | 한 명이 27건으로 큐 독점 가능 |
| 3 | 모델 결정 (라마 이물 82%·출처 날조) | **사용자** | ✅ **결정 (13:5x): 라마 단독 유지** + 후처리(작업 22)로 이물·지어냄 대응 | PROGRESS 2절 선택지 ② |
| 4 | V7 — SSH 터널 NSSM 서비스화 | 보류 | 승인 대기 | 관리자 권한·키 인증 확인 필요 (PROJECT_CONTEXT 12절에 절차 있음) |
| 5 | ~~조사 재사용~~ → **작업 12 의 2단계로 흡수** | — | 통합 | |
| 6 | 모델 표기 → "백석대학교 로컬 LLM" | 세션3+세션2 | ✅ 완료 (공개 `/api/models` 확인) | 교수님 요청 (9/2). 작음 — 각자 자기 파일 1~3줄 |
| 7 | ~~근거 소스 다양화~~ → **작업 12 의 3단계로 흡수** | — | 통합 | |
| 8 | ~~검색 백엔드 추가~~ → **작업 12 의 3단계로 흡수** | — | 통합 | |
| 9 | LlamaIndex RAG — 조사 자료 벡터DB 축적·재사용 | **세션2**(모듈+테스트) → 세션3(파이프라인 연결) | ✅ 완료 — 모듈 (`7b2a6e5`) / 색인 켜짐+0.6 (`677e785`) / 조회 (`84cff3b`, 실호출 적중 0.654·웹 0건). **재시작으로 운영 반영** | 교수님 요청 (9/2). PROGRESS 백로그 "공공데이터 색인" 확장. 설계: `docs/RAG_PLAN.md` |
| 10 | GPU 한계 측정 → 워커 수 상향 | 세션3 + 사용자 | ✅ 완료 — 09:3x 재시작으로 운영 반영 (`/api/health` `max_workers: 30`) | 교수님 요청 (9/2). 어제 부하테스트에서 병목은 vLLM 이 아니라 **우리 워커 12개**였음 |
| 11 | 로컬 트랙·스토리/간결 스타일 UI 제거 | 세션2 | ✅ 완료 (배포 번들 확인) | 사용자 결정 (9/2). 일반/기술 + 논리·근거형만. **프론트만, 백엔드 md 는 유지** |
| 12 | **자료조사 파이프라인 개편 (0~5단계)** | 세션3 | 0~3단계 ✅ (`c747955`·`eda5c05` KCI 포함 정형 5종). 남은 것: 4단계 캐시 개편·5단계 진행 표시 — 급하지 않음 | 사용자 지시서 (9/2) — `docs/RESEARCH_PLAN.md`. 수치 검증 완료. 구 5·7·8 흡수 |
| 13 | **GPU 상한 실측 확대 → 워커 최대치 반영** | 세션3 + 사용자 | ✅ 완료 — 11:0x 재시작으로 운영 반영 (`max_workers: 36`) | 한계는 메모리(KV 45%@100)가 아니라 처리량. 워커를 더 올려도 총 처리량은 안 늘고 문항당 시간만 늘어남 |
| 14 | 프론트 UX 개선 A2~A5·B1~B4 (이어받기 후 자동 생성, 대기 카드, 공통조사 안내, 저장 용량, 모델 표기 등) | 세션2 | ✅ 커밋 (`c1dd0e4`), 세션1 dist-check 빌드 통과. ✅ **11:3x 배포 완료** — 공개 번들 `index-vkBBBKoM.js` (세션1 공개 URL 확인) | 세션2 로그 325행에 항목별 근거. A1(조사·인테이크 큐 전환)은 세션3 `main.py:257` 정리 뒤 |
| 15 | **Q1 문체 획일화** — "저는 [대상]에게 [서비스]를 제공하는 앱입니다" 틀로만 나옴 | 세션3 | ✅ 완료 (`e889a6a`) — 코드 구조 회전 + 정의형 재생성 1회. 실호출 6문장 맺음말 전부 다름. 잔여: 98~99자(한도 100 안) | 라마가 금지형 지시를 우회함(25건 실측). 아래 상세 |
| 16 | **프롬프트를 창업교육 MVP 8단계 프로세스에 맞춰 개선** | 세션3 | ✅ 완료 (`9a51291`) — process.md + 문항 9개 [프로세스 위치] + Q4-1 ⑥⑦⑧ 뼈대 + Q2 검증 규칙. 전/후 실호출 표는 세션3 로그. **Q2 문단 구성 미준수는 잔존** → 작업 18 로 절반 해소 | 기준·매핑·검증은 **`docs/MVP_PROCESS.md`** 가 단일 기준. 백업: 태그 `prompts-before-mvp-2026-09-02` + `docs/prompt_backups/2026-09-02/` |
| 17 | **KCI 학술 어댑터 + 출처 표기** | 세션3(어댑터) + 세션2(UI 문구) | ✅ 완료 — UI (`1bd6ca3`, 배포 `index-D6Y4UlY6.js`) / 어댑터 (`eda5c05`, 준수 1·2·3·5항 코드화, 실호출 4건 적중) | RESEARCH_PLAN 의 KCI 준수 6항이 완료 조건. 세션3: `no_fetch`·벡터DB 제외·논문 인용 형식. 세션2: 푸터+근거 패널에 "KCI(한국학술지인용색인) 데이터 활용" |
| 18 | **긴 문항 분량 미달 → 서버 자동 이어쓰기 1회** | 세션3 | ✅ 코드 완료 (`941742d`+`504a574`), **운영 미반영(재시작 필요)** — Q2 759→1009자, extend 프롬프트를 긍정형 단일 과제로(반복 제거). `config.yaml auto_extend` 로 on/off | 1회 고정이라 70% 를 늘 채우진 않음. 문단 구성 미준수는 잔존(라마 한계) |
| 19 | **운영 계측 정리** — ① pytest 가 `backend/.timing.jsonl` 을 오염(오늘 가짜 job 2,000건+, run_s 0) → 테스트는 `MOCHANG_TIMING_LOG` 를 임시 경로로 ② 소스별 외부 호출 카운터(naver/kosis/ecos/kstartup/sangkwon/kci/ddgs/fetch)를 timing 로그에 남겨 한도 대비 사용률을 볼 수 있게 ③ RESEARCH_PLAN 5단계 서킷브레이커(소스 429·타임아웃 연속 N회 → 일정 시간 제외) | 세션3 | ✅ 완료 (`f61b024`) — 로그 격리·`event=sources` 카운터·연속 3회 실패 소스 300초 제외. 재시작 대기 | 지금은 네이버 사용량을 NCP 콘솔에서만 볼 수 있고, 우리 쪽 로그로는 소스별 요청 수를 알 수 없음 |
| 20 | **문항 간 반복 제거·한 줄기 일관성 — 사업계획 골자 공유** | 세션3 | ✅ 코드 완료 (`f61b024`) — 골자는 문항 몫만 노출(`slice_for`) + 참고자료 문항별 분배(`references_for`). 실측 A 28→11쌍(−61%), B 0→3. ✅ 운영 반영(14:0x 재시작). **세션1 독립 재측정(세탁소): 통계 재인용 3회+ 9종→0 ✅, 유사 문장 쌍 6→10 ❌ — 전부 '저는 …플랫폼을 만들고자 합니다' 아이디어 재진술 첫 문장(Q3-1·Q4-1·Q4-2·Q8)** → 작업 22 ⑥으로 결정적 삭제 | 설계·역할표·완료 조건은 **`docs/OUTLINE_PLAN.md`**. 실측 스크립트 `docs/tools/fullrun_repeat_check.py`. 우선순위: 21 → 20 → 19 |
| 21 | **Q1·Q10 이 한도(100자)를 넘겨 '…' 로 잘림** | 세션3 | ✅ 완료 (`02bcf1e`) — `shorten.md` 압축 재생성 최대 2회. 실호출 10건 잘림 0·초과 0(최대 82자). 코드부 재시작 대기 | 원인: `generate.py postprocess` 가 100자 초과분을 **잘라내고 '…'** 붙임. 잘린 문장이 그대로 사용자에게 감. 수정: 짧은 문항(limit≤100)은 초과 시 자르지 말고 **"지금 N자, {limit-15}자 이내로 같은 뜻으로 줄여 다시" 압축 재생성 최대 2회** → 그래도 넘으면 문장 끝에서만 자름(어절 자름+'…' 금지). `q1.md` target 60~90 → **60~85** 로 여유. 검증: 실호출 10건 중 잘림 0건, 100자 초과 0건 |
| 22 | **생성 후처리 모듈** — 이물·지어낸 수치·상용구·1인칭·통계 재인용·재진술 첫 문장·이어쓰기 중복·금지어 (`WORKORDER_QUALITY.md` 4절 ①~⑩) | **세션2**(소유권 예외: `backend/pipeline/postprocess.py`·`tests/test_postprocess.py` 신규) → 세션3 이 `generate_one` 에 연결 | ✅ 코드 완료 (`91d57f5` 모듈 + `1e40130` 연결, 380 passed). 재시작 대기 | 기준선 실측에서 라마가 system.md 금지를 무시하고 인터뷰("세탁소 주인들에게 물어봤습니다")·친구 일화·단위경제(1만 원/500원/1억)·자금 배분(60/20/10/10%)·역량("알고리즘 개발 가능")을 지어냄 + 한자 이물 2건(途中·需求). 생성 후 ① 이물 문자 정규식 → 해당 문장 재생성 ② 사람 수·인터뷰·금액·% 문장 중 입력·참고자료에 없는 수치 → 삭제 또는 "가정" 문구 강제 (`verify.md` 활용). 작업 20 뒤 |
| 23 | **세션2 잔여** — ① 인테이크 안내 문구 실측 반영("처음 한 번만 1~2분" → 실측 인테이크 p50 107s + 공통 조사 = **2~4분**, `App.jsx:170`·인테이크 대기 화면) ② A1: 조사·인테이크를 `/jobs/research`·`/jobs/intake` 큐 경로로 전환(백엔드 kind 는 이미 있음 `main.py:254·257`) — 새로고침 이어받기·순번 표시가 조사·인테이크에도 적용 | 세션2 | 대기 (사용자 지시 13:5x) | 배포는 승인 후. `main.py:257` 의 client→research_client 통일은 세션3 몫(작업 22 와 함께), A1 은 그것과 무관하게 진행 가능(지금은 같은 라마) |
| 26 | **Q2 품질 — 골든 예문 기준 구조 재설계** (핵심 문제 판별·기존 방식 한계 문단·기능 1회·1,400~1,800자·자기소개 시작 금지) | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | 설계·진단표·문단 구조·완료 조건은 **`docs/Q2_QUALITY.md`**. 검증은 **`docs/PROMPT_GENERALIZATION.md` 분야 세트(A+C+F+B)** 로 — 특정 분야 편향 금지(사용자 14:4x). 순서: 26 → 25 → 22 → 24. 예문은 프롬프트에 넣지 않음(소재 베낌 방지) |
| 27 | **긴 문항 한도 초과 → 자르지 말고 압축 재생성** (Q3-1 실서비스 출력이 2,000자 초과로 "둘째, 비용입니다." 에서 잘림) | **세션2**(작업 22 모듈의 ⑨, `needs_compress` 신호) + 세션3(호출·`shorten.md`) | ✅ 코드 완료 (refine ⑨ + shorten 압축, 문단 끝 자름). 재시작 대기 | 작업 21 의 `shorten.md` 방식을 limit>100 에도: 초과 시 "N자 이내로 같은 뜻으로 줄여" 1회 → 그래도 넘으면 문단 끝에서. 긴 문항 목표를 **1,400~1,800** 으로 내려 여유(26·28 과 같이). 검증: 세트 4건 잘림 0 |
| 28 | **Q3-1 품질 — 골든 예문 기준 구조 재설계** (기능 1회·경쟁사 이름은 근거 있을 때만·범용 도구 대비 필수·측정 가능한 기준·현재 단계 통일) + **경쟁사 조사 각도 보강** | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q3_1_QUALITY.md`**. 순서: 26 → 28 → 27 → 22 → 25 → 24 |
| 29 | **Q3-2 품질 — 골든 예문 기준 구조 재설계** (수익 축 ≤2·데이터 판매 금지·단위경제/매출추정 문단 삭제·가격 검증 방식·이용 패턴별 상품) | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q3_2_QUALITY.md`**. 현재 `q3_2.md` 가 단위 경제·매출 추정을 **필수**로 요구해 숫자를 지어내게 만듦. 순서: 26 → 28 → 29 → 27 → 22 → 25 → 24 |
| 30 | **Q4-1 품질 — 골든 예문 기준 구조 재설계** (1차/2차 MVP 분리·행동 지표·고객이 모인 곳·형태별 확장 축·참고자료 미주입·검증 목표 숫자) | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q4_1_QUALITY.md`**. **발견: 전 아이디어 공통 "베타 500명·유료 30명" 은 `q4_1.md` 의 예시 문구를 라마가 복사한 것** → 예시 삭제. 순서: 26 → 28 → 29 → 30 → 27 → 22 → 25 → 24 |
| 31 | **Q4-2 품질 — 골든 예문 기준 구조 재설계** (**강점 먼저 → 강점의 반대편 축을 요청** 상호보완 원칙(1-1절 표), capability 와 모순 금지, 요청=검증할 것+멘토 경험 지점+반영처, 역량 목록 문단 삭제) | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q4_2_QUALITY.md`**. 인테이크 mentoring 카드 보기도 같은 축으로(작업 24 와 연결). 순서: 26 → 28 → 29 → 30 → 31 → 27 → 22 → 25 → 24 |
| 32 | **Q8 품질 — 입력된 자산만 사업화 역량으로 번역** (신분·나이 기반 추론 금지, 강점→경험→활용 순서, 강점 유형별로 앞세우는 것이 바뀜, 참고자료 미주입, "결과물 없다·팀 필요 없다·데이터 판매" 금지) | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q8_QUALITY.md`**. 검증: 같은 아이디어에 capability 4종(컴공/영업직/요식업 20년/없음)으로 강점 문단이 달라지는지. 순서: 26 → 28 → 29 → 30 → 31 → 32 → 27 → 22 → 25 → 24 |
| 33 | **문항별 근거 배분 + 경쟁 조사** — 문항 역할 기반 조사 각도(problem/competitor/pricing/trend), facts 각도 태깅, `references_for` 를 각도·키워드 기반 무중복 배분으로, 근거 0건 문항엔 "참고자료 없음" 명시 | 세션3 | ✅ 코드 완료 (`df6b26d` 단계 B). 재시작 대기 — 비IT 검색어 품질은 작업 36 | `WORKORDER_QUALITY.md` 2절. 지금은 번호 순서 2건씩(임시) |
| 35 | **긴 문항 하한 — '빠진 문단 지정' 이어쓰기** (이어쓰기가 앞 문단을 되풀이해 refine ⑦이 지우면 다시 짧아짐) | 세션3 | 대기 (재측정 뒤) | 결정(세션1 16:4x): 당장은 **② 하한 1,100자**(`auto_extend.min_ratio` 를 문항별 하한으로) 로 가고, ③ 은 이 작업으로 — 각 문항의 QUALITY 문단 구조를 기준으로 **어느 문단이 비었는지 판정해 그 문단만 쓰게** 하는 이어쓰기(`extend.md` 에 `{missing_section}`) |
| 36 | **비IT 아이디어 조사 검색어 품질** — C(딸기 센터) 조사가 "인도 과일 주스 시장"·"K-푸드 수출" 을 가져와 Q2 가 인용 | 세션3 | 대기 (세션3 제보 17:3x) | `prompts/research/queries.md`·`followup_queries.md`: 검색어에 **국내·해당 분야·아이디어 핵심 명사 고정**, 해외·수출·대기업 각도는 아이디어가 그럴 때만. 결과 필터: 제목·본문에 핵심 명사 중 1개 이상 없으면 제외. 검증: 세트 C·F·G 조사 결과 표(검색어·상위 제목) |
| 37 | `polish.py`(모델 호출) 와 `postprocess.refine()`(순수 함수) 규칙 겹침 정리 | 세션3 | 대기 (낮음) | 세션2 제보. refine 이 잡는 규칙은 polish 에서 빼서 모델 호출 줄이기 |
| 34 | **판정 도구 확장** — `docs/tools/fullrun_repeat_check.py` 가 ACCEPTANCE A·B·C1·C7·D1·D3·H 를 자동으로 세고 표로 출력, 사람 판정 체크리스트(E·F·G) 템플릿 출력 | 세션1 | 대기 | `docs/ACCEPTANCE_CRITERIA.md` |
| 24 | **인테이크 카드 품질** — 실측(캠핑장, 14:1x): 카드 4장뿐(weak/missing 7슬롯 중 customer·evidence·capability·first_step 누락), 보기 전부 `source` 없음, 보기가 일반론("인터넷 검색", "word of mouth", "시장 분석") | 세션3 | ✅ 코드 완료 (`df6b26d` 단계 C). 재시작 대기 | 원본 `docs/measurements/2026-09-02_intake_camping.json`. 고칠 것: ① 공통 조사 facts 가 카드 보기에 실제로 들어가는지(이번 실측은 facts 0 → 원인 확인) ② weak/missing 슬롯은 **전부** 카드(7장 상한 안에서) — 특히 evidence·first_step ③ 보기는 MVP 단계 언어로: first_step = ⑥⑦⑧ 구체 행동 3개, evidence = 검증 방법 3종+인원 입력, alternative = 참고자료의 실제 경쟁 서비스명 ④ 영어 보기 금지 |
| 25 | **Q1·Q10 문장 품질** — 구체 기능 2~3개 필수, 정의형 허용, 추상 가치어 금지, **Q10 질문형 맺음 금지**(현재 `q10.md` 가 "질문형으로 끝내도 됩니다" 허용 → 약한 문장의 원인), 짜임 문항당 2종 | 세션3 | ✅ 코드 완료 (`8514ec1`·`df6b26d` 단계 A, 검증 50건 표는 세션3 로그). 재시작 후 세션1 재측정·사용자 판정 | **`docs/Q1_Q10_QUALITY.md`**. 검증: 세트 5분야 × Q1·Q10 = 10문장을 사용자 판정 |

### 작업 15 상세 — Q1 문체 다양화 (세션3, 프롬프트만)

- 증상(사용자, 실서비스): Q1 이 아이디어가 달라도 항상 "저는 20-30대 취업자와 가족 단위 캠핑객에게 캠핑장 빈자리 실시간 알림과
  당일 취소분 할인 예약 서비스를 제공하는 앱입니다." 꼴. 사람이 쓴 것처럼 문장 구조가 바뀌어야 함.
- 원인 (세션1 진단): ① `q1.md:21` "문장 끝은 '~하는 앱입니다', '~해 주는 서비스입니다'처럼 명사로 맺습니다" — 어미를 예시로 고정.
  ② `q1.md` "[필수 요소] 세 가지가 반드시 한 문장 안에" — 대상→가치→형태 순서로 채우는 틀이 됨.
  ③ `short_question.md:4` 가 "'저는 ~입니다' 로 시작하지 않는다" 인데 출력이 "저는" 으로 시작 — 라마가 이 규칙을 무시함 (①②가 더 강하게 작용).
  temperature 0.7 이라 샘플링 문제는 아님.
- 방향: `q1.md` 만 고친다. 어미 예시(①)를 지우고 **문장 구조 3~4가지를 제시해 아이디어에 맞는 것을 고르게** —
  예) 문제 먼저형("~때문에 곤란한 A 를 위해 …"), 장면형("주말 캠핑장 앞에서 발길을 돌리는 …"), 대비형("기존 X 대신 Y 로 …"), 결과형("… 을 30초 안에 끝내 주는 …").
  "저는 ~" 시작 금지와 "~에게 ~를 제공하는 앱입니다" 단일 패턴 금지를 q1.md 에 **직접** 명시 (short_question 에만 있으면 안 먹힘).
  필수 요소 3개는 유지하되 "순서는 자유" 를 붙인다.
- 검증: 같은 아이디어로 3회, 다른 아이디어 2개로 각 1회 실호출(라마, 한도 무관) → 5개 문장의 **첫 어절과 어미가 서로 다르면** 통과. 결과 문장 5개를 로그에 그대로 적기.
- `tests/test_prompts.py` 통과. 다른 문항 md 는 건드리지 않는다.
- **후속 (11:5x 세션1 승인, 세션3 제안 채택)** — 금지형은 라마가 우회하므로 **긍정형 단일 지시 + 코드 회전**으로:
  ① `assemble.build_prompts` 가 Q1 일 때 문장 구조 4종 중 **하나만** 골라(요청마다 회전 — job id·아이디어 해시 기반) 그 구조의 예시 1개(다른 도메인)와 함께 넣는다.
     md 에는 4종을 `{structure}` 자리로 두고 코드가 채운다 — 프롬프트 문구는 여전히 md 에만.
  ② 생성 후 정규식 검사(`제공(합니다|하는|하여)|앱입니다|서비스입니다` 로 맺으면) → **다른 구조로 1회 재생성** (Q1 은 100자라 비용 무시 가능, 라마 한도 무관).
  ③ 검증: 아이디어 3개 × 2회 = 6문장, 맺음말 6개 중 같은 것 2개 이하면 통과. 문장은 로그에 그대로.

### 작업 9 상세 — RAG 벡터DB (세션2 모듈 → 세션3 연결)

- 설계·결정·**모듈 인터페이스는 `docs/RAG_PLAN.md` 5·5-1절이 기준** (서명 변경은 세션1 승인 후).
- **소유권 예외 (세션1 승인)**: 세션2 가 `backend/rag/vectorstore.py`, `tests/test_vectorstore.py` 를 새로 만들고,
  `backend/requirements.txt` 에 llama-index 2줄만 추가한다. 그 외 `backend/**` 는 여전히 세션3 구역 — `pipeline.py` 는 건드리지 않는다.
- 세션2 완료 조건: fake embedding 으로 upsert(중복 url 재색인 없음)·query(score 붙음)·신선도 컷·is_sufficient 가 mock 테스트로 통과.
  ✅ 10:3x `bge-m3` 로컬 Ollama 설치 완료(세션1) — 세션2 는 mock 통과 후 실호출 1건으로 실제 임베딩 차원·persist 확인 가능.
- 세션3 연결(1단계 색인만 → 2단계 조회)은 세션2 커밋 뒤, 작업 12 3단계 어댑터 다음에.

### 작업 13 상세 — GPU 상한 실측 확대 (세션3 + 사용자)

- 목적: "맥스로 하면 몇 개까지 병렬로 처리되나, 그때 GPU 를 얼마나 먹나" — 작업 10 의 45 는 한계가 아니었다
  (KV 캐시 7%, waiting 0, preemption 0). 한계가 보일 때까지 올려서 잰다.
- 방법 (`scripts/gpu_probe.py`, 코드 수정은 필요한 만큼만):
  1. `--levels 60,80,100,150` — **실사용 길이 프롬프트**로 (참고자료 facts 8건 붙인 문항 생성 프롬프트, `max_tokens 1200`).
     작업 10 은 짧은 프롬프트라 KV 를 실제보다 적게 썼다.
  2. 단계마다 vLLM `/metrics`: `num_requests_running`·`num_requests_waiting`·`kv_cache_usage_perc`·preemption 을 1초 샘플링.
     **`running` 이 동시성보다 작게 머무는 지점 = vLLM `--max-num-seqs` 상한** → 그 위는 GPU 서버 설정을 바꿔야 함(리포 밖, 사용자).
  3. 사용자: 같은 시간에 PuTTY 로 GPU 서버에서 `nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.total --format=csv -l 1`
     → 단계별 최대치를 세션3에 전달 (GPU 사용률·메모리).
  4. 판정: KV 캐시 90% 근접 / p95 가 직전 단계보다 1.5배 이상 꺾임 / preemption>0 / 실패 발생 — 넷 중 하나가 걸리는
     **직전 단계가 안전 상한**. 본문:조사 = 4:1 로 나눠 `max_workers`·`research.llm.max_workers` 반영 → 재시작(사용자).
- 결과 표는 작업 10 형식 그대로 세션3 로그에. 요약(안전 상한·GPU 사용률)은 세션1이 PROGRESS 로 이관.
- ✅ 10:4x **nvidia-smi 는 세션1이 SSH(`ssh gpu`, 키 인증)로 직접 샘플링한다** — 사용자 PuTTY 불필요. 세션3 은 측정 시작·각 단계 시각만 로그에 남기면 세션1이 맞춰 정리.
  유휴 상태 실측: RTX PRO 6000 Blackwell 96GB × 3장. GPU1 90.2GB 사용(=vLLM 라마 70B AWQ 추정), GPU0 14.7GB(다른 사용처), GPU2 유휴. 사용률 0%.
  → vLLM 은 **한 장**에서 도는 것으로 보임. KV 캐시 여유 = 96GB − 모델 가중치(~40GB) 근처라 작업 10 의 KV 7%@45 와 부합.
- ⚠️ 공유 GPU 라 실행 시점은 사용자 승인. 라마 실호출 ~400회(측정용, 한도 무관).

### 작업 12 상세 — 자료조사 개편 (세션3)

- 전체 설계·API 한도·완료 조건은 **`docs/RESEARCH_PLAN.md`** 가 단일 기준.
- **1단계(두 줄)는 키 없이 지금 바로 가능**: `pipeline.py:141` `* 2` 제거 + `research.py:179`
  `d.news()` 제거(뉴스는 3단계에서 네이버 API 로 이관). 500명 기준 10만 → 4만 요청.
- 0단계(검색어 중복도 분석)는 라마 실호출 필요 — 한도 부담 없음, 결과는 RESEARCH_PLAN 에 기록.
- 3단계 어댑터+mock 테스트는 키 발급 전에 선작업 가능. 실키 연결만 키 이후.
- 5단계의 프론트 진행 표시는 세션2 V1(큐 전환)과 맞물림 — 인터페이스 바뀌면 여기 공지.
- **세션3 진행 순서 (10:2x 사용자 "진행" 지시)**: ① 작업 13 GPU 실측(사용자 nvidia-smi 동행, 짧음) → ② 네이버 HUB 규격 수정(🔴)
  → ③ 정형 API 어댑터 KOSIS·ECOS·data.go.kr(상권정보·K-Startup) + 도메인별 상한 → ④ 작업 9 파이프라인 연결.
  목표는 RESEARCH_PLAN 완료 조건 그대로: 1인 시나리오 외부 요청 40건 이하, **ddgs 5건 이하**.
- 🔴 **3단계 정정 (09:5x, 세션1 웹 검증)** — 네이버 검색 API 가 **NAVER API HUB 로 이관**됐다(2026-06-25).
  `research.py` `NaverSearch`(`23ad8de`) 는 구 규격이라 실키 연결 전에 고쳐야 한다. 정확한 값은 RESEARCH_PLAN "API 한도" 절:
  ① `base_url` → `https://naverapihub.apigw.ntruss.com/search/v1`, 경로 `/{kind}` (`.json` 없음)
  ② 헤더 → `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` (env 이름 `NAVER_CLIENT_ID/SECRET` 은 그대로)
  ③ `NAVER_ENDPOINTS` 에서 `doc`(전문자료) 제거 — 2026-07-31 종료. 학술은 KCI → OpenAlex/Semantic Scholar 로 (KCI 키 `KCI_API_KEY`)
  ④ 오류 본문 2형태(`errorCode` 평면 / `error.errorCode` 중첩) 모두 파싱, 429 는 서킷브레이커 대상
  ⑤ 한도 상수·주석 25,000/일 → 월 775,000·50 RPS 로. 기존 mock 테스트는 새 규격으로 갱신.

### 작업 10 상세 — GPU 한계 측정과 워커 상향 (세션3 + 사용자)

**현황 근거** (어제 측정, PROGRESS·BOTTLENECKS):
- 백엔드 큐 `max_workers: 12` (본문) + 4 (조사) — 부하테스트 내내 `running=12` 고정, 대기 최대 90건.
  **대기(2분49초)가 실행(24초)의 7배 — 병목은 GPU 가 아니라 우리 쪽 워커 수.**
- vLLM 직접 45 동시 호출은 45/45 성공, 73초 — GPU 쪽에 여유가 있다는 뜻.

**측정 (코드 수정 없음, 세션3):**
1. vLLM `/metrics` 를 터널(30800) 경유로 조회 — `vllm:num_requests_running`,
   `vllm:num_requests_waiting`, KV 캐시 사용률이 핵심 — **이 vLLM 의 실제 지표명은 `kv_cache_usage_perc`** (9/2 스모크로 확인).
   SSH 필요 없음 — 백엔드가 쓰는 같은 포트다.
2. `scripts/load_test.py` 로 동시성을 **단계적으로** 12 → 20 → 30 → 45 올리며
   문항당 p50/p95 실행 시간과 `gpu_cache_usage_perc` 를 기록.
   **KV 캐시가 90% 근처로 가거나 p95 가 꺾이는 지점의 한 단계 아래가 안전 상한.**
3. `nvidia-smi` (GPU 사용률·메모리)는 사용자가 PuTTY 로 원격 서버에서 확인해 값만 전달.

**적용:** `backend/config.yaml` `max_workers` 를 측정된 안전 상한으로 (한 줄).
되돌리기도 한 줄이라 위험 낮음. 조사 워커(4)도 같은 방식으로.

**⚠️ 조율:** 부하 테스트는 공유 GPU 서버에 실부하를 건다 — **실행 시점은 사용자 승인 후**
(한가한 시간대 권장). GPU 서버 자체 설정(vLLM 재기동, `--max-num-seqs` 등)은 이 리포 밖 —
필요해지면 사용자가 직접.

### 작업 11 상세 — 로컬 트랙·스토리/간결 스타일 UI 제거 (세션2)

- 결정(사용자, 9/2): **일반/기술 트랙만, 논리·근거형 스타일만** 쓴다.
- 방법: **프론트에서만 제거** — `App.jsx` 의 `TRACKS`(6행)에서 local 항목,
  `STYLES`(23행)에서 story·plain 제거(또는 선택 UI 숨기고 logic 고정).
  트랙 선택 화면(509행)과 스타일 선택(597행)이 항목 1개가 되면 화면 자체를 접는 것도 고려.
- **백엔드 프롬프트(`tracks/local.md`, `styles/story.md`·`plain.md`)와 API 는 그대로 둔다** —
  테스트가 파일 목록을 검사하고, 되살릴 때 버튼 복원만으로 끝나게.
- 효과: 문항당 생성 호출이 1회로 고정 → 부하 계산 단순화 (V8 완화).

### 작업 6 상세 — 모델 표기 (세션3 + 세션2)

- 세션3: `backend/config.yaml:36` `name: Llama 3.3 70B (사내 GPU)` → `백석대학교 로컬 LLM`
  (/models 응답으로 프론트 선택 목록에 내려감. id 는 그대로 두기 — 실제 모델 지정에 쓰임)
- 세션2: `frontend/src/App.jsx` 의 사용자 노출 문구 3곳 — 156행("라마 70B"), 592행 등
  "로컬 라마 70B" → "백석대학교 로컬 LLM" 으로. 주석(45·152·231행)은 그대로 둬도 됨.
- 공개 반영은 dist 재빌드 필요 → 배포 시점은 "조율 필요" 참조.

### 작업 7 상세 — 근거 소스 다양화 (세션3)

- 증상: 어떤 때는 근거자료가 같은 신문사·같은 글에 편중됨.
- 방향: `backend/rag/pipeline.py` `collect_pages`/`extract_facts` 에서
  ① 페이지 수집 시 **도메인별 상한** (예: 같은 도메인 최대 2건)
  ② facts 선별 시 같은 url·publisher 편중이면 다른 소스 우선
  ③ 검색어 생성 프롬프트(`prompts/research/queries.md`)에 "뉴스·통계·보고서를 섞어라" 지시 검토.
- 검증: mock 테스트로 도메인 상한 동작 확인 (`tests/test_research.py` 에 추가).

### 작업 8 상세 — 검색 백엔드 추가 (세션3, API 키 나온 뒤)

- 현재: ddgs(덕덕고) 단독 — 무료·비공식이라 수백 명 규모에서 봇 판정·차단됨 (BOTTLENECKS 1층).
- 방향: `backend/rag/research.py` 의 `Researcher(backends=[...])` 가 이미 플러그인 구조.
  ① **네이버 뉴스/블로그 검색 API** — 공식 키, 일 25,000회 무료. 한국어 뉴스 품질도 ddgs 보다 좋음
  ② **공공데이터포털(data.go.kr)·KOSIS API** — 통계 근거용, 차단 걱정 없음
  ③ 순서: naver → ddgs(폴백) 로 두면 ddgs 의존이 자연히 줄어듦. 키는 `.env` 로 (커밋 금지).
- 키 없이도 어댑터+mock 테스트까지는 선작업 가능. 실키 연결만 키 발급 후.
- 작업 7(다양화)과 같은 파일을 만지므로 **세션3이 이어서 한 묶음으로** 처리 권장.

### 작업 1 상세 — V1 (세션2)

- 현재: `frontend/src/api.js` 의 `generate()`/`extend()` 가 동기 `POST /generate` 를 부르고 3분간 연결을 붙잡음.
- 목표: `POST /jobs/generate` → 응답의 job id 로 `GET /jobs/{job_id}` 폴링(2~3초 간격) 전환.
  - 큐가 주는 `position`(대기 순번)을 화면에 표시 (V5)
  - job id 를 `sessionStorage` 에 보관해 새로고침해도 이어받기 (V6)
- 건드릴 파일: `frontend/src/api.js`, `frontend/src/App.jsx`. 백엔드는 완성돼 있음 — **수정 금지**.
  응답 스키마는 `backend/main.py:261~284`, `backend/llm/jobs.py` 를 읽고 맞추기.
- **같이 고칠 버그 (2026-09-02 발견)**: 조사 결과 메모(`researchRef`, `App.jsx:305 ensureResearch`)가
  문항 id 로만 키가 잡혀 있고 localStorage 에서도 복원돼서, **아이디어를 바꿔도 옛 아이디어의
  조사 결과를 그대로 재사용**한다 (조사가 아예 생략되거나 엉뚱한 참고자료가 끼어듦).
  실측: 9/2 아침 생성 34건 동안 `/research` 호출 0회. 아이디어(또는 트랙)가 바뀌면
  `researchRef`·`researchState` 를 비우도록 수정. 참고: 조사 실패 시 `[]` 를 넣는데 JS 에서
  빈 배열은 truthy 라 재시도도 영영 안 된다 — 의도인지 확인하고 주석 남기기.
- 검증: `npm run dev` (:5173) 로 로컬 확인 → `npx vite build` 통과.
- ⚠️ **`frontend/dist` 재빌드는 즉시 공개 배포다** (nginx 가 dist 를 그대로 서빙).
  개발 중엔 dev 서버로만 확인하고, dist 빌드·반영은 사용자 확인 후.

### 작업 2 상세 — V4 (세션3)

- 목표: 클라이언트당 동시 작업 수 제한 (인증이 없으므로 우선 IP 기준, 예: 동시 3건).
  초과 시 429 + 안내 메시지.
- 건드릴 파일: `backend/llm/jobs.py`, `backend/main.py`, `tests/test_jobs.py`(테스트 추가).
- 제약: `POST /jobs/{kind}` / `GET /jobs/{job_id}` / `GET /jobs` **응답 필드 삭제·개명 금지** (세션2가 의존).
  필드 추가는 허용 — 추가하면 아래 "조율 필요"에 공지.
- 검증: `.venv/Scripts/python -m pytest -q` 전체 통과 (현재 87 passed). 실모델 호출 없이 mock 으로.
- 참고: 개발용 백엔드가 필요하면 **:8001** 로 (`uvicorn backend.main:app --port 8001`). :8000 은 운영 중.

## 조율 필요 / 결정 대기

- ⚠️ **프롬프트 md 는 저장 즉시 운영 반영** (요청마다 다시 읽음, 서비스가 이 폴더에서 돔) — 프롬프트 작업(15·16)은 **git worktree** 에서 하고 검증 후 merge. 절차 `docs/MVP_PROCESS.md` 4절. 원복: `git checkout prompts-before-mvp-2026-09-02 -- backend/prompts`
- [x] **모델 결정** — 13:5x 사용자: **② 라마 단독 유지 + 후처리**. OpenRouter 는 백업 설정(`config.yaml.openrouter.bak`)으로만 유지
- [ ] **SSH 터널 서비스화** (사용자 승인): nssm install 은 관리자 권한 필요, 키 인증 전제
- [x] dist 재빌드(=배포) 시점 — 08:57 사용자 승인으로 배포 완료 (`index-CG-vzjlN.js`).
  ⚠️ 09-02 08:18 세션2가 문법 검증용 `npx vite build` 로 미검증 작업본을 오배포 → 직전 커밋 빌드로 복구 진행.
  재발 방지: 문법 검증은 `--outDir dist-check` 로 (CLAUDE.md·TEAMWORK 규칙화 완료)
- [~] **API 키 발급** (사용자만 가능, 작업 12 3단계 전제 — 상세 한도는 RESEARCH_PLAN 참조):
  - ✅ **네이버 (API HUB) 09:4x 발급 완료** — `.env` 에 `NAVER_CLIENT_ID/SECRET` 들어감. 세션1이 파이썬으로 실호출 1건
    (`/search/v1/news` query=창업) → 200, UTF-8 한글 정상, total 478만. **세션3: 어댑터를 HUB 규격으로 고친 뒤 실키 검증 1~2건 가능.**
    운영 `mochang-api` 는 아직 재시작 전(구 어댑터라 재시작해도 네이버 호출은 실패함 → 어댑터 수정 후 재시작).
  - ✅ 10:0x `.env` 현황(값 확인 안 함, 이름만): `DATA_GO_KR_API_KEY`·`KOSIS_API_KEY`·`ECOS_API_KEY` 들어감.
    ~~`KCI_API_KEY` 승인 대기~~ → **11:5x KCI 발급 완료, 12:0x `.env` 투입 확인(세션1, 이름만)**. 세션3 작업 17 실키 검증 가능. 이용 준수 사항 6개를 RESEARCH_PLAN "API 한도" 절에 코드 규칙으로 명시 —
    메타데이터만·벡터DB 제외·UI 출처 표기 필수. `KIPRIS_API_KEY` 미신청. data.go.kr 는 상권정보·K-Startup 활용신청 기준.
  - 네이버 → **NAVER API HUB** (ncloud.com 계정 → 콘솔 > Application Services > NAVER API HUB > Application 등록 > 검색 API).
    developers.naver.com 은 신규 신청 종료. 뉴스·블로그·웹문서 공용, 전문자료는 종료됨. 콘솔에서 한도 알림 설정
  - KCI OpenAPI (kci.go.kr) — 학술 근거용 키. OpenAlex·Semantic Scholar 는 키 불필요
  - 공공데이터포털(data.go.kr) → 활용신청 → 인증키 (상권정보·K-Startup 포함. **오픈 전 운영계정 전환 필수**)
  - KOSIS(kosis.kr), 한국은행 ECOS(ecos.bok.or.kr), KIPRIS(특허), 빅카인즈 — 각 자체 발급
  - 발급되면 `.env` 에 넣고 (커밋 금지) 세션3에 알리기. 키 복수 발급 우회 금지

- [ ] 📌 **긴 문항 하한 미달 (세션3, 09-02 단계 A 검증 34/40 · 연결 검증 7/9)** — 라마 이어쓰기가 앞 문단을 되풀이해 refine ⑦ 이 문항당 6~22문장을 지우면 다시 짧아진다.
  선택지: ① 반복 없는 800~1,600자 그대로(현행) ② 하한을 1,100자 선으로 ③ 이어쓰기를 '빠진 문단 지정' 방식으로(문단별 생성, 큰 변경). 세션3 로그 참조.
- [ ] ⚠️ **비IT 아이디어 조사 검색어 품질 (세션3)** — C(딸기 센터) 조사가 '인도 과일 주스 시장'·'K-푸드 수출' 을 가져와 Q2 가 인용함. 검색어 생성 프롬프트(`research/queries.md` 계열)를 아이디어 고유 명사 중심으로 좁히는 작업이 필요(작업 번호 배정 요청).

## 세션 로그

> 형식: `- HH:MM 시작|진행|완료|막힘 — 내용 (커밋 해시)`

### 세션1 (조율)

- 2026-09-02 세션 시작. TEAMWORK.md·WORKBOARD.md 생성, 작업 1·2 배정.
- 세션3 커밋 4개(`1bb3ad0`~`23ad8de`) push 완료. `.env.example` 에 NAVER 2줄 추가(세션3 요청).
- 08:18 dist 오배포 사고 조율 — 직전 커밋 빌드로 복구 결정(사용자 승인), 문법 검증을 `dist-check` 로 규칙화 (CLAUDE.md·TEAMWORK 반영).
- 세션3의 0단계 실측 표는 세션3 로그가 원본 — RESEARCH_PLAN 이관은 2단계 설계 확정 때 함께.
- 09:2x 세션 비정상 종료 → 재시작. 파일 기준으로 복구, 유실 작업 없음.
- 09:3x **실서비스 검증 + 워커 반영 확인** (공개 URL `/api/health`·`/api/jobs`·`/api/models`).
  08:57 재시작이 `d7d34a9`(08:59) 보다 2분 먼저라 워커가 12 로 남아 있던 것 발견 → 큐 비어 있음(running 0) 확인 후
  사용자가 `nssm restart mochang-api` → `max_workers: 30`, `max_per_client: 3` 확인. 작업 1·6·10·11 배정표 ✅ 갱신.
  남은 브라우저 확인 1건(생성 중 F5 → 이어받기)은 사용자 몫.
- 세션3 📌 (`/research`=research_client, `/jobs/research`=client 불일치) 판단: **큐 경로도 `research_client` 로 통일**
  (`main.py:257` 한 줄). 지금은 같은 라마라 결과 동일 — 세션3 다음 작업 때 같이 처리하면 됨.
- 세션2 다음 배정: **프론트 UX 개선 후보 목록** (구현은 승인 후, 배포 금지).
- 09:5x 사용자 제보(네이버 API HUB 이관·전문자료 종료) 웹 검증 → 사실. RESEARCH_PLAN·.env.example 정정, 세션3에 `NaverSearch` 규격 수정 지시(작업 12 상세 🔴).
- 10:1x 사용자 지시 2건 접수: ① GPU 상한 실측 확대 → **작업 13** 등록(세션3). ② RAG 벡터DB(작업 9)·다양한 소스(12-3단계) 진행 확인 →
  둘 다 미구현. 작업 9 는 사용자 결정 3개(임베딩 bge-m3 / 임계 0.8·5건 / 신선도 2년) 대기, 결정되면 세션2에 `backend/rag/vectorstore.py` 독립 구현 배정 제안.
- 10:2x 사용자 "진행" — 작업 9 결정 3개 추천안 확정, RAG_PLAN 5-1절에 모듈 인터페이스 고정. 작업 9 세션2 배정(소유권 예외), 세션3 순서 ①13→②네이버→③정형API→④RAG 연결.
- 10:5x 세션2·세션3 결과 검수 — pytest **124 passed** 로컬 재확인, 커밋 전부 push 됨.
  작업 9 모듈(세션2) ✅, 작업 13 측정(세션3) ✅ 36+9, 네이버 HUB 어댑터+실키 검증(세션3) ✅.
  ⚠️ nvidia-smi 샘플링은 **못 했음** — 세션3 측정이 제게 알림 없이 먼저 끝남. KV 45%@100 이라 상한 판단엔 영향 없음.
  필요하면 60 단계 1회만 재측정하며 GPU 사용률 확인 가능(라마 60회). 운영 반영은 재시작 1회(36/9 + 네이버 검색 활성화).
- 11:0x **재시작(사용자) → 운영 검증**: `/api/health` `max_workers: 36`. 공개 URL 로 조사 1건(새 아이디어, 라마 실호출 1건):
  60초, `backend: naver`, 검색어 4개(공통 조사만, 보완 0), 페이지 6건 전부 네이버 뉴스(아이뉴스24·뉴스핌·머니투데이·노컷·메트로 등 6개 매체),
  facts 4건 — **ddgs 0건, 소스 편중 해소**. 관찰(세션3 참고): 같은 기사(100세 이상 고령자 8,604명)가 3개 매체로 3건 중복 —
  도메인 상한으로는 못 거르니 3단계에서 **제목 유사도 중복 제거**도 같이 검토.
- 11:3x 세션3 커밋 2개(`c747955`·`12d44a5`) 검수 — pytest **152 passed** 재확인, push 완료. 작업 12 3단계·작업 9 색인 연결 ✅.
  세션3 제안(min_score 0.8 → 0.5~0.6) 수용 → **0.6** 으로 결정(RAG_PLAN 5절 갱신). 색인은 지금부터 쌓이도록 `enabled: true` 로 켜기로.
- 11:3x 사용자 지적 "Q1 문장이 늘 같은 틀" → 원인 진단(`q1.md:21` 고정 어미) → **작업 15** 세션3 배정.
- 11:4x 사용자 지시 "프롬프트 백업 + 창업교육 MVP 8단계에 맞춰 개선" → 백업(태그+사본), `docs/MVP_PROCESS.md`(매핑·개선 기준·검증) 작성, **작업 16** 세션3 배정.
  발견: 프롬프트가 요청마다 디스크에서 읽혀 **편집 즉시 운영 반영** → 프롬프트 작업은 worktree 규칙 추가.
- 11:5x KCI 키 발급 → 이용 준수 사항을 RESEARCH_PLAN 에 코드 규칙 6항으로 번역, **작업 17** 배정(세션3 어댑터·세션2 표기).
- 11:5x 세션2 배포 검수 — 공개 URL 번들 `index-vkBBBKoM.js`·`index-Da5jo6yn.css` 확인, 워커는 이미 36(세션2 메모는 재시작 전 정보). 작업 14 ✅.
  세션3 작업 15 부분 성공(`ff2ef57`) — 후속으로 코드 회전+재생성 승인. ⚠️ 세션3 이 q1.md 를 본 작업 폴더에서 편집(운영 즉시 반영) — 커밋된 상태는 이전보다 나으므로 원복 안 함, 이후 프롬프트 작업은 worktree.
- 12:1x KCI 캐시 판단: 사용자 해석(원문 저장 금지·발췌 인용 허용) + 캐시 실측(107건 전부 초록 스니펫 ≤536자, PDF 0) → **삭제 안 함**, 준수 1·2항 정정 (`3fe8573`).
- 12:3x 세션3 커밋 검수(9 조회·15 후속·16·17·18) — pytest **197 passed**, push 완료. 작업 9·15·16·17·18 ✅. 12 는 4·5단계만 남음(급하지 않음).
- 12:4x **운영 검증** (12:28 재시작분, 공개 URL 실호출 2건): 조사 → facts `source_kind` news 4·stat 1(정형 API 살아 있음), `backend: naver`,
  벡터DB `backend/.vectorstore/` 생성·갱신(2.5MB, 색인 시작). 생성 Q1 → 81자, 새 짜임("…비용을 줄여줍니다") 반영.
  단, 응답에 `auto_extended` 없음 = **작업 18(`941742d`)은 12:28 재시작 이후 커밋이라 미반영 → 재시작 1회 더 필요**.
- 12:5x 사용자 질문(속도 개선·ddgs 절감 효과·API 한도 위험) 분석 — timing.jsonl·캐시 실측:
  생성 대기 어제(워커12, 부하테스트 150건) p50 178s → 오늘(워커36) 0s, 실행 p50 25s 동일. 12:28 이후 검색 결과 소스: 네이버 96(news 48·blog 48)·stat 1·kci 3·**ddgs 0**.
  발견: pytest 가 timing 로그를 오염, 소스별 호출 카운터 없음, 서킷브레이커 없음 → **작업 19**. data.go.kr 개발계정 1,000/일은 수백 명 규모에서 한계 → 운영계정 전환(사용자).

### 세션2 (프론트)

- 13:2x **기준선 실측 (작업 20 전)** — 실서비스에서 세탁소 아이디어 1인분(인테이크 32s·조사·생성 8문항, 라마 ~20회). 원본 `docs/measurements/2026-09-02_baseline_laundry.json`.
  분량: 긴 문항 1,495~1,942자(전부 `auto_extended: true` = 작업 18 운영 반영 확인), Q1 81자·Q10 52자.
  반복: 유사 문장 쌍(4-gram Jaccard≥0.5) **6쌍** — 아이디어 재진술 첫 문장이 Q3-1·Q3-2 **완전 동일**, Q4-1 근사. 같은 통계 재인용: 한경 917→350(Q2·Q3-1), 침투율 10→20%(Q3-1·Q4-1), irobotnews 400억→1,300억(Q2·Q4-1·Q4-2), irobotnews "수거·배달 원함"(Q3-2·Q8). 참고자료가 8건뿐이라 전 문항이 같은 것을 돌려 씀.
  지어냄(금지 지시 무시): Q2 인터뷰·친구 일화, Q3-2 단위경제 수치, Q4-1 500명/30명/1억/배분율, Q8 "알고리즘 개발 가능". 이물: 途中·需求 → **작업 22** 제안.
  좋아진 것: Q4-1 이 MVP 기획→노코드 3개월→10명 사용성 테스트 뼈대(작업 16 효과).
- 13:5x 사용자 결정: **모델 라마 단독 유지**(후처리로 대응). 세션2 잔여 진행 → **작업 23** 배정(인테이크 문구·A1 큐 전환).
- 13:5x 세션3 작업 21·19·20 검수 — pytest **239 passed**, push. 실측이 설계를 뒤집은 2건(골자 전체 노출→반복 증가, 반복 주범은 공유 조사 인용문) 타당 — OUTLINE_PLAN 5절 주입 방식은 `slice_for` 기준으로 갱신 필요(세션1).
  측정 도구 수치 지표를 통계 인용(%·금액·억/만)으로 좁힘(`docs/tools`). **재시작 후** 세탁소 아이디어로 독립 재측정 + 명칭 통일 사람 판정.
- 14:1x **독립 재측정(세탁소, 개편 후, `docs/measurements/2026-09-02_after_laundry.*`)**: 통계 재인용 9종→**0** ✅ / 유사 문장 쌍 6→**10** ❌(전부 아이디어 재진술 첫 문장, 4문항) / '저희' Q3-2·Q4-1 잔존 / 이물 需求 2 / 상용구 500명·30명 Q4-1 / Q1 94자지만 추상적("효율화해 줍니다"), Q10 40자.
  인테이크 실측(캠핑장): 134초, 카드 4장, 보기 근거 0 → **작업 24**. 사용자 Q1 지적 → **작업 25**.
- 14:3x 사용자 Q2 피드백(골든 예문 + 진단 7항) → `docs/Q2_QUALITY.md` 로 규칙화, **작업 26** 최우선 배정.
- 14:4x 사용자 지시 "18개 분야·모든 아이디어에 일반화, 특정 케이스 편향 금지" → `docs/PROMPT_GENERALIZATION.md`(규칙 6 + 분야 검증 세트 7개 아이디어) + TEAMWORK 7절. 이후 모든 프롬프트 작업의 완료 조건. 이후 Q3-1·Q3-2·Q4-1·Q8 도 같은 방식(골든 예문+진단표)으로.
- 14:5x 사용자 Q3-1 피드백(골든 예문 + 진단 8항 + 한도 초과 잘림) → `docs/Q3_1_QUALITY.md`, **작업 28**(Q3-1 구조·경쟁 조사) + **작업 27**(긴 문항 압축 재생성). 순서 26 → 28 → 27 → 22 → 25 → 24.
- 15:0x 사용자 Q3-2 피드백 → `docs/Q3_2_QUALITY.md`, **작업 29**. 발견: `q3_2.md` 가 단위 경제·매출 추정을 필수 요소로 둬서 지어낸 숫자를 강제함 / 자동 이어쓰기가 본문을 통째로 복제 → 작업 22 ⑦.
- 15:1x 사용자 Q4-1 피드백 → `docs/Q4_1_QUALITY.md`, **작업 30**. 발견: "베타 500명·유료 30명"·"60/20/10/10" 이 전 아이디어에 같았던 이유는 `q4_1.md` 의 예시 문구 — 라마가 예시를 복사(작업 15 와 같은 패턴). 22 ③ 범위 축소.
- 15:2x 사용자 Q4-2 피드백 → `docs/Q4_2_QUALITY.md`, **작업 31**. 원인: `q4_2.md` 가 "역량 목록 5~7개 → 부족 2~3개" 체크리스트라 일반 명사로 채워지고, 인테이크 mentoring 보기가 실행 역량(제품 개발 등)이라 전공자가 스스로를 깎는 문장이 나옴.
- 15:4x 사용자 Q8 피드백(입력 자산만·인물상 전제 금지·강점 유형별 구조 변화) → `docs/Q8_QUALITY.md`, **작업 32**. 원인: `q8.md` 가 "결과물 없으면 밝혀라·투입 시간·1인 운영" 을 지시해 감점 문장을 만들고, Q8 에도 참고자료가 주입돼 2010년 기사가 끼어듦. "데이터 판매" 는 22 ⑧ 금지어로.
- 15:5x 사용자 Q10 피드백(질문형·기능 없음) → Q1 과 묶어 `docs/Q1_Q10_QUALITY.md`(작업 25 상세). 원인: `q10.md` 가 질문형 맺음을 권장하고 Q1 짜임 회전을 그대로 씀. 이로써 **문항 8개 전부 골든 기준 문서화 완료**.
- 15:5x 사용자 종합 지시(일반화·품질·한 글 흐름·문항별 근거·분량) → **`docs/WORKORDER_QUALITY.md`** 로 통합 지시문. 역할 분담: 세션3=프롬프트·조사·카드(단계 A→B→C), **세션2=생성 후처리 모듈(22+27, 새 파일, 소유권 예외)**, 세션1=재측정·판정. 작업 33 등록.
- 16:0x 사용자: 통과 기준 확장(분량 적정·한 글 흐름·반복·입력 기반 정확성·기타) → **`docs/ACCEPTANCE_CRITERIA.md`** A~H 8묶음 48항목. 자동/사람 판정 구분, 판정 절차 명시. 세션1 도구 확장 = 작업 34.
- 16:4x 세션2(작업 22+27 모듈, 334 passed)·세션3(단계 A·B·C + refine 연결) 검수 — pytest **380 passed**, push. 조율 2건 결정:
  ① 긴 문항 하한 미달 → **② 하한 1,100자 로 우선**(사용자 골든 예문이 750~1,100자라 1,400 하한은 과했음), ③ '빠진 문단 지정' 이어쓰기는 **작업 35**.
  ② 비IT 검색어 품질 → **작업 36**. polish/refine 겹침 → 작업 37(낮음). 운영 반영은 재시작 1회. 사용자 판정용 전문 12건은 세션3 로그.
- 14:0x **after3_A(냉장고 앱) 사람 판정** (원자료 `docs/measurements/after3_*.json`):
  ✅ 명칭 통일 — "1인 가구 주민", "사진 한 장으로 재고 등록·임박 레시피 추천" 이 Q1·Q3-1·Q4-1·Q8 에서 같은 말로 쓰임 (골자 효과 확인).
  ❌ **1인칭 불일치** — Q2·Q3-1·Q8 "저", Q3-2·Q4-1 "저희" (팀원 없음인데). 문항 간 통일은 system.md 로는 안 됨 → 작업 22 후처리 항목 추가.
  ❌ 지어냄 — Q4-1 "베타 500명·유료 30명·1,000만 원·60/20/10/10%" (세탁소 실측과 **똑같은 숫자** = 라마의 상용구). 이물 5건(生活·不断·まず·更多·需求).
  (정정 14:1x: Q2 "10명에게 확인"·Q8 "프로그래밍" 은 세션3 측정기가 카드 보기를 자동 선택한 값(evidence=10명, capability=프로그래밍 언어)이라 지어낸 게 아님.
  세탁소 기준선의 "세탁소 주인들에게 물어봤습니다" 는 evidence 입력이 없었으므로 지어낸 것 맞음.)
  → **작업 22 스펙 보강**: ① 이물 정규식 재생성 ② 인터뷰·인원·금액·% 문장 중 입력·참고자료에 없는 수치 제거/가정화
  ③ 라마 상용구 블랙리스트 — **매출·원가 상용구(1만 원·5천 원·1억)만**. 500명·30명·60/20/10/10 은 `q4_1.md` 예시가 원인이라 작업 30 에서 예시 삭제로 해결(Q4_1_QUALITY 5절) ④ 1인칭 통일(팀원 없음 → '저희' 0건) ⑤ 문항당 같은 통계 재인용 1회 초과 시 두 번째부터 삭제 ⑥ **아이디어 재진술 첫 문장 삭제** — Q1·Q3-1 이외 문항의 첫 문장이 아이디어 원문/골자 solution 과 4-gram Jaccard≥0.5 면 그 문장을 지운다(금지 지시는 라마가 무시하므로 코드로). 후측정에서 유사 쌍 10개 전부 이 유형. ⑧ **"데이터 판매" 전 문항 금지어** — 문장 삭제 + 골자 revenue 배제(고객이 맡긴 정보를 파는 수익원, Q3-2·Q8 실서비스 출력에서 재발). ⑦ **이어쓰기 중복 제거** — 자동/수동 extend 결과 중 기존 본문과 Jaccard≥0.5 인 문장은 버리고, 남는 문장이 없으면 이어쓰기 무효(Q3-2 실서비스 출력이 통째로 2번 반복됨, 15:0x).
- 시작 — 작업 1(V1 큐 전환 + researchRef 버그), 작업 11(로컬 트랙·스토리/간결 제거), 작업 6 프론트 몫.
  건드릴 파일: `frontend/src/api.js`, `frontend/src/App.jsx`. dist 는 건드리지 않음(사용자 확인 후).
- 08:18 사고·복구 — 문법 검증으로 `npx vite build` 를 outDir 없이 돌려 **`frontend/dist` 가 재빌드되며
  작업본이 그대로 공개**됐다. 08:26 직전 커밋(HEAD) 소스를 스크래치패드에서 빌드해 dist 를 원상 복구
  (`index-CS-PX4XH.js` 로 확인, 공개 사이트는 이전 버전으로 돌아옴). 이후 검증은 `--outDir dist-check` 로만.
  `frontend/.gitignore` 에 `dist-check` 추가. (규칙은 세션1이 TEAMWORK 4절에 반영함)
- 완료 — **작업 1(V1) + 작업 11 + 작업 6 프론트 몫** (`b4839a1`). `vite build` 통과, dist 미변경.
  - `api.js`: `runJob(kind, body, {onSubmit,onTick})` = 제출 → 2.5초 폴링, `followJob(jobId)` = 이어받기,
    `JobExpiredError`(404). `generate()`/`extend()` 가 이걸 쓴다. 제출 429 는 5초 뒤 재시도(최대 12회),
    폴링 중 네트워크 오류는 연속 3회까지 무시.
  - `App.jsx`: 대기 순번 표시(`JobProgress`), job id 를 `sessionStorage`(`modoo-writer-jobs-v1`)에 보관 →
    마운트 시 남은 작업을 이어받음. 조사 결과에 `(트랙|아이디어)` 서명을 붙여 아이디어가 바뀌면 폐기.
    조사 실패 문항은 ref 에 안 남겨 다음 생성에서 재시도.
  - 실호출 1회(라마, 무료 한도 아님): `POST /jobs/generate` → 폴링 → done 계약 확인.
    응답 필드가 프론트 기대와 일치(`job_id/position/status/queued_seconds/elapsed_seconds/result.text/model`),
    없는 job 은 404. **운영 :8000 백엔드는 아직 구코드** — `GET /jobs` 에 `max_per_client` 가 없음(=V4 미반영).

#### 사용자 검증 체크리스트 (dev 서버 :5173 띄워둠, 백엔드는 운영 :8000)

`http://localhost:5173` 에서 확인. 처음 열면 예전 저장본은 자동 정리된다(로컬 트랙·옛 스타일 제거).

1. **대기 순번** — 아이디어 입력 → "신청서 초안 만들기". 문항 카드 안에
   `작업을 대기열에 넣는 중… → 대기 중 — 다음 차례입니다 → 작성 중… (보통 30~60초)` 순서로 뜨는지.
   ⚠️ 혼자 쓰면 큐가 비어 있어 **"앞에 N건"은 안 뜨는 게 정상**이다(운영 워커 12개, 브라우저는 2건씩만 제출).
   숫자를 직접 보려면 dev 백엔드에서 `max_workers` 를 1~2로 낮춰야 하는데 `config.yaml` 은 세션3 파일 → 조율 필요.
2. **새로고침 이어받기** — "작성 중…" 이 떠 있을 때 F5. 그 문항이 다시 "작성 중…"으로 살아나고
   잠시 뒤 글이 채워지면 성공. (개발자도구 Application → Session Storage 에 `modoo-writer-jobs-v1` 확인,
   작업이 끝나면 키가 사라진다.) 서버가 그 사이 재시작됐다면 "이어받지 못했어요 (서버에서 만료됨)" 안내가 정상.
3. **조사 재사용 버그** — 초안이 다 나온 뒤 "입력 수정"에서 **아이디어를 다른 내용으로 바꾸고** 다시 생성.
   문항마다 `웹에서 근거 자료를 조사하는 중… (백석대학교 로컬 LLM)` 이 다시 뜨고, 조사 근거 패널의
   **검색어가 새 아이디어 기준으로 바뀌면** 수정된 것. (네트워크 탭에 `/research` 호출이 다시 보임.
   고치기 전에는 호출이 0회였다.) 아이디어를 그대로 두고 다시 생성하면 조사는 건너뛰는 게 정상.
4. **429 안내** — 생성이 도는 중에 다른 문항의 "새로 생성"을 3~4개 연달아 누르면 4건째부터
   `먼저 넣은 작업이 끝나기를 기다리는 중… (한 번에 3건까지)` 이 뜨고, 앞 작업이 끝나면 저절로 진행돼야 한다.
   ⚠️ **운영 :8000 은 아직 V4 구코드라 429 가 안 난다.** 확인하려면 dev 백엔드가 필요하다(PowerShell, 한 줄씩):
   ```
   .\.venv\Scripts\python -m uvicorn backend.main:app --port 8001
   ```
   ```
   "VITE_API_BASE=http://localhost:8001" | Out-File -Encoding utf8 frontend\.env
   ```
   그 뒤 dev 서버 재시작(:5173). 확인이 끝나면 `frontend\.env` 를 지워 운영 :8000 으로 되돌린다.

- **사용자 브라우저 검증 결과 (dev :5173)** — ②③④ 통과, ⑤(429)는 운영 백엔드 구코드라 미확인.
  - ② 새로고침 이어받기: F5 후 다시 "작성 중"으로 살아나 결과까지 도착 — 정상.
  - ③ 조사 재사용: 스탬프투어 → 음식 칼로리계산앱 으로 아이디어를 바꾸자 **조사가 새로 돌았다** — 버그 수정 확인.
  - ⑤ 트랙 선택 없음 + 스타일 1종 고정 — 정상.
  - 지적받아 고친 것 (`dae680c`): 이어받는 동안 "작업을 대기열에 넣는 중"으로 보이던 것 →
    "새로고침 전에 맡긴 작업을 이어받는 중…". 헤더의 "여러 스타일로 받아서 고르고" → "받아서 다듬고".
- **막힘/조율 필요 — 모델 드롭다운이 아직 "Llama 3.3 70B (사내 GPU)"** (사용자 지적).
  프론트는 `/models` 응답을 그대로 뿌리는 구조이고, `config.yaml` 은 이미 `백석대학교 로컬 LLM`(세션3 `1bb3ad0`).
  **운영 :8000 서비스가 그 커밋 이전 코드로 돌고 있는 게 원인** — `GET /jobs` 에 `max_per_client` 가 없는 것과 같은 이유다.
  → `mochang-api` 재시작이면 해결되지만 공개 서비스 중단이라 **사용자 승인 필요**. 이름을 프론트에 하드코딩하지 않았다.
  재시작하면 작업 6(프론트+백엔드)과 ⑤(429) 확인이 같이 풀린다.
- **08:57 공개 배포 (사용자 승인)** — `npx vite build` 로 dist 재빌드 → `index-CG-vzjlN.js` 서빙 중.
  `b4839a1`+`dae680c` 가 공개됨. 배포 번들은 `.env.production` 대로 API 주소가 `/api`(같은 오리진 → nginx 프록시).
  같은 시점에 사용자가 `mochang-api` 를 재시작해 백엔드도 최신 코드로 올라옴(`/jobs` 에 `max_per_client:3` 확인).

#### 실서비스 검증 결과 (https://bustartup.kr:50001)

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | 모델 선택에 "백석대학교 로컬 LLM" | ✅ | 공개 URL `GET /api/models` → `"name":"백석대학교 로컬 LLM"`. 프론트는 이 목록을 그대로 렌더 |
| 2 | 로컬 트랙·스타일 선택 UI 제거 | ✅ | 배포 번들 문자열: `로컬 분야` 0건, `스토리텔링형` 0건, `간결·실무형` 0건 / `지원 분야` 1건 |
| 3 | 새 아이디어 생성 → 순번 → 결과 | ✅(API 레벨) | 새 아이디어(고령자 스마트 약통)로 `POST /api/jobs/research` → `position=0` → running → **113.9초** done, 이어서 `jobs/generate` → **27.7초** done, 1447/2000자 |
| 4 | 생성 도중 새로고침 → 이어받기 | ⏸ 미확인 | 브라우저 조작이 필요. dev :5173 에서는 사용자가 이미 통과 확인 |
| 5 | 결과에 조사 근거(출처) | ✅ | 검색어 6개 새로 생성(`cached:false`), 페이지 9건 → 근거 **8건 전부 URL 보유**, 그 facts 를 references 로 넘겨 생성 |

- 검증에 쓴 실호출: 라마 조사 1건 + 생성 1건 (무료 한도와 무관).
- ⚠️ **관찰(제 구역 아님, 세션3 작업 12-3단계 소관)**: 이번 조사에서 근거 8건 중 상위 4건의
  publisher 가 전부 `한국전자통신연구원` 이었다 — 소스 편중이 실서비스에서도 재현됨.
- 1·2·5 는 배포 산출물·공개 API 로 확인한 것이고, **화면으로 눈으로 본 확인과 4번은 브라우저 도구가 켜져야 가능**.
- 09:1x 사용자가 `/chrome` 실행·확장 설치했으나 **이 세션에는 브라우저 도구가 등록되지 않음**
  (`Skill(claude-in-chrome)`→Unknown skill, `ToolSearch` 로 `mcp__claude-in-chrome__*` 조회 실패).
  세션 시작 후에 붙은 도구라 등록이 안 된 것으로 보임 → **세션 재시작(`claude --continue`) 후 재시도**하거나
  사용자가 직접 화면 확인. 스크린샷 검증은 **미수행**(없는 결과를 기록하지 않음).


#### 프론트 UX 개선 후보 (세션1 배정, 2026-09-02 — **구현·배포 전 승인 필요**)

`App.jsx`(944행)·`api.js`(147행) 전수 검토. 취향 항목은 뺐고, 재현 경로가 있는 것만 적는다.

**A. 결함급 — 사용자가 멈춘 줄 알거나 결과를 잃는다**

| # | 문제 | 근거 | 고치는 법 |
|---|---|---|---|
| A1 | **조사·인테이크가 아직 동기 호출**이라 큐를 안 탄다 | `api.js:39·45·53` 이 `POST /intake`·`/intake/regenerate`·`/research` 를 직접 호출. 백엔드엔 이미 `/jobs/intake`·`/jobs/research` 가 있다(`main.py:251~258`). 실측 조사 1건 **113.9초** — 그동안 HTTP 연결을 붙잡고, 새로고침하면 통째로 유실되며, `max_jobs_per_client` 집계에도 안 잡힌다 | V1과 같은 `runJob("research"/"intake", …)` 로 전환. **선행**: 세션1이 적은 `/jobs/research`→`research_client` 통일(`main.py:257`) 뒤에 붙이는 게 안전 |
| A2 | 새로고침하면 **아직 제출 안 된 문항은 영영 안 만들어진다** | 이어받기(마운트 useEffect)는 `sessionStorage` 의 job id 만 복원한다. `generateAll` 의 `runLimited` 큐(아직 차례가 안 온 6~7문항)는 복원 대상이 아니다 → 9문항 중 2~3개만 채워진 채 멈춘다 | 이어받기가 끝난 뒤 "본문이 빈 문항"을 이어서 생성하거나, 상단에 `남은 N개 계속 만들기` 버튼 |
| A3 | 조사 차례를 기다리는 문항 카드가 **완전히 빈칸** | `generateAll` 은 조사 3병렬. 아직 차례가 안 온 문항은 `researchState[qid]`·`status[qid]` 둘 다 없어 `ResearchPanel`=null, 본문 블록 3개가 전부 false → 제목만 있는 빈 카드가 6~7개 늘어선다 | `running` 중 상태 없는 문항에 "차례를 기다리는 중" 한 줄 |
| A4 | **첫 문항 조사만 4배 느린데** 표시가 같다 | 세션3 2단계: 공통 조사(모델 4회)는 첫 문항에서만 돈다. 화면 문구는 모든 문항이 `웹에서 근거 자료를 조사하는 중…` 하나뿐 | 첫 문항에 "아이디어 공통 조사 중 — 처음 한 번만, 1~2분" 별도 문구 |
| A5 | 조사 결과 `pages` 를 **쓰지도 않으면서 localStorage 에 저장** | `App.jsx:393` 이 `pages` 를 넣는데 렌더에서 참조 0회(`ResearchPanel` 은 facts/queries/cached 만 씀). 9문항×수 KB → 용량 초과 시 `saveState` 가 통째로 실패해 **초안까지 저장이 안 된다** | 저장 직전에 `pages` 제외 |

**B. 공개 서비스에 안 맞는 문구·설정**

| # | 문제 | 근거 | 고치는 법 |
|---|---|---|---|
| B1 | 백엔드 다운 시 일반 사용자에게 **개발자 명령어**를 보여준다 | 헤더 하단 `uvicorn backend.main:app --port 8000 을 실행해 주세요` | "서버와 연결이 끊겼어요. 잠시 후 다시 시도해 주세요" |
| B2 | 모델이 1개인데 **드롭다운**이 있다 | `/models` 목록이 지금 1개 | `models.length <= 1` 이면 텍스트로 |
| B3 | 브라우저 동시 제출 `PARALLEL = 2` 가 서버보다 보수적 | `App.jsx:41`. 서버는 워커 30, IP 당 3건 허용 | 2 → 3. 9문항 기준 5배치 → 3배치 |
| B4 | 복사 실패가 조용하다 | `copy()` 가 `navigator.clipboard?.` — 없으면 아무 반응 없음 | 실패 시 "복사 실패 — 직접 선택해 복사하세요" |

**C. 있으면 좋은 것**

- C1 초안 화면 상단 **진행 바**(`3/9 작성됨`) + 미완성 문항으로 점프. 지금 `doneCount` 는 "선택됨" 기준이라 진행률과 다르다.
- C2 조사 패널에 세션3 신규 필드 활용 — `shared_queries`(공통) / `followup_queries`(이 문항 보완) 분리 표시. A4 가 자연히 설명된다.
- C3 `q.limit` 초과분을 `slice` 로 말없이 자른다(`runQuestionJob`) → "2000자에서 잘렸어요" 안내.
- C4 진행 표시에 `aria-live`, 아이콘 버튼에 `aria-label` (공공 서비스 접근성).
- C5 `새로 시작` 의 `confirm()` → 인앱 확인 UI.

**권장 순서**: A5·B1·B2·B4 (각 1~3줄, 위험 0) → A3·A2 → B3 → A1(백엔드 선행 필요) → C.
**A1 은 조율 필요** — 세션3의 `main.py:257` 정리 뒤에 붙는다.


- 완료 — **UX 개선 A2~A5 + B1~B4** (`c1dd0e4`). `vite build --outDir dist-check` 통과, **dist 미변경(배포 안 함)**.
  - A2 `generateMissing()` 신설. 마운트 이어받기가 **끝난 뒤** 본문이 빈 문항을 이어서 만든다.
    최신 `texts` 를 봐야 해서(마운트 클로저는 낡는다) `textsRef` 를 따로 둔다. 초안 화면 상단에
    `남은 N개 계속 만들기` 버튼도 추가 — 이어받기 말고 다른 이유로 빠진 경우까지 덮는다.
  - A3 `!st && running` 인 문항에 회색 자리표시 + "차례를 기다리는 중…" (조사 중이면 파란 패널이 대신 설명).
  - A4 `ResearchPanel({ warm })` — 이 아이디어로 조사 결과를 아직 하나도 못 받았으면
    "아이디어 공통 조사부터 합니다 — 처음 한 번만 1~2분". `dropStaleResearch` 에서 같이 초기화된다.
  - A5 저장 직전 `pages` 제거(`leanResearch`). 예전 저장본에 있던 pages 는 다음 저장 때 자연히 빠진다.
  - B1 `uvicorn …` 명령 제거 → "서버와 연결이 끊겼어요. 잠시 후 새로고침해 주세요." 주소·원인은 `title` 로만.
  - B2 `models.length <= 1` 이면 드롭다운 대신 글자.
  - B3 `PARALLEL 2 → 3`. 서버 워커 30 · IP 당 3건이므로 429 는 안 난다(제출 429 재시도 로직은 그대로).
  - B4 `copy()` 가 `navigator.clipboard` 부재·거부를 잡아 버튼 문구를 "복사 안 됨 — 직접 선택하세요"로.
  - **A1(조사·인테이크 큐 전환)은 안 했다** — 세션1이 적은 `main.py:257`(`/jobs/research` → `research_client`)
    정리 뒤에 붙이는 게 안전. 그때 `api.js` 에 `runJob("research"/"intake", …)` 추가하면 된다.

#### 사용자 검증 체크리스트 2차 (dev :5173, HMR 로 이미 반영됨 — 백엔드는 운영 :8000)

1. **A2** 생성이 도는 중에 F5 → 이어받은 문항이 끝난 뒤 **나머지 문항도 저절로 이어서** 만들어지는지.
   (전에는 2~3개만 채워지고 멈췄다.) 중간에 멈춰 있으면 상단 `남은 N개 계속 만들기` 가 보여야 한다.
2. **A3** 초안 화면에 들어간 직후, 아직 차례가 안 온 문항 카드에 **회색 상자 + "차례를 기다리는 중…"** 이 보이는지.
   (전에는 제목만 있는 빈 카드였다.)
3. **A4** 새 아이디어로 처음 만들 때 첫 문항에 **"아이디어 공통 조사부터 합니다 — 처음 한 번만 1~2분"**,
   두 번째 문항부터는 원래 문구로 바뀌는지.
4. **B2** 헤더에 드롭다운 대신 `모델 백석대학교 로컬 LLM` 글자로 보이는지.
5. **B1** (확인 어려움) 백엔드를 끄면 개발자 명령 대신 "서버와 연결이 끊겼어요" 만 보인다.
6. **A5** 개발자도구 Application → Local Storage → `modoo-writer-state-v1` 안 `researchState` 에
   `pages` 키가 없는지 (한 번 새로 조사한 뒤).

⏸ **배포(=`npx vite build`)는 승인 대기** — 승인 전까지 `dist` 는 `index-CG-vzjlN.js` 그대로 둔다.


- 완료 — **작업 9: 벡터DB 모듈** (`7b2a6e5`). `RAG_PLAN` 5-1절 서명 그대로, **pytest 전체 124 passed, 실호출 0**.
  - `backend/rag/vectorstore.py` (신규) + `tests/test_vectorstore.py` (신규 18건) + `requirements.txt` 2줄.
    **`pipeline.py` 등 기존 백엔드 파일은 하나도 안 건드렸다** — 파이프라인 연결은 세션3 몫.
  - `upsert_pages` 는 `collect_pages` 의 page dict 를 그대로 받는다. url 해시로 upsert(프래그먼트·끝
    슬래시 무시), 본문 40자 미만이면 색인 안 함(`snippet` 폴백은 있음), 청크 512토큰.
    메타: `url/title/publisher/date/fetched_at/source_kind/query` — `publisher` 없으면 도메인
    (`pipeline.py:271` 과 같은 규칙), `source_kind` 는 검색 결과의 `source_type` 을 옮긴 것.
    메타는 임베딩 본문에서 제외했다(제목·URL 이 섞이면 유사도가 주제가 아니라 형식에 끌린다).
  - `query` 는 같은 문서의 청크를 **최고점 하나로 합쳐** 페이지 목록으로 준다. 신선도 컷은
    `date` 가 `max_age_days`(기본 730) 초과면 제외, **날짜를 모르는 문서는 남긴다**.
  - **📢 세션3 참고 — 고정 서명 외에 추가한 것 3개** (서명 변경 아님, 안 써도 그만):
    ① `indexed_urls() -> set[str]` — `collect_pages(skip_urls=…)` 에 바로 넘길 수 있다.
    ② `ollama_embedding(model_name="bge-m3", base_url=…)` — 운영용. **지연 import 라 Ollama 없어도
       모듈 import 는 된다.** `VectorStore(persist_dir, embed_model=ollama_embedding())` 로 넘기면 됨.
    ③ `query()` 결과에 `from_vectorstore: True` — 웹에서 새로 받은 페이지와 구분용.
  - `embed_model=None` 기본값은 `OfflineEmbedding`(글자 2-gram 해시 + L2 정규화). LlamaIndex
    `MockEmbedding` 을 안 쓴 이유: 모든 문장에 같은 벡터를 줘서 유사도가 항상 1.0 → `min_score`
    동작을 테스트로 지킬 수 없다. **운영에서는 반드시 `embed_model` 을 넘겨야 한다** (안 넘기면 조회 품질이 없다).
  - ⚠️ **공유 `.venv` 에 패키지를 설치했다** — `llama-index-core 0.14.24`,
    `llama-index-embeddings-ollama 0.10.0` + 의존성 45개(numpy·aiohttp·sqlalchemy·nltk 등).
    `setuptools 65.5.0 → 84.0.0` 만 업그레이드됐고 pydantic·httpx·fastapi 는 그대로다.
    설치 직후 기존 테스트 106건 통과를 먼저 확인했다. 다른 PC 는 `pip install -r requirements-dev.txt` 필요.
  - 아직 안 한 것(세션3 소관): `config.yaml research.vectorstore.{enabled,min_score,min_hits,max_age_days}`
    설정 항목, `collect_pages` 연결, `asyncio.to_thread` 래핑. Ollama 실호출(`ollama pull bge-m3`) 검증도 미수행.


- **공개 배포 (사용자 승인, 2026-09-02)** — `npx vite build` 로 `frontend/dist` 갱신.
  **새 번들: `index-vkBBBKoM.js` / `index-Da5jo6yn.css`** (이전 `index-CG-vzjlN.js` → 교체됨).
  배포 내용은 `c1dd0e4` (UX 개선 A2~A5 + B1~B4). 빌드 전 워킹트리에 프론트 미커밋 변경 없음,
  `frontend/.env` 없음(= `.env.production` 의 `VITE_API_BASE=/api` 로 빌드됨) 확인 후 실행.
  - 공개 URL 확인: `https://bustartup.kr:50001/` 이 `assets/index-vkBBBKoM.js` 를 참조 → nginx 가 새 번들 서빙 중.
  - 번들 문자열 검증 — 들어간 것: `차례를 기다리는 중`(A3) · `남은 N개 계속 만들기`(A2) ·
    `아이디어 공통 조사부터`(A4) · `복사 안 됨`(B4) · `서버와 연결이 끊겼어요`(B1).
    빠진 것(0건): `uvicorn`(B1 제거 확인) · `로컬 분야` · `스토리텔링형`(작업 11 유지 확인).
  - `frontend/dist` 는 `frontend/.gitignore` 로 추적 제외라 **커밋할 산출물은 없다** (배포 = 파일 교체).
  - ⏸ 남은 브라우저 확인은 이제 공개 URL 에서 하면 된다: A2(생성 중 F5 → 나머지 문항까지 이어서 생성),
    A3(진입 직후 대기 카드), A4(첫 문항 문구), B2(모델이 드롭다운 아닌 글자).
  - 📌 세션1/세션3: 워커 36+9(`054a51f`)는 **아직 운영 미반영** — `mochang-api` 재시작이 필요하다.
    이번 배포는 프론트 정적 파일만 바꾼 것이라 백엔드는 그대로다.


- 완료 — **작업 17 UI 몫: KCI 출처 표기** (`1bd6ca3`). `dist-check` 통과, **dist 미변경 = 아직 공개 안 됨**.
  `RESEARCH_PLAN` "KCI 이용 준수 사항" 4항(서비스 화면에 항상 표시) 이행. 문구는 `KCI_NOTICE` 상수 한 곳.
  - **푸터 신설** — 지금까지 푸터가 아예 없었다. 모든 단계(입력·보충·초안·정리) 아래에 상시 표시:
    `KCI(한국학술지인용색인) 데이터 활용` + `학술 근거는 논문 서지정보(제목·저자·연도·학술지)만 인용합니다.`
    (뒷줄은 준수 사항 1·3항 — 메타데이터만·왜곡 금지 — 을 사용자에게도 알리는 문장이다.)
  - **조사 근거 패널** — KCI 항목에 `KCI` 배지, 패널 안에 출처 문구 한 줄. 패널이 접혀 있어도 보인다.
  - `isKciFact(f)` 판정 3중: `source_kind === "kci"` | **호스트**가 `kci.go.kr` | `publisher === "KCI"`.
    세션3 어댑터가 붙기 전에도 표기가 빠지지 않는다 — 지금은 ddgs 가 `kci.go.kr` 문서를 물어 오는 경우가 실제로 있다
    (`backend/.cache/research/` 에 사례 다수). 호스트만 보므로 `evil.com/kci.go.kr/...` 같은 경로 위장은 안 잡힌다.
    판정 케이스 10건을 node 로 확인.
  - 📢 **세션3에게**: 어댑터에서 facts 에 `source_kind: "kci"` 를 실어 주면 첫 조건이 잡는다 (필드 추가만,
    없어도 URL·publisher 로 동작). 지금 `/research` 응답의 fact 에는 `source_kind` 가 없다.
  - ⏸ **배포 대기** — 공개 번들은 아직 `index-vkBBBKoM.js`(KCI 표기 없음)다. 승인해 주시면 `npx vite build`.


- **공개 배포 2 (사용자 승인, 2026-09-02)** — `npx vite build`.
  **새 번들: `index-D6Y4UlY6.js` / `index-lAf1A43D.css`** (이전 `index-vkBBBKoM.js` / `index-Da5jo6yn.css` → 교체됨).
  배포 내용은 `1bd6ca3` (작업 17 UI — KCI 출처 표기). 빌드 전 프론트 미커밋 변경 없음, `frontend/.env` 없음 확인.
  - 공개 URL `https://bustartup.kr:50001/` 이 `assets/index-D6Y4UlY6.js` 참조 → nginx 가 새 번들 서빙 중.
  - 검증: 공개 URL 에서 번들을 직접 받아 `한국학술지인용색인` 1건 확인.
    번들 문자열 — `KCI(한국학술지인용색인) 데이터 활용` 1 · `논문 서지정보` 1 · 판정 정규식 `kci\.go\.kr` 1 ·
    `source_kind` 1. 직전 배포분(`차례를 기다리는 중` 2 · `서버와 연결이 끊겼어요` 1)도 그대로 살아 있다.
  - 이로써 **KCI 준수 사항 4항(화면 상시 표기)은 공개 서비스에 반영됐다.** 남은 KCI 항목은 세션3 몫
    (1항 `no_fetch` · 2항 벡터DB 색인 제외 · 3항 왜곡 금지 · 5항 파기 스크립트).
  - 📌 워커 36+9(`054a51f`)는 **여전히 운영 미반영** — 프론트 정적 파일만 바꾼 배포라 `mochang-api` 재시작이 따로 필요하다.


- 완료 — **작업 22+27: 생성 후처리 `refine()`** (`91d57f5`). pytest 전체 **334 passed**, 모델·네트워크 호출 0.
  신규 2파일만 만들었고 `generate.py`·`polish.py` 등은 **건드리지 않았다** (연결은 세션3).
  - `backend/pipeline/postprocess.py` — `refine(text, ctx) -> (text, report)`. 지시서 4절 ①~⑩ 순서대로.
    `ctx` 는 `{question_id, limit, target_range, idea, outline, answers, references, team, other_texts, current}`.
    이어쓰기는 **더한 부분만** `text` 로, 기존 본문은 `ctx["current"]` 로 넘긴다(백엔드 extend 요청 필드명과 같다).
  - **호출자가 볼 신호 4가지**: `foreign`·`invented_flagged`·`banned_flagged`(그 문장만 재생성) /
    `extend_empty`(이번 이어쓰기 버림) / `needs_compress`(shorten.md 로 1회 압축 — **여기서 자르지 않았다**) /
    `needs_extend`(1회 이어쓰기 후 refine 재적용).
  - 판단이 갈릴 만한 곳 3개(설계 근거를 남긴다):
    ① **이물은 지우지 않는다** — 지시서대로 표시만. 실측 baseline q4_2 는 이물 문장이 9개라 지우면 문항이 빈다.
    ② **글이 통째로 비게 되는 삭제는 취소**하고 `*_flagged` 로 넘긴다. 짧은 문항(Q1·Q10)은 아예 삭제하지 않는다.
    ③ 수치 지문을 **자릿수+자릿수말**로 잡는다(`5만` ≠ `5`). 자릿수만 보면 입력 어딘가의 `5` 때문에
       지어낸 `5만 명`이 통과한다. 이걸 고치면서 회귀표가 한 번 움직였다(after3_A).
  - `tests/test_postprocess.py` 63건 — 규칙별 케이스 + **`docs/measurements` 실측 5파일 40문항 전/후 건수 회귀표**.
    표가 흔들리면 규칙이 바뀐 것이니 의도 확인 후 갱신(늘면 과삭제, 줄면 놓침).

    | 파일 | 지어냄 | 재인용 | 재진술 | 금지어 | 1인칭 | 이물(표시) |
    |---|---|---|---|---|---|---|
    | baseline_laundry | 11 | 5 | 0 | 3 | 23 | 18 |
    | after_laundry | 7 | 0 | 3 | 0 | 13 | 3 |
    | before_A | 8 | 12 | 0 | 2 | 11 | 4 |
    | after3_A | 7 | 8 | 0 | 0 | 26 | 5 |
    | after3_B | 7 | 0 | 3 | 3 | 28 | 12 |

    불변 조건도 함께 지킨다: **어떤 문항도 통째로 비지 않는다 / 길어지지 않는다 / '저희' 0건**(team=팀원 없음).
  - 📢 **세션3 연결 시 참고**:
    ① `③ unit_economics` 는 회귀표에서 0으로 나온다 — 실측 11문장이 전부 ②(근거 없는 수치)에 먼저 걸리기 때문이다.
       입력에 있는 숫자로 쓴 단위경제 문장을 위해 남겨 둔 규칙이다.
    ② `⑤` 의 `other_texts` 는 **이미 확정된 문항만** 넣어야 한다. 아직 안 고른 문항까지 넣으면 먼저 쓴 쪽이 지워진다.
    ③ `④ 1인칭`은 골자에 `voice` 가 있으면 그것을, 없으면 `team` 을 본다. 지시서 1-5의 `voice: 저|저희` 를 넣어 주면 된다.
    ④ `polish.py` 와 규칙이 겹치지만 그쪽은 모델을 부르는 경로라 **일부러 합치지 않았다** — 정리 여부는 세션3·세션1 판단.

### 세션3 (백엔드)

- 시작 — 작업 2(V4), 작업 6 백엔드 몫, 작업 12 1단계.
  건드릴 파일: `backend/llm/jobs.py`, `backend/main.py`, `backend/config.yaml`, `backend/rag/*`, `tests/`, `scripts/load_test.py`.
- 완료 — **작업 2(V4) + 작업 6 백엔드 몫** (`1bb3ad0`). 테스트 90 passed.
  - IP 당 동시 작업 3건(`config.yaml max_jobs_per_client`), 초과 제출은 `429` + `Retry-After: 10`,
    detail 은 한국어 안내문("동시에 진행할 수 있는 작업은 3건까지입니다 …").
  - 클라이언트 식별은 `X-Forwarded-For` 첫 항목 우선 → nginx 50001 뒤에서도 사용자별로 구분됨.
  - **📢 세션2 참고 (응답 필드 추가만, 삭제·개명 없음)**: `POST /jobs/{kind}` 가 429 를 낼 수 있음
    (본문 `{"detail": "…"}`) — 프론트에서 429 는 오류가 아니라 "잠시 후 재시도" 안내로 다루면 좋음.
    `GET /jobs` 에 `max_per_client`, `your_active`(내 IP 가 점유 중인 작업 수) 추가.
  - `config.yaml` 모델 표기 `백석대학교 로컬 LLM` (id 는 그대로) → `/models` 로 내려감.
  - `scripts/load_test.py` 는 요청마다 다른 IP 를 흉내내도록 수정(안 하면 부하가 아니라 429 를 재게 됨).
    `--same-ip` 옵션으로 제한 동작 확인 가능.
- 완료 — **작업 12 1단계** (`8da2f0a`). 테스트 91 passed. 조사 1회 외부 요청 **20건 → 10건**.
  - `collect_pages` 가 `max_pages_to_extract * 2` 를 받아 절반 버리던 것 제거 (fetch 12 → 6).
    여유가 필요하면 `config.yaml research.max_pages_to_fetch` 로만 올린다 (기본 6, 실패 잦으면 8).
  - `DDGSearch.include_news` 기본 `False` — DDG 호출 8 → 4. 뉴스 기능은 삭제하지 않았고
    `DDGSearch(include_news=True)` 로 즉시 되살아난다 (3단계에서 네이버 뉴스 API 로 이관 예정).
- 완료 — **작업 12 0단계: 검색어 중복도 실측** (`ca8e71e`). 라마 실호출 20회 (아이디어 2개 × 10문항).
  재현: `.venv/Scripts/python -m scripts.query_dump --ideas 2 --json out.json` (검색·fetch 없음 = 외부 요청 0건).

  | 지표 | 아이디어 A(냉장고 관리 앱) | 아이디어 B(전통시장 배달 중개) |
  |---|---|---|
  | 총 검색어 (아이디어 조사 1 + 문항 9, 각 4개) | 40 | 40 |
  | 문자열 중복 제거 후 | 37 (**중복률 7%**) | 39 (**3%**) |
  | 사실상 같은 검색어 쌍(토큰 50% 이상 겹침) | **49쌍** | 17쌍 |
  | 아이디어 검색어가 토큰으로 덮는 문항 검색어 | **21/36 (58%)** | 13/36 (36%) |
  | 문항쌍 유사도 평균 / 최대 | 0.42 / **0.75 (q3_1↔q8)** | 0.30 / 0.48 (q2↔q4_2) |

  **해석 (2단계 설계 근거)**: 글자 그대로 같은 검색어는 3~7% 뿐이라 **문자열 캐시로는 거의 못 줄인다**.
  반면 토큰 기준으로는 절반 가까이("… 현황"/"… 통계"/"… 시장 규모") 사실상 같은 검색이라 검색 결과 페이지가 크게 겹친다.
  → 2단계는 "아이디어 단위 공통 조사 1회(검색어 4개)를 먼저 하고, 문항별로는 그 결과로 못 채우는
  각도만 1~2개 추가"가 맞다. 문항당 4개 고정을 없애는 것이 핵심 (조사 10회 → 1회 + 보완).
  아이디어별 편차가 크므로(58% vs 36%) 문항 보완 검색어 수는 고정하지 말고 상한만 두는 쪽.
- 완료 — **작업 12 3단계 선작업: 네이버 검색 API 어댑터** (`23ad8de`). 테스트 96 passed, 실호출 0. *(구 규격 — 아래 정정으로 대체됨)*
  - `NaverSearch` — kinds(news/blog/doc/webkr) 각각 요청 1회, `<b>` 태그·엔티티 제거, originallink 우선,
    유형 하나가 실패해도 나머지는 살림. `_norm_date` 가 네이버 날짜 2종(pubDate, postdate) 인식.
  - **키가 없으면 백엔드 목록에 아예 안 들어간다** → 지금은 예전과 100% 동일하게 ddgs 단독.
    `.env` 에 `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` 이 들어오는 순간 `naver → ddgs` 순서로 자동 전환.
  - 📌 세션1에게: `.env.example` (루트, 제 구역 밖이라 안 건드림)에 아래 2줄 추가해 두면 좋겠습니다.
    `NAVER_CLIENT_ID=` / `NAVER_CLIENT_SECRET=`
- 완료 — **작업 12 2단계: 아이디어당 1회 조사** (`0c1f125`). 테스트 103 passed, 실호출 0.
  - `run_research` 가 아이디어 **공통 조사 결과를 물려받고**, 그 사실로 못 채우는 각도만 보완 검색한다.
    공통 조사는 그 아이디어의 첫 문항에서만 실제로 돌고, 나머지 8문항은 캐시 적중 = 외부 요청 0건.
  - 보완 검색어는 새 프롬프트 `research/followup_queries.md` 가 만든다 —
    **"이미 확보한 사실로 충분하면 빈 배열"이 최선의 답**이라고 지시하고, 개수는 고정하지 않고 상한만 둔다.
    모델 응답이 깨지면 폴백 검색어를 만들지 않는다(요청을 늘리지 않는 게 목적).
  - `collect_pages(skip_urls=…)` — 공통 조사에서 이미 본문을 받은 페이지는 다시 안 받는다.
  - **외부 요청 실측 (mock 카운터, `test_one_person_scenario_external_requests_before_and_after`)**

    | 1인 시나리오 (인테이크 + 문항 9개) | 검색 | 페이지 fetch | 합계 | 모델 호출 |
    |---|---|---|---|---|
    | 2단계 전 (문항마다 처음부터 조사) | 40 | 60 | **100** | 20 |
    | 2단계 후 (보완 2개 = 최악의 경우) | 22 | 33 | **55** | 20 |
    | 2단계 후 (모델이 "충분" 판단) | 4 | 6 | **10** | 11 |

    0단계 실측(공통 조사가 문항 검색어의 36~58% 커버)을 감안하면 실제 값은 55와 10 사이,
    대략 **30~40건**으로 예상. 1단계까지 합치면 개편 전 200건 → 지금 30~55건.
  - 📢 **세션2 참고 (응답 필드 추가만, 삭제·개명 없음)**: `POST /jobs/research` · `POST /research` 응답에
    `shared_queries`(공통 조사 검색어) · `followup_queries`(이 문항에서만 추가한 검색어) ·
    `shared_facts_used`(facts 중 공통 조사에서 물려받은 개수) 3개 추가. `queries` 는 둘을 합친 값이라
    기존 화면은 그대로 두면 된다. `facts`/`references`/`pages`/`cached` 는 의미·형태 그대로.
  - ⚠️ 체감 변화: **첫 문항 조사만 느려진다** (공통 조사 + 보완 = 모델 호출 4회). 2~9번째 문항은
    모델 호출 1~2회로 오히려 빨라진다. 인테이크를 거친 사용자는 공통 조사가 이미 캐시에 있어 첫 문항도 빠르다.
  - 되돌리기: `config.yaml research.share_idea_research: false` 한 줄이면 예전 동작(문항마다 처음부터 조사)으로
    복귀한다. 예전 경로도 테스트로 계속 지키고 있다.
  - 📌 세션1에게(코드 아님, 확인만): `/research` 는 `research_client`(조사용 모델)로, `/jobs/research` 는
    `client`(작성 모델)로 조사를 돌린다 — 2단계 전부터 있던 불일치다. 지금은 둘 다 같은 로컬 라마라 결과는 같지만,
    나중에 모델을 나누면 조사 품질이 경로마다 달라진다. 정리 여부는 세션1 판단.
- 완료 — **작업 10: GPU 한계 측정 + 워커 상향** (`d7d34a9`) *(1차 — 짧은 프롬프트 기준. 작업 13에서 실사용 프롬프트로 재측정해 값이 36/9 로 바뀜)*. 사용자 승인 후 실측, 라마 실호출 **107회**(측정용).

  측정 조건: `scripts/gpu_probe.py`, vLLM 에 **직접** 동시 호출(우리 큐 우회), 문항 생성 길이 프롬프트,
  `max_tokens 600`, 단계마다 `/metrics` 1초 간격 샘플링. 2026-09-02 저녁.

  | 동시성 | 성공/실패 | p50 | p95 | 최대 | 전체 소요 | 생성 tok/s | KV캐시 최대 | 서버 running 최대 | 서버 waiting 최대 | preemption |
  |---|---|---|---|---|---|---|---|---|---|---|
  | 12 | 12 / 0 | 8.6s | 9.6s | 10.0s | 10.2s | 311 | **1.9%** | 12 | 0 | 0 |
  | 20 | 20 / 0 | 10.7s | 12.1s | 12.9s | 13.2s | 423 | **3.1%** | 20 | 0 | 0 |
  | 30 | 30 / 0 | 11.4s | 13.1s | 14.7s | 14.9s | 580 | **4.6%** | 30 | 0 | 0 |
  | 45 | 45 / 0 | 14.3s | 15.9s | 17.9s | 18.1s | 706 | **7.0%** | 45 | 0 | 0 |

  **분석**
  - **한계점이 측정 구간 안에 없다.** KV 캐시는 45 동시에서도 7%(기준 90%), 서버 waiting 0, preemption 0.
    45개를 전부 동시에 처리했다 = vLLM 의 `--max-num-seqs` 도 45 이상.
  - p95 는 9.6 → 12.1 → 13.1 → 15.9초로 **완만히만** 증가(45에서도 어제 실사용 대기 2분49초보다 훨씬 짧다).
    처리량은 311 → 706 tok/s 로 계속 늘지만 증가폭은 둔해진다(20→30 +37%, 30→45 +22%).
  - 어제 진단(“병목은 GPU 가 아니라 우리 워커 12개”)이 숫자로 확인됐다. 워커 12 단계에서 서버 waiting 이 0이라는 것은
    **GPU 가 놀고 있었다**는 뜻이다.

  **채택값과 근거**: 판단 기준(KV 90% 근접·p95 꺾임·preemption)이 하나도 걸리지 않았으므로
  “측정된 최대(45)의 한 단계 아래”인 **30** 을 안전 상한으로 잡았다. 45 를 그대로 쓰지 않은 이유 3가지 —
  ① 측정용 프롬프트는 실사용보다 짧다(실사용은 참고자료가 붙어 KV 를 더 쓴다) ② 조사 워커와 GPU 를 나눠 쓴다
  ③ 공유 GPU 서버라 다른 사용처를 위한 여유가 필요하다.
  - `max_workers: 12 → 30` (본문 생성)
  - `research.llm.max_workers: 4 → 8` (조사) — 같은 근거. **본문 30 + 조사 8 = 38 ≤ 측정 확인된 45.**
  - 되돌리기는 두 줄. 더 올리려면 `--levels 60,80` 으로 한 번 더 재면 된다(측정 자체는 5분).
  - 측정 원자료 JSON 은 세션 임시 폴더에만 두었다(리포는 깨끗하게). 필요하면 위 명령으로 재현 가능.

  ⚠️ **운영 반영은 아직 안 됐다 — `mochang-api` 재시작이 필요하다.**
  `config.yaml` 은 프로세스가 시작할 때 한 번만 읽는다(`load_config()` at import). 지금 커밋된 30/8 은 파일에만 있고,
  :8000 에서 도는 서비스는 여전히 12/4 로 동작한다. TEAMWORK 4절대로 **세션3은 NSSM 서비스를 건드리지 않았다.**
  - 필요한 조작(사용자): 관리자 PowerShell 에서 `nssm restart mochang-api` (또는 stop → start).
  - **권장 시점**: ① 세션2 의 V1(큐 전환) + 작업 11 프론트 변경이 끝나 **dist 배포와 한 번에 묶어서**,
    ② 사용 없는 시간대(심야/이른 아침). 재시작 중 진행 중이던 생성 작업은 **큐가 메모리에만 있어 전부 사라진다**
    (작업 ID 폴링이 404 가 된다) — 사용자가 붙어 있는 낮 시간대는 피할 것.
  - 재시작 뒤 확인: `GET /jobs` 의 `max_workers` 가 30 인지. 그 다음 부하 체감은
    `scripts/load_test.py --n 100 --jobs` 로 큐 쪽만 따로 볼 수 있다(모델은 가짜라 GPU 부하 없음).
- 완료 — **작업 12 3단계 정정: NaverSearch 를 NAVER API HUB 규격으로** (`6c7e9bc`). 테스트 106 passed, **실호출 0**.
  상태판 "🔴 3단계 정정" 5개 항목 전부 반영:
  - ① `base_url` → `https://naverapihub.apigw.ntruss.com/search/v1`, 경로 `/{kind}` (`.json` 없음)
  - ② 인증 헤더 → `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` (구 `X-Naver-Client-*` 삭제).
    **`.env` 이름은 `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` 그대로** — NCP 콘솔의 Client ID/Secret 을 그 이름에 넣으면 된다.
  - ③ `doc`(전문자료) 제거(2026-07-31 종료). 대신 `webkr`(웹문서)·`cafearticle`(카페 글, 고객 불만 근거)을 넣었다.
    학술 근거는 KCI(`KCI_API_KEY`) → OpenAlex/Semantic Scholar(키 불필요) 자리로 비워 둔다.
  - ④ 오류 본문 2형태 모두 파싱(`naver_error_message`): 평면 `{"errorCode","errorMessage"}` /
    중첩 `{"error":{"errorCode","message"}}`. **429 는 남은 유형을 더 쏘지 않고 중단**하고
    `SearchRateLimited` 로 올려 ddgs 폴백 — 5단계 서킷브레이커가 그대로 잡을 신호 타입이다.
    429 가 아닌 오류(500 등)는 유형 하나만 건너뛰고 나머지는 살린다. 오류 문자열은 `NaverSearch.last_error`.
  - ⑤ `display` 1~100 로 제한, 한도 주석 25,000/일 → **월 775,000건·키당 50 RPS** 로 정정 (config.yaml 포함).
  - 동작 변화 없음 확인: **키가 없으면 백엔드 목록에서 빠져 지금도 ddgs 단독**이다. 키가 들어오면 `naver → ddgs`.
  - 남은 3단계 작업(키 필요 없음): KCI/OpenAlex 어댑터, 정형 API(KOSIS·ECOS·data.go.kr) 별도 경로 설계.
- 📌 세션1에게 — 키가 발급되면 `.env` 에 이 이름으로 넣어 주세요 (코드가 이 이름을 읽습니다):
  `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`, `DATA_GO_KR_API_KEY`, `KOSIS_API_KEY`, `ECOS_API_KEY`,
  `KCI_API_KEY`, `KIPRIS_API_KEY`. 지금 코드가 실제로 쓰는 건 네이버 2개뿐이고 나머지는 어댑터를 만들 때 씁니다.
  `.env.example`(루트, 제 구역 밖)에도 같은 줄을 넣어 두면 다른 PC 에서 헤매지 않습니다.
- 완료 — **작업 13: 실사용 길이 프롬프트로 고동시성 실측 → 워커 36+9** (`d3d26a2`, `054a51f`).
  라마 실호출 **약 500회**(측정용). 사용자 지시로 진행.

  **1차(09-02 저녁)와 조건이 다르다**: 1차는 한 줄짜리 짧은 프롬프트 + `max_tokens 600` 이었다.
  이번엔 `--prompt real` — 실제 생성 경로와 같은 프롬프트(system.md + 트랙 + 문항 md + 참고자료 컨텍스트,
  **3,370 토큰**)에 `max_tokens 4096`, 실제 생성량 **795 토큰**. 1차 수치는 prefill 비용과 KV 점유를 과소평가한 값이다.

  | 동시성 | 성공/실패 | p50 | p95 | 생성 tok/s | 평균 프롬프트/생성 토큰 | KV캐시 최대 | 서버 running | waiting | preempt |
  |---|---|---|---|---|---|---|---|---|---|
  | 20 | 20 / 0 | 36.5s | 40.1s | 379 | 3370 / 798 | 11.0% | 20 | 0 | 0 |
  | 30 | 30 / 0 | 42.0s | 44.6s | 509 | 3370 / 789 | 15.3% | 30 | 0 | 0 |
  | 45 | 45 / 0 | 54.6s | 58.5s | 574 | 3370 / 795 | 21.6% | 45 | 0 | 0 |
  | 60 | 60 / 0 | 65.1s | 70.1s | **650** | 3370 / 795 | 28.9% | 60 | 0 | 0 |
  | 80 | 80 / 0 | 90.8s | 97.6s | 627 | 3370 / 791 | 36.5% | 80 | 0 | 0 |
  | 100 | 100 / 0 | 114.5s | 122.2s | 640 | 3370 / 800 | 45.1% | 100 | 0 | 0 |
  | ~~150~~ | 150 / 0 | 137.3s | 194.9s | 594 | — | 47.1% | **100** | 0 | 0 |

  ⚠️ **150 행은 무효다 — 측정기 쪽 함정.** httpx 기본 커넥션 풀이 100이라 나머지 50건이 **서버가 아니라
  클라이언트에서** 줄을 섰다(그래서 서버 running 이 100 에서 멈추고 waiting 은 0). 풀을 `n+20` 으로 열도록
  고쳤다(`054a51f`). **운영 코드는 무관** — `AsyncOpenAI` 는 풀이 1000이다. 같은 함정을 다시 밟지 않도록 주석으로 남겼다.

  **분석 — 한계는 메모리가 아니라 처리량이고, 포화점은 60이다.**
  - 처리량은 379 → 509 → 574 → **650(60)** 까지 오르고 그 뒤로 평평하다(80: 627, 100: 640).
    60 이상에서는 **동시성을 올려도 전체 처리량이 늘지 않고 문항당 대기만 비례해서 길어진다**(65s → 122s).
  - KV 캐시는 100 동시에서도 45% 라 90% 기준까지 한참 남았고, 실패·서버 대기·preemption 은 전 구간 0.
    즉 vLLM 은 더 받아 줄 수 있지만 **받아도 더 빨리 처리하지는 못한다**.
  - 1차 결론(“45 까지 여유”)과 모순되지 않는다. 1차는 짧은 프롬프트라 60까지 못 가 봤을 뿐이다.

  **채택: 총 동시 호출 45** = `max_workers 30 → 36`(본문) + `research.llm.max_workers 8 → 9`(조사).
  - 규칙(꺾이는 지점의 한 단계 아래)대로 60 이 아니라 45. 60 으로 올려도 처리량은 같고 문항당 p50 이
    55초 → 65초로 늘 뿐이다. **남는 요청은 vLLM 이 아니라 우리 큐가 잡는 편이 낫다** — 우리 큐는
    대기 순번(`position`)을 화면에 보여줄 수 있지만, vLLM 안에서 기다리는 요청은 그냥 느린 요청으로만 보인다.
  - 조사 워커를 낮게 두는 이유: 조사 호출은 프롬프트가 훨씬 길다(페이지 6개 ≈ 2만 토큰) — 슬롯당 GPU 비용이 크다.
  - 시스템 처리 능력 자체는 워커 수와 무관하게 **≈ 650 tok/s ≈ 문항 0.8건/초 ≈ 분당 49문항**이다.
    더 필요하면 워커가 아니라 GPU·모델 쪽(양자화·서버 증설)을 봐야 한다.
  - 📌 **`nvidia-smi` 값은 아직 못 받았다.** 다만 KV 캐시가 100 동시에서도 45% 라 메모리는 제약이 아니어서
    상한 판단이 바뀌지는 않을 것으로 본다. 값 주시면 로그에 붙이고, GPU 사용률이 100% 에 붙어 있다면
    “처리량 포화” 해석을 한 번 더 확증하는 자료가 된다.
  - vLLM `--max-num-seqs` 의 실제 값은 아직 모른다(≥100 인 것만 확인). 우리 상한이 45라 지금은 알 필요가 없어
    150 재측정은 하지 않았다 — 공유 GPU 에 3분 넘게 부하를 다시 거는 값어치가 없다.

  ⚠️ **운영 반영은 여전히 `mochang-api` 재시작 필요** (앞 항목의 재시작 안내 그대로. 값만 30/8 → **36/9** 로 바뀜).
  재시작 뒤 `GET /jobs` 의 `max_workers` 가 36 이면 반영된 것이다.
- 완료 — **네이버 API HUB 실키 검증** (`2f3d66b`). `.env` 에 키가 들어온 것을 확인하고 실호출 **2건**(news·blog).
  - `scripts/naver_check.py` 로 언제든 재확인 가능 (`--query`, `--kinds`). 캐시를 거치지 않는다.
  - 결과: 경로·헤더·응답 파싱 전부 정상. **뉴스는 품질이 좋다**(2026년 1인 가구·식품 폐기 기사가 최신순으로).
  - ⚠️ **blog 는 `sort=sim` 이라 2023년 무관한 글이 섞여 나온다.** `sort: "date"` 로 바꾸거나 blog 를 빼고
    `webkr` 을 쓰는 게 나아 보인다 — 3단계 라우팅에서 유형별로 `sort` 를 다르게 주는 설계로 정리하겠다.
  - 키가 있으므로 **재시작하는 순간부터 검색이 `naver → ddgs` 순으로 나간다**(지금 :8000 은 아직 ddgs 단독).
    한도는 월 775,000건이라 여유가 크지만, 재시작 후 첫날은 NCP 콘솔에서 사용량을 한 번 봐 두면 좋다.
- 다음(세션3 소관, 세션2 작업 9 인계분) — `backend/rag/vectorstore.py` 를 파이프라인에 연결:
  `config.yaml research.vectorstore.{enabled,min_score,min_hits,max_age_days}` 추가, `collect_pages` 연결
  (`indexed_urls()` → `skip_urls`), `asyncio.to_thread` 래핑, Ollama `bge-m3` 실호출 검증.
- 완료 — **작업 12 3단계: 정형 공공 API 어댑터 4종 + 도메인별 상한 + 네이버 정렬** (`c747955`). 테스트 147 passed.
  각 소스 **실호출 1건씩** 확인 (KOSIS 는 검색+수치라 2건). 재확인: `.venv/Scripts/python -m scripts.opendata_check`

  | 소스 | 하는 일 | 요청 수 | 실호출 결과 (2026-09-02) |
  |---|---|---|---|
  | **KOSIS** | 통계표 검색 → 이름이 검색어와 겹치는 표의 최신 수치 | 2 | 「1인가구비율」 **전국 1인가구 8,244,308가구 / 비율 36.6% (2025)** |
  | **ECOS** | 100대 지표 1회 조회 후 검색어와 맞는 지표만 | 1 (이후 캐시) | **기준금리 3%**, 원/달러 1,370.4원, 원/엔 855.21원 … |
  | **K-Startup** | 공고명 LIKE 검색, 공고 본문이 그대로 근거 | 1 | 청년창업 공고 3건 (지원대상·본문 포함) |
  | **상권정보** | 업종 대분류 매칭 → 전국 상가업소 수 | 2 (업종목록 캐시) | **전국 '음식' 업종 840,255개 (2026-06 기준)** |

  - **폴백이 아니라 덧붙이는 소스**다. `Researcher(extras=[...])` 로 늘 같이 물어보고 웹 결과 **앞에** 붙인다.
    웹 검색이 전부 막혀도 정형 결과만이라도 나오고, 정형 소스가 죽어도 웹 검색은 그대로 간다(양방향 격리, 테스트로 지킴).
  - **검색어에 그 소스 키워드가 없으면 HTTP 요청 자체를 하지 않는다**(`TRIGGERS`). 예: "음식 업종 점포 수" 는
    상권정보만 부르고 K-Startup 은 안 부른다. 한도 절약 + 로그 가독성. 모델로 유형 분류하는 원안은 호출이
    한 번 더 늘고 디버깅이 어려워 보류 — 실사용 로그를 본 뒤 필요하면 바꾼다.
  - 결과에 `no_fetch` 플래그 → `collect_pages` 가 **fetch 없이 snippet 을 본문으로** 쓴다. 정형 소스는
    받아올 페이지가 없고 이미 사실 문장이라, 외부 요청이 늘지 않는다(오히려 웹 fetch 자리를 대체한다).
  - ⚠️ **KOSIS 는 수치를 못 가져오면 아무것도 주지 않는다.** 축이 여러 개인 표는 KOSIS 가 '40,000셀 초과'로
    거절하는데, 그때 "이런 통계표가 있다"는 문장을 근거랍시고 넣으면 진짜 근거 자리만 차지한다
    (첫 실호출에서 관련 없는 관광 통계표가 그렇게 1위로 끼어들었다 — 표 이름 매칭까지 넣어 해결).
    표별 축·항목 조합 목록을 갖추면 더 많은 표에서 수치를 뽑을 수 있다 → **후속 과제**(자주 쓰는 표부터).
  - **도메인별 상한** `max_pages_per_domain: 2` (구 작업 7) — 같은 신문사 글이 근거를 독식하지 못하게.
    정형 결과는 상한 대상이 아니다(공식 통계는 소스가 하나뿐이라 쏠림이 문제되지 않는다).
  - **네이버 유형별 정렬**: 블로그·카페는 `date`(최신순), 뉴스·웹문서는 `sim`. 지난 실호출에서 블로그를
    `sim` 으로 부르면 3년 전 무관한 글이 섞여 나온 문제를 이렇게 정리했다. `research.naver.sort` 에 값을 주면
    전 유형에 강제된다(비워 두는 게 기본).
  - 키 없는 소스는 목록에서 자동으로 빠진다 — 키를 지워도 예전처럼 돈다.
- 완료 — **작업 9 연결(색인 단계)** (`12d44a5`). 테스트 152 passed. Ollama 실호출 1건.
  - `collect_pages` 가 만든 page dict 를 그대로 `upsert_pages` 로 넘긴다. `asyncio.to_thread` 로 감쌌고,
    **색인 실패는 조사를 막지 않는다**(부가 기능이라 조용히 넘어간다 — 테스트로 지킴).
  - `config.yaml research.vectorstore.{enabled,dir,embed_model,ollama_url,min_score,min_hits,max_age_days}` 추가.
    **`enabled: false` 가 기본** — 켜면 페이지마다 임베딩 호출이 붙고 디스크가 쌓이므로 사용자 결정 뒤에 켠다.
    켜져 있을 때만 저장소를 열고, `llama_index` import 도 그때 처음 일어난다(꺼져 있으면 기동 비용 0).
  - 세션2 인계 노트대로 **조회(웹 검색 대체)는 붙이지 않았다** — 이번엔 색인까지만.
  - 실호출 검증: bge-m3 로 페이지 1건 색인 → 조회 적중, 저장소 파일 5개 생성 확인.
    📌 **세션1·2 참고 — `min_score: 0.8` 은 너무 빡빡해 보인다.** 같은 주제 문장으로 조회했는데
    **score 0.668** 이 나왔다(1인가구 통계 본문 ↔ "1인 가구 비율"). 지금은 색인만 해서 영향이 없지만,
    조회 단계를 붙일 때 이 값을 그대로 두면 벡터DB 가 사실상 안 쓰인다. 실측 몇 건 더 뽑아 0.5~0.6 선으로
    조정하는 걸 권한다(조회 단계 작업자가 정할 문제라 지금은 안 건드림).
- 완료 — **작업 12 3단계: 정형 공공 API 어댑터 4종 + 도메인별 상한 + 네이버 정렬** (`c747955`). 테스트 147 passed.
  각 소스 **실호출 1건씩** 확인 (KOSIS 는 검색+수치라 2건). 재확인: `.venv/Scripts/python -m scripts.opendata_check`

  | 소스 | 하는 일 | 요청 수 | 실호출 결과 (2026-09-02) |
  |---|---|---|---|
  | **KOSIS** | 통계표 검색 → 이름이 검색어와 겹치는 표의 최신 수치 | 2 | 「1인가구비율」 **전국 1인가구 8,244,308가구 / 비율 36.6% (2025)** |
  | **ECOS** | 100대 지표 1회 조회 후 검색어와 맞는 지표만 | 1 (이후 캐시) | **기준금리 3%**, 원/달러 1,370.4원, 원/엔 855.21원 … |
  | **K-Startup** | 공고명 LIKE 검색, 공고 본문이 그대로 근거 | 1 | 청년창업 공고 3건 (지원대상·본문 포함) |
  | **상권정보** | 업종 대분류 매칭 → 전국 상가업소 수 | 2 (업종목록 캐시) | **전국 '음식' 업종 840,255개 (2026-06 기준)** |

  - **폴백이 아니라 덧붙이는 소스**다. `Researcher(extras=[...])` 로 늘 같이 물어보고 웹 결과 **앞에** 붙인다.
    웹 검색이 전부 막혀도 정형 결과만이라도 나오고, 정형 소스가 죽어도 웹 검색은 그대로 간다(양방향 격리, 테스트로 지킴).
  - **검색어에 그 소스 키워드가 없으면 HTTP 요청 자체를 하지 않는다**(`TRIGGERS`). 예: "음식 업종 점포 수" 는
    상권정보만 부르고 K-Startup 은 안 부른다. 한도 절약 + 로그 가독성. 모델로 유형 분류하는 원안은 호출이
    한 번 더 늘고 디버깅이 어려워 보류 — 실사용 로그를 본 뒤 필요하면 바꾼다.
  - 결과에 `no_fetch` 플래그 → `collect_pages` 가 **fetch 없이 snippet 을 본문으로** 쓴다. 정형 소스는
    받아올 페이지가 없고 이미 사실 문장이라, 외부 요청이 늘지 않는다(오히려 웹 fetch 자리를 대체한다).
  - ⚠️ **KOSIS 는 수치를 못 가져오면 아무것도 주지 않는다.** 축이 여러 개인 표는 KOSIS 가 '40,000셀 초과'로
    거절하는데, 그때 "이런 통계표가 있다"는 문장을 근거랍시고 넣으면 진짜 근거 자리만 차지한다
    (첫 실호출에서 관련 없는 관광 통계표가 그렇게 1위로 끼어들었다 — 표 이름 매칭까지 넣어 해결).
    표별 축·항목 조합 목록을 갖추면 더 많은 표에서 수치를 뽑을 수 있다 → **후속 과제**(자주 쓰는 표부터).
  - **도메인별 상한** `max_pages_per_domain: 2` (구 작업 7) — 같은 신문사 글이 근거를 독식하지 못하게.
    정형 결과는 상한 대상이 아니다(공식 통계는 소스가 하나뿐이라 쏠림이 문제되지 않는다).
  - **네이버 유형별 정렬**: 블로그·카페는 `date`(최신순), 뉴스·웹문서는 `sim`. 지난 실호출에서 블로그를
    `sim` 으로 부르면 3년 전 무관한 글이 섞여 나온 문제를 이렇게 정리했다. `research.naver.sort` 에 값을 주면
    전 유형에 강제된다(비워 두는 게 기본).
  - 키 없는 소스는 목록에서 자동으로 빠진다 — 키를 지워도 예전처럼 돈다.
- 완료 — **작업 9 연결(색인 단계)** (`12d44a5`). 테스트 152 passed. Ollama 실호출 1건.
  - `collect_pages` 가 만든 page dict 를 그대로 `upsert_pages` 로 넘긴다. `asyncio.to_thread` 로 감쌌고,
    **색인 실패는 조사를 막지 않는다**(부가 기능이라 조용히 넘어간다 — 테스트로 지킴).
  - `config.yaml research.vectorstore.{enabled,dir,embed_model,ollama_url,min_score,min_hits,max_age_days}` 추가.
    **`enabled: false` 가 기본** — 켜면 페이지마다 임베딩 호출이 붙고 디스크가 쌓이므로 사용자 결정 뒤에 켠다.
    켜져 있을 때만 저장소를 열고, `llama_index` import 도 그때 처음 일어난다(꺼져 있으면 기동 비용 0).
  - 세션2 인계 노트대로 **조회(웹 검색 대체)는 붙이지 않았다** — 이번엔 색인까지만.
  - 실호출 검증: bge-m3 로 페이지 1건 색인 → 조회 적중, 저장소 파일 5개 생성 확인.
    📌 **세션1·2 참고 — `min_score: 0.8` 은 너무 빡빡해 보인다.** 같은 주제 문장으로 조회했는데
    **score 0.668** 이 나왔다(1인가구 통계 본문 ↔ "1인 가구 비율"). 지금은 색인만 해서 영향이 없지만,
    조회 단계를 붙일 때 이 값을 그대로 두면 벡터DB 가 사실상 안 쓰인다. 실측 몇 건 더 뽑아 0.5~0.6 선으로
    조정하는 걸 권한다(조회 단계 작업자가 정할 문제라 지금은 안 건드림).
- ⚠️ 완료(부분 성공) — **작업 15: Q1 문체 다양화** (`ff2ef57`). 라마 실호출 **25건**(프롬프트 5안 × 5문장).
  - 고친 것(`q1.md` 만): 어미 예시 고정('~하는 앱입니다') 삭제 / 문장 구조 4종(문제·장면·대비·결과) 제시 /
    필수 요소 3개는 유지하되 **순서 자유** 명시 / "저는" 시작 금지와 정의형 틀 금지를 **q1.md 에 직접** 명시 /
    맺음말 다양화·길이(60~90자) 지시 / 예시 소재를 아이디어와 안 겹치는 도메인(세탁·소아과·공방·월세)으로.
  - **최종본으로 생성한 문장 5개 (실호출 그대로)**:
    1. (81자) 캠핑을 계획하는 사람에게 캠핑장의 빈 자리를 실시간으로 알려주고, 당일 취소된 자리를 할인된 가격에 예약할 수 있도록 해주는 서비스를 제공합니다.
    2. (81자) 캠핑을 계획하는 이용객에게 빈 자리 실시간 알림과 당일 취소분을 할인된 가격으로 예약할 수 있는 서비스를 제공하여 캠핑 준비의 불편을 해소합니다.
    3. (64자) 캠핑을 계획하는 사람에게 빈 자리 실시간 알림과 당일 취소분을 할인된 가격으로 예약할 수 있는 서비스를 제공합니다.
    4. (47자) 1인 가구의 식재료 관리 어려움을 사진 등록과 임박 재료 레시피 추천으로 해결합니다.
    5. (52자) 전통시장 상인에게 주문만 받고 지역 라이더가 묶음 배달을 처리해 주는 배달 대행 플랫폼입니다.
    (1~3 은 같은 아이디어 3회, 4·5 는 다른 아이디어 각 1회. 검증 기준대로 트랙 tech·스타일 logic 고정)
  - **판정: 기준 미달.** 좋아진 것 — "저는" 시작 소멸, '~앱입니다' 어미 소멸, 아이디어마다 시작 어절이 달라짐(4·5).
    남은 것 — 같은 아이디어 3회가 여전히 "캠핑을 계획하는…"으로 똑같이 시작하고, **'~를 제공합니다' 맺음이 안 사라진다.**
    4·5 는 60자 미만(47·52자)으로 짧다.
  - **원인 (실측 근거)**: 이 라마는 **금지형 지시를 우회한다**. 프롬프트 5안을 차례로 시험했다 —
    ① 구조 메뉴만 → 아이디어 문장을 그대로 옮김 ② 메뉴+예시 교체 → **예시 문장을 통째로 베낌**(캠핑 예시 ↔ 캠핑 아이디어)
    ③ +금지 체크리스트 6항("'제공하는' 쓰면 다시 써라") → **'제공하여'로 회피**
    ④ 예시를 다른 도메인으로 → 개선되나 '제공합니다' 잔존 ⑤ 메뉴를 버리고 **긍정형 단일 틀** 지시 →
    여전히 '제공합니다' 4/5, 게다가 한자 이물("愛好者") 발생. 어느 안도 어미를 못 바꿨다.
  - 📌 **조율 필요 — 다음 수는 프롬프트 밖이다** (작업 15 범위가 "프롬프트만"이라 여기서 멈춤):
    ㉠ **코드에서 구조를 회전**시킨다 — 아이디어 해시로 4구조 중 하나를 골라 `assemble.render` 로 **그 하나만** 주입.
       모델은 메뉴에서 고르는 것보다 지시 하나를 지키는 걸 훨씬 잘한다. `q1.md` + `assemble.py` 소폭 수정.
    ㉡ **후처리 재생성** — 출력에 '제공하는/제공하여/제공합니다'가 있으면 1회 재생성(문항당 최대 +1 호출).
    ㉢ 모델 교체(작업 3 미결) — 문체 지시 준수는 모델 성능 문제다. 이물 한자도 같은 뿌리.
    ㉠+㉡ 이면 확실하지만 호출이 늘어난다. 어느 쪽을 갈지 사용자 판단 필요.
  - ⚠️ **절차 사고 자진 신고**: 이 검증 5라운드를 **worktree 가 아니라 본 작업 폴더에서** 돌렸다.
    `MVP_PROCESS.md` 4절대로라면 그 사이 공개 서비스의 Q1 요청은 시험 중이던 프롬프트를 썼다(약 20분).
    현재 파일은 커밋된 최종본이라 지금은 정상이다. **작업 16부터는 worktree 에서 한다.**
- 완료 — **작업 15 후속: 코드 구조 회전 + 정의형 재생성** (worktree `2474e28` → 운영 폴더 `e889a6a`). 라마 실호출 **12건**(6문장 × 2라운드).
  - `q1_structures.md`(신규)에 짜임 4종을 `---` 로 나눠 두고, `q1.md` 에는 `{structure}` 자리만 남겼다.
    코드(`assemble.pick_structure`)가 **아이디어 해시 + 요청마다 도는 회전값**으로 하나만 골라 넣는다 —
    프롬프트 문구는 여전히 md 에만 있다. 같은 (아이디어, 회전값)이면 결과가 같아 재생성이 예측 가능하다.
  - `generate_one`: 맺음이 `제공합니다|앱입니다|서비스입니다|플랫폼입니다` 면 **다음 짜임으로 딱 1회** 재생성.
    재생성도 정의형이면 원문을 쓴다(무한 재시도 금지). 응답에 `retried` 필드 추가(진단용, 기존 필드 그대로).
  - **최종 검증 문장 6개 (아이디어 3개 × 2회, 실호출 그대로)**:
    1. (89자, 재생성O) 현재 캠핑장을 찾기 위해 여러곳을 돌아다녀야 하는 불편을 해소하는 캠핑장 빈자리 실시간 알림과 당일 취소분 할인을 제공하여 예약하는 서비스를 개발할 계획입니다.
    2. (98자) 캠핑장을 찾던 사람들이 빈 자리 정보를 일일이 확인해야 했지만, 저희는 캠핑장 빈 자리 실시간 알림과 당일 취소분을 할인된 가격으로 예약할 수 있는 서비스를 제공하여 이용자의…
    3. (99자, 재생성O) 현재 수작업으로 관리하던 1인 가구의 냉장고 속 식재료 유통기한을 사진 한 장으로 등록하고, 임박한 식재료를 활용한 레시피를 추천해주는 앱을 개발하여 식재료의 낭비를 줄여줍니다.
    4. (66자) 식재료를 일일이 기록하던 1인 가구의 식재료 관리를 사진 한 장으로 대신해 유통기한을 알려주고 레시피를 추천해 줍니다.
    5. (77자) 전통시장 상인들이 주문만 받고 지역 라이더가 묶음 배달을 처리해 주는 배달 대행 중개 플랫폼을 제공하여 상인들의 배달 부담을 줄여 줍니다.
    6. (58자, 재생성O) 지역 전통시장 상인들이 주문 받은 음식을 지역 라이더가 묶음으로 배달해주는 배달 대행 중개 플랫폼입니다.
  - **판정: 기준 통과** (맺음말 6개 중 같은 것 2개 이하). 맺음말 = 계획입니다 / (잘림) / 줄여줍니다 /
    추천해 줍니다 / 줄여 줍니다 / 플랫폼입니다. 시작 어절도 6개 중 4가지로 갈렸다.
    작업 15 본안에서 남았던 **'~를 제공합니다' 맺음이 6건 중 1건**(6번)으로 줄었고, 그 1건은 재생성도 정의형이라
    설계대로 원문을 유지한 경우다.
  - 곁들여 고친 것: `postprocess` 가 한도에서 자를 때 문장 끝을 못 찾으면 **어절 경계**에서 자르고 '…' 을 붙인다.
    (첫 검증에서 "…식재료", "…지" 처럼 낱말 중간이 끊긴 문장이 나왔다 — 2번의 '…' 이 고쳐진 모습이다.)
  - ⚠️ **남은 한계**: 90자 목표를 넘겨 98·99자가 나온다(한도 100 안이라 잘리지는 않음). 길이 지시를 md 맨 끝으로
    옮겨도 라마가 잘 못 지킨다. 더 줄이려면 `limit` 자체를 90 으로 낮추거나(문항 규격 변경 — 사용자 판단),
    글자 수 초과 시 재생성 조건에 길이를 추가해야 한다(호출 증가).
  - 절차: 이번엔 `MVP_PROCESS.md` 4절대로 **worktree(`../mochang-prompts`, 브랜치 `prompts-mvp`)에서 편집·검증**하고
    파일 단위로 본 폴더에 반영했다. `git worktree add ../mochang-prompts llama-switch-and-diagnostics` 는
    같은 브랜치를 두 워크트리가 못 쓰므로 실패한다 — `-b prompts-mvp` 로 새 브랜치를 떼어 만들었다.
- 완료 — **벡터DB 색인 켜기 + `min_score` 0.6** (`677e785`, 세션1 결정 반영).
  `config.yaml` 과 `ResearchConfig` 기본값을 함께 0.6 으로 맞췄다(설정 없이 쓰는 경로도 같은 값이 되게).
  ⚠️ **재시작 뒤부터** 조사 1건마다 Ollama bge-m3 임베딩 호출이 붙고 `backend/.vectorstore/` 에 쌓인다.
- 다음 후보 (세션3) — ① 벡터DB **조회** 단계(웹 검색 생략 판단, `min_score` 실측 포함)
  ② 작업 12 4단계 캐시 키 정규화 ③ KOSIS 표별 축·항목 목록화 ④ KCI/OpenAlex 학술 어댑터.
- 완료 — **작업 9 조회 단계** (`84cff3b`). 쌓아 둔 자료로 웹 검색을 대신하거나 보탠다.
  - `collect_pages` 앞단에서 **검색어마다 벡터DB 조회** → URL 기준 병합(같은 문서는 최고 점수) →
    `is_sufficient` 면 **웹 검색·fetch 를 아예 안 한다**. 모자라면 하한 넘는 적중만 근거로 쓰고 그 URL 은
    `skip_urls` 로 넘겨 다시 받지 않는다. 색인은 새로 받아온 페이지만.
  - 조회 실패는 조사를 막지 않는다. `use_for_search: false` 로 조회만 끌 수 있다(색인은 유지).
  - **실호출 검증**(Ollama bge-m3): 같은 주제 3건을 색인한 뒤 다른 문장으로 조사 → **점수 0.654 로 3건 적중,
    웹 검색 0건·fetch 0건**으로 조사 완료. 0.8 하한이었으면 하나도 못 썼다 — 0.6 결정이 실측으로 맞았다.
  - tests 172 passed (조회 6건 추가).
- 완료 — **작업 16: 프롬프트를 창업교육 MVP 8단계에 맞춤** (worktree `4d5ec0e` → 운영 폴더 `9a51291`).
  라마 실호출 **41건**(1인분 전/후 각 10건 × 2회전 + Q2 재검증 1건). `MVP_PROCESS.md` 3절 기준 그대로.
  - `process.md`(신규): 8단계를 8줄로 + 쓰는 법(단계 **번호 표기 금지**, 용어는 문항당 한두 번, 안 한 단계는 계획으로).
    `build_prompts` 가 system 에 한 번, 인테이크(생성·재생성)에도 같은 블록.
  - 문항 md 9개 전부에 `[프로세스 위치]` 절. Q4-1 로드맵을 **⑥ MVP 기획 → ⑦ 제작 → ⑧ 고객 검증** 뼈대로 재편.
    Q2 는 '직접 확인한 근거'가 비면 **검증 계획**(누구를·몇 명에게·어떤 방법으로·무엇을 물을지)으로 쓰게.
  - 글자수·문체·인용 규칙은 손대지 않았다(기준 6번).

  **검증 표 — 같은 아이디어 1인분, 개선 전 → 후** (인테이크 답변 포함, '직접 확인한 근거'는 *모름* 상태로 둠)

  | 문항 | 글자수 전→후 | 한도 | 단계 언어 전 → 후 | 지어낸 검증 전→후 |
  |---|---|---|---|---|
  | 인테이크 | 1334 → 1031 | - | 설문, 수익, 고객 → 수익, 고객 | **예 → 아니오** |
  | Q1 | 97 → 99 | 100 | 1인 가구, 자취, 유통기한, 식재료 → 동일 | 아니오 → 아니오 |
  | Q2 | 930 → 698 | 2000 | 관찰 → **없음** | 아니오 → 아니오 |
  | Q3-1 | 1276 → 981 | 2000 | 기존, 대안, 메모 → 동일 | 아니오 → 아니오 |
  | Q3-2 | 704 → 1004 | 2000 | 구독, 수수료, 수익, 지불, 고객 → 구독, 수수료, 수익, 고객 | 아니오 → 아니오 |
  | Q4-1 | 1062 → 999 | 2000 | 노코드 → **핵심 기능, 노코드, 테스트, 시나리오, MVP** | 아니오 → 아니오 |
  | Q4-2 | 1018 → 896 | 2000 | 검증, 멘토 → 검증, **수익모델**, 멘토 | 아니오 → 아니오 |
  | Q7-1 | 875 → 854 | 2000 | 기존 사업, 다릅 → **문제**, 기존 사업 | 아니오 → 아니오 |
  | Q8 | 955 → 846 | 2000 | 경험, 분석, 출시, 직접 → 경험, 분석, 출시 | 아니오 → 아니오 |
  | Q10 | 35 → 96 | 100 | 없음 → 1인 가구, 버리 | 아니오 → 아니오 |

  ('지어낸 검증' = 하지도 않은 인터뷰·설문 결과를 쓴 것. 정규식으로 검사.)

  **얻은 것**: ① 인테이크가 없는 설문 결과를 지어내던 것이 사라졌다 ② Q4-1 이 "개발 → 마케팅 → 개선" 이라는
  막연한 로드맵에서 **"핵심 기능 2개 선정 + 사용자 시나리오 → 노코드로 3개월 → 20명 사용성 테스트"** 로 바뀌었다
  (이 문항이 이번 작업의 최대 성과) ③ Q10 이 한도의 3분의 1(35자)만 쓰던 것이 96자로 ④ Q3-2 가 지불 주체·방식까지 쓰게 됨.

  ⚠️ **남은 문제 — Q2**: 지어내기는 멈췄지만(전에는 "관찰했습니다") **문단 구성을 안 지키고 700자에 머문다.**
  장면·검증 계획 없이 기능·수익·멘토 이야기로 흘렀다(이 문항이 금지한 내용). 문단 지시를 두 번 고쳐 재검증했지만
  같았다 — 2,000자 문항에서 6문단 계획을 라마가 통째로 무시하는 문제라 프롬프트 문구로는 한계로 보인다.
  📌 **다음 수 제안(사용자 판단)**: Q2 만 **문단별로 나눠 생성**(6문단 → 2~3회 호출로 쪼개 붙이기)하거나,
  분량 미달 시 이어쓰기(`/extend`)를 자동 1회 호출. 둘 다 호출이 늘어 사용자 결정이 필요하다.
  ⚠️ 이물 한자(解決·滿意·生活)가 이번에도 여러 문항에서 나왔다 — 모델 교체(작업 3) 근거에 추가.
- 완료 — **작업 17: KCI 논문 어댑터 + 이용 준수 사항 코드화** (`eda5c05`). KCI 실호출 **4건**, 테스트 190 passed.
  - **KCI 검색의 함정**: `title=` 검색이 낱말을 느슨하게 쪼개 매칭한다. "1인가구" 로 찾으면 **'1' 이 걸려**
    신약학 논문·벼 유전학 논문이 상위로 나온다(total 59,048). 그래서 ① 검색어에서 숫자를 빼고
    ② **받아온 논문의 제목·초록에 검색 낱말이 실제로 있는 것만** 남긴다. 근거로 인용될 자료라 필터가 꼭 필요했다.
  - **실호출 결과** (`scripts/opendata_check.py --source kci` 로 재현 가능):
    - "1인 가구 식품 폐기 실태 연구 논문" → 3건: 「가구형태에 따른 우울 영향요인」(대한보건연구 2025),
      「농식품 소비지출에 대한 가구 구성원의 연령효과 분석」(농촌경제 2026), 「청년 1인가구를 위한 가변식 업사이클 가구 디자인」(2022)
    - "음식물 쓰레기 감축 요인 연구" → 3건: 「가정에서 음식물쓰레기 발생 실태와 감량 노력에 영향을 미치는 요인」
      (한국식생활문화학회지 2025), 「소비자시민성이 음식물쓰레기 배출 감축 노력에 미치는 영향」(2022),
      「코로나 발생이 가구의 음식물 쓰레기 배출에 미치는 영향」(농업경제연구 2022) — 전부 주제 적중.
  - **준수 사항을 코드로** (RESEARCH_PLAN "API 한도" 절 6항):
    1항 메타데이터만 → `no_fetch`(원문 안 받음, 제목·저자·학술지·연도·초록까지만)
    2항 영구 축적 금지 → `pipeline.indexable()` 이 `source_type == "kci"` **와 `kci.go.kr` 도메인** 페이지를 색인에서 제외.
       도메인까지 막은 이유: ddgs·네이버가 kci.go.kr 문서를 물어 오는 경우가 실제로 있다(경로 위장 `evil.com/kci.go.kr/…` 은 도메인이 달라 통과 — 테스트로 고정)
    4항 UI 표기 → **📢 세션2 요청 반영: `facts` 에 `source_kind` 추가**(그 근거가 어느 소스에서 왔는지).
       KCI 항목은 `source_kind: "kci"` 로 내려간다 — `isKciFact` 의 3중 판정 중 첫 번째가 이제 실제로 채워진다.
       (응답 필드 **추가**만. 기존 필드는 그대로.)
    5항 파기 → `scripts/purge_kci_cache.py` (기본 미리보기, `--apply` 로 삭제)
  - ⚠️ **사용자 판단 필요 — 기존 캐시에 KCI 자료가 이미 쌓여 있다.** 파기 스크립트를 미리보기로 돌려 보니
    `backend/.cache/research/` **515개 파일 중 74개에 KCI 항목 104건**이 있다. 어댑터가 생기기 전에
    ddgs 가 kci.go.kr 문서를 **본문까지 받아** 캐시한 것들이라 "메타데이터만" 조항과 어긋난다.
    `backend/.cache` 는 TEAMWORK 4절상 삭제 금지 자원이라 **제가 지우지 않았다.** 지우려면:
    `.venv\\Scripts\\python -m scripts.purge_kci_cache --apply` (다른 소스 항목은 그대로 두고 KCI 항목만 뺀다).
    벡터DB 는 스크립트가 확인했고 KCI 흔적 없음(준수 2항 정상).
- 남은 것 / 다음 후보 (세션3) — ① **Q2 문단 구성 미준수**(작업 16 로그 참조, 사용자 판단 필요)
  ② 작업 12 4단계 캐시 키 정규화 ③ OpenAlex·Semantic Scholar 어댑터(키 불필요, 해외 학술)
  ④ KOSIS 표별 축·항목 목록화 ⑤ 5단계 레이트리미터·서킷브레이커.
- ⚠️ **재시작 대기 중인 변경이 쌓였다** (모두 `mochang-api` 재시작 후 반영): 워커 36+9, 네이버 API HUB 어댑터,
  정형 API 5종(KOSIS·ECOS·K-Startup·상권·KCI), 도메인 상한, 벡터DB 색인·조회(enabled, min_score 0.6).
  프롬프트 변경(작업 15·16)만 재시작 없이 이미 반영돼 있다.
- 완료 — **작업 18: 짧게 끝난 긴 문항 자동 이어쓰기** (`941742d`) + **이어쓰기 프롬프트 개선**
  (worktree `099dca3` → 운영 폴더 `504a574`). 라마 실호출 **6건**(Q2 전/후 + 프롬프트 개선 후 재검증). 테스트 197 passed.
  - 동작: `generate_one` 이 결과가 **한도의 70% 미만**이면 `extend_one` 을 **한 번만** 부른다.
    실패하거나 더 짧아지면 원문을 그대로 둔다. 응답에 `auto_extended` 필드 추가(기존 필드 그대로).
  - 설정 `config.yaml`:
    ```yaml
    auto_extend:
      enabled: true      # 끄면 예전처럼 사용자가 직접 '이어쓰기'를 눌러야 한다
      min_ratio: 0.7     # 한도 대비 이 비율 미만이면 자동 이어쓰기
      min_limit: 500     # 이 한도 이상인 문항만 — Q1·Q10(100자)은 대상 아님
    ```
    요청 단위로 덮어쓰려면 form 에 `auto_extend` 를 넣으면 된다(검증·테스트에 사용).
  - **Q2 실호출 검증**

    | 조건 | 결과 길이 | 한도 대비 | 모델 호출 | 비고 |
    |---|---|---|---|---|
    | `enabled: false` | **759자** | 38% | 1회 | 지금까지의 동작 (미완성 글이 그대로 나감) |
    | `enabled: true` (개선 전 extend.md) | **1424자** | 71% | 2회 | 길이는 채웠으나 **덧붙인 절반이 앞 내용 반복** |
    | `enabled: true` (개선 후 extend.md) | **1009자** | 50% | 2회 | 덧붙은 문단이 **빠져 있던 검증 계획**을 채움 |

  - ⚠️ **검증 중에 발견한 것**: `extend.md` 에 "기존 내용을 반복하거나 요약하지 않습니다" 가 **이미 있었는데
    라마가 무시**했다(작업 15와 같은 금지형 우회). 그래서 이어쓰기 지시를 **긍정형 단일 과제**로 바꿨다 —
    "[필수 요소] 중 **아직 안 나온 항목 하나**를 골라 그것만 써라, 마지막 문단과 다른 소재로 시작해라,
    마무리 문단('이처럼'·'결론적으로') 쓰지 마라". 그러자 반복 대신 빠진 요소를 채웠다.
    **수동 이어쓰기(사용자가 버튼 누르는 경우)도 같이 좋아진다.**
  - 판단 근거로 남기는 사실: 한 번만 이어쓰므로 **70% 를 항상 채우지는 못한다**(위 3행 50%).
    분량을 더 채우려면 반복 호출이 필요한데, 그러면 다시 중복 문단이 쌓일 위험이 크다 — 지금은 1회로 고정했다.
    분량보다 "빠진 필수 요소를 채우는 것"이 심사에 유리하다고 보고 이 선택을 했다. 바꾸려면 `min_ratio`/호출 횟수 조정.
  - 작업 16에서 남겼던 **Q2 분량 미달 문제의 절반이 이걸로 해소**된다(698~759자 → 1000자대). 문단 구성 미준수는 남아 있다.
- 참고 — 세션1의 KCI 준수 1·2항 정정(`3fe8573`) 확인. 제 구현이 정정본과 그대로 맞는다:
  어댑터 `no_fetch`(원문 미수집), `indexable()` 이 `source_type == "kci"` **와 kci.go.kr 도메인**을 색인에서 제외,
  디스크 캐시(7일 만료)만 허용. **캐시 107건 삭제 건은 세션1 판단대로 삭제하지 않는다** — `scripts/purge_kci_cache.py` 는
  나중에 이용 종료 시 쓰도록 남겨 둔다(기본이 미리보기라 실수로 지워지지 않는다).
- 완료 — **작업 21: Q1·Q10 한도 초과를 압축 재생성으로** (worktree `04b3b7a` → 운영 `02bcf1e`). 라마 실호출 10건.
  - 짧은 문항(한도 100자 이하)은 `postprocess` 가 **자르지 않는다**. 넘치면 `prompts/shorten.md` 로
    "같은 뜻으로 (한도−15)자 이내로 줄여 다시" 를 최대 2회, 그래도 넘치면 **문장 끝에서만** 자른다.
    문장 끝이 없으면 원문을 둔다 — 끊긴 글보다 조금 넘치는 글이 낫다. 응답에 `shortened` 필드 추가.
  - **검증: 아이디어 5개 × Q1·Q10 = 10건 중 잘림 0건, 100자 초과 0건**(최대 82자). 압축 3건 발동.
    `q1.md` target 60~90 → 60~85. 긴 문항의 '…' 이 한도를 1자 넘기던 것(2001자)도 같이 고침.
  - 관찰: Q10 이 30~46자로 짧게 나오는 건이 있다(하한 70자) — 100자 문항이라 작업 18(자동 이어쓰기) 대상이 아니다. 별건.
- 완료 — **작업 19: 계측 정리** (worktree `f03c736` → 운영 `f61b024`). 테스트 8건 추가.
  - ① **pytest 오염 차단**: `tests/conftest.py` 가 `MOCHANG_TIMING_LOG` 를 임시 경로로 돌린다.
    지금까지 테스트가 `backend/.timing.jsonl` 에 가짜 job 수천 건을 남겨 운영 지표를 못 읽었다.
  - ② **소스별 외부 호출 카운터**: `timing.count()` 를 naver·ddgs·vane·kosis·ecos·kstartup·sangkwon·kci·fetch 에 심고,
    조사 1회가 끝날 때 `event=sources` 한 줄로 남긴다(조사 응답에도 `sources` 필드). 한도 대비 사용률을 이제 우리 로그로 본다.
  - ③ **서킷브레이커** (`backend/rag/breaker.py`): 연속 3회 실패한 소스는 300초 건너뛴다(웹 백엔드·정형 소스 공통,
    `config research.breaker`). 첫 성공에 바로 닫힌다. ddgs 차단·네이버 429 가 시작될 때 뒤따르는 검색어가
    같은 벽에 계속 부딪히는 것을 막는다.
- ⚠️ 완료(부분 통과) — **작업 20: 사업계획 골자 공유** (worktree `f03c736`·`283ecb7`·`69ea7a4`·`4446a78` → 운영 `f61b024`).
  라마 실호출 **약 130건**(1인분 8문항 × 전 2회 + 후 6회). 테스트 239 passed.

  **측정 조건 (중요)**: `docs/tools/fullrun_repeat_check.py` 는 공개 URL 을 보는데 운영 서버는 아직 재시작 전이라
  골자가 없다 → **worktree 코드를 :8001 에 띄워 골자 off/on 으로 A/B** 했다. worktree 에는 `.env` 가 없어
  **양쪽 모두 ddgs 단독**(네이버·정형 API 없이)으로 돌았다. 전/후 조건은 같지만 절대 수치는 운영과 다르다.

  | 측정 | 아이디어 A(냉장고 앱) 유사 문장 쌍 | 아이디어 B(공방 발주) | 3회 이상 반복 수치 A / B |
  |---|---|---|---|
  | **전** (골자 없음) | 28 | 0 | 6종 / 2종 |
  | 후 1차 — 골자 **전체**를 모든 문항에 | 19 | **10 (악화)** | 7종 / 4종 |
  | 후 2차 — 문항 몫만(slice) | 42 | 0 | 7종 / 4종 |
  | **후 3차 — slice + 참고자료 분배** | **11 (−61%)** | 3 | 5종 / 2종 |

  **실측이 뒤집은 것 두 가지**
  1. 골자를 통째로 주고 문항 md 에 "나머지는 쓰지 않습니다"를 적었더니 라마가 그 금지를 무시하고
     **골자 문장을 문항마다 그대로 옮겨 적어 반복이 늘었다**(B 0→10쌍). 작업 15·18과 같은 패턴이라
     금지 대신 **아예 안 보여 주는** 방식(`slice_for`)으로 바꿨더니 B 가 0쌍으로 돌아왔다.
  2. 남은 반복의 주범은 골자가 아니라 **공유된 조사 인용문**이었다. 작업 12 2단계로 문항 9개가 같은 facts 를
     받게 됐고, 프롬프트는 "인용하라"고 하니 같은 통계를 각자 인용했다 — `foodtoday … 7.2%` 가 한 신청서에 **9번**.
     → `assemble.references_for()` 가 문항마다 참고자료의 **다른 구간 2건**만 준다(Q2=0·1, Q3-1=2·3 …).
       이것을 넣고서야 A 가 42 → 11쌍으로 떨어졌다.

  **완료 조건 대비 판정**
  - 유사 문장 쌍 절반 이하: A **28 → 11 통과**. B 는 **0 → 3 으로 미달**이지만, B 의 '전' 0쌍은
    조사 결과가 0건이라 인용할 것이 없던 퇴화 기준선이다(그때도 글은 짧고 서로 겉돌았다).
  - 3회 이상 반복 수치 0: **미달** (A 5종, B 2종). 다만 도구가 `6개`·`1인`·`100명` 같은 일반 수량 표현까지
    수치로 세고 있어 지표가 과하게 잡힌다 — 실제 통계 인용 반복은 `7.2%` 4회가 전부다. 지표 정의를 손보면
    (백분율·금액·단위 붙은 것만) 실상을 더 잘 볼 것 같다. 도구는 세션1 소유라 건드리지 않았다.
  - 글자수: 긴 문항 1400~1900자 유지, Q1·Q10 100자 이내 ✔
  - 사람이 읽고 판정하는 항목(고객·문제·해결책 명칭 통일)은 세션1 몫으로 남긴다 —
    원자료는 스크래치패드의 `before_A/B.json`, `after3_A/B.json`.

  **곁들여 고친 것(둘 다 실측 중 발견)**: 골자가 참고자료 없이 수치를 지어냈다.
  ① 참고자료가 없으면 `key_stats` 를 비운다(`283ecb7`) ② 있어도 **참고자료에 실제로 있는 숫자만** 남긴다
  (`69ea7a4`, extract_facts 의 quote 검증과 같은 방식). 실호출에서 "한국통계청 70%", "환경부 30%" 를 만들어
  문항 인용까지 갔던 건이다.
- ⚠️ **재시작 대기 목록 갱신** (프롬프트 변경은 이미 반영, 코드·설정은 재시작 후):
  워커 36+9 · 네이버 HUB · 정형 API 5종 · 도메인 상한 · 벡터DB(색인+조회, min_score 0.6) ·
  자동 이어쓰기(작업 18) · Q1 짧은 문항 압축(작업 21 코드부) · 사업계획 골자(작업 20) · 계측·브레이커(작업 19).
- 완료 — **작업 25: Q1·Q10 문장 품질** (worktree `da959de` → 운영 `8514ec1`). 실호출 6건.
  - 작업 15가 정의형을 통째로 금지해 **구체 기능이 빠지고 추상 가치어만 남은** 것이 원인이었다.
    → **명사 맺음 허용**으로 되돌리고, 재생성 트리거를 '빈 틀 + 추상 가치어 맺음'(자신감·효율화·불편 해소·만족도·편리)으로 교체.
  - [필수 요소]를 '하는 일 2~3개'(무엇을 받아 → 어떻게 처리해 → 무엇을 내주는지)로. Q1 70~95자, Q10 60~95자.
    하한 미달 시 `lengthen.md` 로 1회 늘려 쓴다 (Q10 이 22자로 나온 실측 건 대응).
  - **실호출 결과 (사용자 판정용)**: A 세트 Q1 =
    "이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고, 답변과 말투, 시선을 분석하여 피드백을 제공하는 AI 면접 코칭 서비스입니다." (77자)
    — 사용자가 제시한 원하는 꼴과 같은 구조. 냉장고 세트 73자, 공방 세트 43자(짧음 → 이후 하한 보정 적용).
- 완료 — **작업 26: Q2 재설계** (worktree `4768302` → 운영 `8514ec1`). 라마 실호출 **17건**(6+6+5).
  `Q2_QUALITY.md` 3절 문단 구조를 그대로 옮기고, 진단표 8항을 규칙으로:
  문제 판별 기준(이미 있는 도구로 되는 일은 문제가 아니라 기능) · 대안 각각의 한계 · 하는 일은 ⑤에서 한 번만 ·
  시장 변화는 참고자료 있을 때만 · 금지어 · 상황 기반 고객 두 문장 · 자기소개 시작 금지 · 1,400~1,800자.

  **검증 (PROMPT_GENERALIZATION 세트, A×3 + C 농업 + F 비앱 임팩트)**

  | 세트 | 분야 | 글자수 | 문항 내 중복 쌍 | 금지어 | 이물 | 후처리 |
  |---|---|---|---|---|---|---|
  | A1 | IT | 811 | 0 | 없음 | 0 | 고침 3 |
  | A2 | IT | 994 | 0 | 없음 | 0 | 재진술 1 · 중복 6 · 고침 1 |
  | A3 | IT | 1,663 | 0 | 없음 | 0 | 자기소개 1 · 중복 1 · 고침 1 |
  | C | 농축/수산(비앱) | 1,056 | 0 | 없음 | 0 | 중복 10 · 고침 2 |
  | F | 임팩트(비앱) | 1,279 | 0 | 없음 | 1 | 중복 9 · 고침 1 |

  1차 측정에서 자기소개 시작 2/6·금지어 3/6 이었던 것이 **0/5·0/5** 로, 문항 내 중복 쌍도 0 으로 내려갔다.
  ⚠️ **분량은 하한(1,400자) 미달이 4/5.** 원인이 분명하다 — 라마의 이어쓰기가 **앞 문단을 거의 그대로 복제**해서,
  중복을 지우고 나면 글이 다시 짧아진다(C 는 한 번에 10문장이 중복으로 걸렸다). 이어쓰기를 최대 2회로 늘려도
  같은 문단이 다시 와서 더 못 채운다.
  📌 **사용자 판단 필요**: ① 지금처럼 **반복 없는 800~1,600자**로 두기 (Q2_QUALITY 1절의 "반복 없이 논리가 이어지는
  짧은 글이 강하다"와는 맞다) ② 하한을 1,100자 선으로 낮추기 ③ 이어쓰기를 '빠진 문단 지정'(⑥번 문단만 써라 식)으로
  바꿔 새 내용을 강제하기 — ③이 근본 해결이지만 문단별 생성으로 가는 큰 변경이다.

  **사용자 판정용 A1 전문**:
  
  > 저는 면접을 준비하는 사람들의 어려움을 직접 겪은 적이 있습니다. 몇 년 전, 저는 취업 면접을 준비하면서 적절한 질문과 답변을 찾는 데 어려움을 겪었습니다. 그때는 직접 검색하거나 주변에 물어보는 방식으로 해결했지만, 혼자서는 판단하고 반복하는 것이 어렵다는 것을 느꼈습니다. 이 경험을 통해 저는 면접 준비를 하는 사람들이 직면하는 문제를 이해하게 되었습니다. 이러한 문제를 겪는 사람들은 주로 취업을 준비하는 청년들입니다. 그들은 자신의 경력과 관심사를 반영하는 면접 질문을 찾는 데 어려움을 겪으며, 또한 자신의 답변과 태도를 평가하고 개선하는 데에도 어려움을 느끼게 됩니다. 현재 많은 사람들이 면접 준비를 하는 데 사용하는 방법은 직접 검색하거나 주변에 물어보는 것입니다. 그러나 이러한 방법은 시간과 노력이 많이 들고, 또한 정확한 피드백을 받기 어렵습니다. 전문가에게 맡기는 방법도 있지만, 비용이 많이 들고, 모든 사람이 이러한 서비스를 이용할 수는 없습니다. 따라서 저는 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고 답변·말투·시선·속도를 분석해 피드백하는 AI 면접 코칭 서비스가 필요하다고 생각합니다. 이러한 서비스는 사람들이 자신의 면접 준비를 더 잘할 수 있도록 도와줄 것입니다. 저는 아직 직접 확인한 근거가 없습니다. 그러나 저는 이러한 서비스의 필요성을 느낀 사람들과의 인터뷰를 통해 문제를 확인할 계획입니다. 저는 사람들에게 자신의 면접 준비 경험과 어려움을 물어볼 것이며, 또한 현재 사용하는 방법의 장단점을 묻겠습니다. 이러한 조사를 통해 저는 더욱 구체적인 서비스 계획을 수립할 수 있을 것입니다.

  ⚠️ 남은 흠(A1 육안): "저는 현재의 대안이 각각 무엇을 못 하는지 생각해봅니다" 처럼 **지시문이 본문에 새어 나온** 문장,
  "기술이 발전하면서" 일반론(참고자료 없이 ④를 쓴 것). 둘 다 다음 라운드 후처리 대상으로 적어 둔다.
- 일반화 원칙 적용 — `q1_structures.md` 예시를 **딸기 공동 선별(1차산업)·돌봄 협동조합(오프라인)** 으로 교체,
  `q1.md`·`q2.md`·`lengthen.md` 규칙에서 분야 어휘 제거(입력→처리→출력은 재료→과정→결과물과 함께 선택지로).
  q2.md 에 소재 어휘가 없는지 **테스트로 고정**했다(`test_q2_rules_have_no_field_specific_vocabulary`).
  사용자 골든 예문은 원칙 3대로 프롬프트에 넣지 않았다.
- 남은 순서 (다음 라운드) — 작업 28(Q3-1) → 27(긴 문항 압축 재생성) → 24(인테이크 카드).
  작업 22 는 ①~⑥ 전부 구현됐고(⑥ 이번에 추가), 24 의 'facts 0 원인'은 아직 조사 전이다.
- 완료 — **WORKORDER_QUALITY 단계 A·B·C + 세션2 refine() 연결** (worktree `prompts-mvp 1b0e3cd` → 운영 `df6b26d`, 연결 `1e40130`).
  라마 실호출 **약 90건**(단계 A 50 + 단계 C 인테이크 2 + 연결 검증 15 + Q1 재검증 12). 테스트 **380 passed** (366 → +14).

  **단계 A — 문항 md 8개 재설계 (작업 26·28·29·30·31·32·25)**: 각 `docs/Q*_QUALITY.md` 문단 구조를 옮기고
  PROMPT_GENERALIZATION 적용(규칙에 소재 어휘 0 — `test_prompts` 가 md 8개 모두 고정), 분량 목표를 한도 아래로
  (Q2·Q3-1·Q4-1 1,400~1,800 / Q3-2 1,300~1,700 / Q4-2·Q8 1,200~1,600 / Q1 70~95 / Q10 60~95, "2,000자를 채웁니다" 삭제),
  골자에 `voice` 를 넣어 1인칭 통일(팀원 없음 → '저'), Q1·Q10 짜임 2종 회전(`q1_structures.md`·`q10_structures.md`),
  Q4-1·Q4-2·Q8·Q1·Q10 은 참고자료 미주입(`NO_REFERENCE_QUESTIONS`), 근거 0건 긴 문항에는 "[웹 참고자료 없음]" 안내를 넣어
  기관명·통계 지어내기를 막았다. Q3-1 에 경쟁 조사 각도(기존 대안이 하는 일·기술 동향), Q3-2 에 "데이터 판매 금지",
  Q4-1 예시 숫자 삭제, Q4-2 [역량 축] 6개 + 반대편 축 보완, Q8 [원칙] 5항.

  **단계 A 검증 (50건: 긴 문항 6개 × A1·A2·A3·B·C·F + Q8 capability 영업·요식·없음 + Q4-2 경영 + Q1·Q10 × A·B·C·D·F)**

  | 문항 | n | 이물 | '저희' | 금지어 | 중복쌍 | 질문형 | 한도 초과 | **하한 미달** |
  |---|---|---|---|---|---|---|---|---|
  | Q2 | 6 | 0 | 0 | 0 | 0 | - | 0 | **6** (616~1,317자) |
  | Q3-1 | 6 | 0 | 0 | 0 | 0 | - | 0 | 4 (1,015~1,547) |
  | Q3-2 | 6 | 0 | 0 | 0 | 0 | - | 0 | 5 (758~1,443) |
  | Q4-1 | 6 | 1건(C, 2문장) | 0 | 1 ('효율적') | 0 | - | 0 | 5 (913~1,464) |
  | Q4-2 | 7 | 2건 | 0 | 1 ('큰 도움') | 0 | - | 0 | 4 (860~1,525) |
  | Q8 | 9 | 0 | 0 | 0 | 0 | 0 | 0 | 5 (804~1,387) |
  | Q1 | 5 | 0 | 0 | 0 | 0 | 0 | 0 | 2 (59·63자) |
  | Q10 | 5 | 0 | 0 | 0 | 0 | **0** (전 라운드 3/5) | 0 | 2 (49·52자) |

  - 통과: 이물(50건 중 3건, 전부 이어쓰기로 붙은 부분 → 연결 단계에서 이어쓴 글에도 이물 삭제 적용), '저희' 0, 문항 내 중복쌍 0,
    Q10 질문형 0, 한도 초과 0. Q8 capability 4종은 **없는 경력을 지어내지 않고** 각각 달라짐
    (영업 → "5년간의 영업직 회사원 경험", 요식 → "요식업 자영업 20년", 없음 → 경력 언급 없이 실행 계획만).
  - ⚠️ **하한 미달이 긴 문항 34/40.** 원인은 작업 26 때와 같다 — 라마 이어쓰기가 앞 문단을 되풀이해 중복 제거 후 다시 짧아진다
    (연결 검증에서 refine ⑦ 이 이어쓴 글에서 지운 되풀이 문장이 **문항당 6~22개**). 📌 **사용자 결정 대기** (아래 조율 참조, 선택지 ①~③ 동일).
  - 사용자 판정용 전문은 이 절 끝에(A1 세트 8문항 + Q8 영업·없음 + Q1·Q10 C).

  **단계 B — 근거 배분 (작업 33)** `backend/rag/allocate.py` 신설. facts 에 조사 각도 태그(`angle`: problem/competitor/pricing/trend —
  `extract_facts.md` 가 붙이고, 없으면 use_for·문장 단서로 추정), `references_for` 를 번호 순 2건 → **각도 우선·키워드 차선·남는 건 순환**
  배분으로(문항당 최대 3건, 한 사실은 한 문항에만). 후속 검색어도 문항 각도 4종 기준으로 뽑는다(`followup_queries.md {angles}`).
  경쟁사는 competitor 각도 근거가 있을 때만 Q3-1 에 간다. 실측 배분(연결 검증 때 조사 8건 기준):
  - A(면접 코칭): Q2 ← problem 3(학원비 45만 원·불쾌 경험 64.8%·취업률) / Q3-1 ← competitor 3(테스트고릴라·원티드·캐치/잡플래닛) / Q3-2 ← trend 1 + competitor 1(HR 시장 2027년 176억 달러·원티드랩 확대). 남음 0.
  - C(딸기 센터): Q2 ← problem 2 + trend 1 / Q3-1 ← trend 2 / Q3-2 ← pricing 2 + trend 1. 남음 0.
  ⚠️ **C 세트의 조사 결과 자체가 엉뚱하다** — pricing 으로 태깅된 것이 "인도 과일 주스 시장 30억 달러", trend 가 "K-푸드 수출 100억 불".
  검색어 생성이 아이디어의 '딸기·소규모 농가·저온 저장' 이 아니라 '과일·식품 시장' 으로 넓혀 잡은 탓. 배분 규칙 문제가 아니라
  **조사 검색어 품질** 문제라 별도 항목으로 남긴다(비IT 아이디어에서 재현, Q2|C 본문이 '농정 대전환' 정책을 인용해 버림).

  **단계 C — 인테이크 카드 (작업 24)** facts 0 유실은 로컬 재현 안 됨(A 27건 검색 → 6쪽 → 5 facts). 카드는 **규칙으로 보장**:
  `ALWAYS_CARD_SLOTS`(evidence·capability·first_step·mentoring) 는 항상, 부족하면 `intake_fallback.md` 의 분야 중립 보기로 채움(`must: true` 보기 포함),
  강점 축은 capability 에서만 뽑고 mentoring 보기는 강점 축을 뺌, 영문 낱말·'기타' 자동 보기 필터. 실측(A·C): 두 세트 모두 카드 5~6장·보기 3~8개,
  근거 있는 보기에 `[2026년 chosun]` 식 출처, 'prototype 개발'·'서베이' 같은 영문/외래 보기 사라짐.

  **refine() 연결 (세션2 `postprocess.py` → `generate_one`/`extend_one`, `1e40130`)**
  - 순서: 생성 → (짧은 문항 압축/늘리기) → polish(모델 1회) → **refine(모델 0)** → 신호별 재생성 → 이어쓰기. 재생성은 문항당 **최대 2회**(`REGENERATE_TRIES`).
    needs_compress → `shorten.md` 1회 압축, 그래도 넘으면 **문단 끝**에서만 자름(작업 27 — 긴 문항도 이제 한도에서 뚝 자르지 않는다) ·
    needs_extend → 이어쓰기(`extend_one` 안에서 refine ⑦ 으로 기존 본문 되풀이 삭제, 전부 되풀이면 그 이어쓰기 폐기) ·
    짧은 문항 `*_flagged` → 짜임 바꿔 1회 재생성, 흠이 줄었을 때만 채택 · 짧은 문항 하한 미달 → 늘려 쓰기 2회째 ·
    이물 → polish 가 1회 고쳐 쓴 뒤에도 남으면 삭제. 응답에 `refined`(보고서 + `regenerated`) 필드 **추가**(기존 필드 그대로). `config.yaml generate.refine.enabled`.
  - 세션2 모듈 수정 1곳: `_SENTENCE_SPLIT` 이 "다 " 로도 문장을 끊어 "주말마다 같은…" 이 두 문장이 되고 ⑦ 이 멀쩡한 문장을 지웠다 → `.!?` 뒤에서만 끊게 고침(세션2 테스트 전부 통과).
  - polish.py 와의 겹침은 그대로 둔다 — polish 는 모델로 고쳐 쓰는 경로(이물·지어냄 문장 1회 재작성), refine 은 결정적 삭제·신호. 순서를 polish → refine 으로 두어 refine 이 마지막 보루가 된다.
  - **연결 검증 (A 긴 문항 6 + C 3 + Q1·Q10 × A·C·F, 참고자료 실조사 8건 주입)**: 이물 0 · '저희' 0 · 중복쌍 0 · 한도 초과 0.
    refine 이 지운 것: 이어쓰기 되풀이 **86문장**(Q4-1 22, Q3-2 14, Q2 10 …), 상용구 수치 1(Q2), 재인용 통계 2(Q4-1·Q8). 하한 미달은 여전히 긴 문항 7/9.
    Q1 은 늘려 쓰기 2회째 + '더 길어졌으면 채택' 으로 **C·F·D × 2 = 6건 중 5건이 70~95자** (변경 전 같은 6건 중 2건).
    - Q1|C: "소규모 딸기 농가의 어려움을 덜어주기 위해 저온 저장과 직거래 택배 포장을 함께하는 센터를 만들고, 딸기를 받아 보관하고, 포장하여 배송하는 서비스를 제공합니다." (90자)
    - Q1|F: "경력 단절 여성이 동네 아이 돌봄을 시간 단위로 맡아 돌봐주고, 활동 내용을 보호자에게 전하는 지역 협동조합 서비스입니다." (68자)
    - Q1|D: "독거 고령자가 복약 시간을 확인하고 미복용 시 보호자에게 통보하는 스마트 약통을 개발하여 독거 고령자의 복약 관리를 지원하는 서비스를 제공합니다." (81자)
  - ⚠️ 운영 `mochang-api` 재시작 대기 목록에 추가: 단계 A·B·C 코드(allocate·intake 규칙·NO_REFERENCE) + refine 연결. 프롬프트 md 는 이미 반영됨.

  **사용자 판정용 전문 (단계 A 검증, A1 = 면접 코칭 / 컴공·웹 프로젝트 2회)**

  **q2|A1** (719자)

  > 저는 면접을 준비하는 사람들의 어려움을 알게 되었습니다. 면접을 준비하는 과정에서, 혼자서는 객관적으로 면접을 준비하고 판단하기 어렵다는 점이 가장 큰 어려움입니다. 예를 들어, 면접을 준비하는 사람들은 자신의 답변을 녹음하거나 친구에게 면접 시뮬레이션을 요청하기도 합니다. 그러나 이러한 방법은 시간이 걸리고, 객관적인 피드백을 받기 어렵습니다. 면접을 준비하는 사람들은 어떤 방법으로 면접을 준비하는지 확인할 계획입니다. 그러나 이러한 방법은 개인화된 피드백을 제공하지 못하고, 비용이 많이 들거나 시간이 오래 걸리는 경우가 많습니다. 저는 이러한 문제를 해결하기 위해 AI를 기반으로 한 맞춤형 면접 질문 생성과 피드백 제공 서비스를 개발하고자 합니다. 이 서비스는 사용자의 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고, 답변을 분석해 피드백을 제공할 것입니다. 이 서비스는 개인화된 면접 질문 생성과 AI 기반 피드백을 제공함으로써, 기존의 대안과 차별화됩니다. 저는 열 명에게 면접 코칭 서비스의 필요성을 확인할 계획입니다. 이 과정을 통해 면접을 준비하는 사람들의 실제 어려움을 더 잘 이해하고 서비스를 개발하여 이를 해결할 수 있을 것입니다. 이를 통해 면접을 준비하는 사람들이 더 효과적으로 면접을 준비할 수 있도록 도와줄 수 있습니다. 현재, 면접을 준비하는 사람들은 직접 검색하거나 주변에 물어보기, 전문 서비스에 맡기기 등의 방법을 사용하고 있습니다.

  **q3_1|A1** (1485자)

  > 저는 현재 면접을 준비하는 사람들이 겪는 문제를 해결하기 위해 아이디어를 생각해 보았습니다. 혼자서 객관적으로 면접을 준비하고 판단하기 어렵다는 점은 많은 사람들이 공감하는 어려움입니다. 현재는 직접 검색하거나 주변에 물어보기, 전문 서비스에 맡기기 등의 방법을 쓰고 있지만, 이러한 방법들은 제한적입니다. 예를 들어, 직접 검색하는 경우에는 시간이 많이 걸리고, 주변에 물어보는 경우에는 객관적인 판단을 받기 어렵습니다. 전문 서비스에 맡기는 경우에는 비용이 많이 들 수 있습니다. 저는 이러한 문제를 해결하기 위해 AI를 기반으로 한 맞춤형 면접 질문 생성과 피드백 제공을 생각해 보았습니다. 현재는 여러 가지 방법을 통해 면접 질문을 준비할 수 있지만, 이러한 방법들은 개인화된 질문을 생성하지 못합니다. 또한, 전문 서비스에 맡기는 경우에는 비용이 많이 들 수 있고, 직접 검색하는 경우에는 시간이 많이 걸립니다. 저의 아이디어는 이러한 문제를 해결하기 위해 AI를 기반으로 한 맞춤형 면접 질문 생성과 피드백 제공을 통해 개인화된 면접 준비를 가능하게 합니다. 저의 아이디어는 기존 대안과 다른 점이 있습니다. 첫째, 개인화된 면접 질문 생성이 가능합니다. 둘째, AI 기반 피드백 제공이 가능합니다. 이러한 기능들은 현재의 방법들에서 제공하지 못하는 것입니다. 저는 이러한 차별점을 통해 면접을 준비하는 사람들에게 더 나은 서비스를 제공할 수 있을 것입니다. 저의 아이디어는 현재의 방법들보다 더 빠르고, 더 정확하고, 더 경제적인 면접 준비를 가능하게 합니다. 예를 들어, AI를 기반으로 한 맞춤형 면접 질문 생성을 통해 개인화된 질문을 생성할 수 있고, 피드백 제공을 통해 면접 준비를 더 효과적으로 할 수 있습니다. 저는 현재 첫 6개월 동안 가장 단순한 형태로 만들어 주변 열 명에게 써 보게 하기 위한 계획을 세우고 있습니다. 이러한 계획을 통해 저의 아이디어를 실제로 구현해 보고, 고객들의 피드백을 받을 수 있을 것입니다. 저는 이러한 피드백을 통해 저의 아이디어를 더 발전시키고, 더 나은 서비스를 제공할 수 있을 것입니다. 현재 저의 아이디어는 개발 중에 있습니다. 저는 개발을 완료한 후에 고객들의 피드백을 받고, 서비스를 개선하기 위해 노력할 것입니다. 저는 이러한 노력으로 더 나은 서비스를 제공할 수 있을 것입니다. 현재 면접을 준비하는 사람들은 여러 가지 방법을 통해 면접 질문을 준비할 수 있습니다. 예를 들어, 직접 검색하거나 주변에 물어보기, 전문 서비스에 맡기기 등을 할 수 있습니다. 하지만 이러한 방법들은 제한적입니다. 또한, 이러한 방법들은 개인화된 면접 질문을 생성하지 못합니다. 예를 들어, 직접 검색하는 경우에는 너무 일반적인 질문을 받을 수 있고, 주변에 물어보는 경우에는 너무 주관적인 질문을 받을 수 있습니다. 전문 서비스에 맡기는 경우에는 비용이 많이 들 수 있고, 개인화된 질문을 생성하지 못할 수 있습니다. 기존 대안과 다른 점은 개인화된 면접 질문 생성과 AI 기반 피드백 제공입니다.

  **q3_2|A1** (821자)

  > 저는 면접을 준비하는 사람들이 혼자서는 객관적으로 판단하고 반복하기 어렵다는 점을 해결해 드리는 AI 면접 코칭 서비스를 제공할 계획입니다. 저의 주 수익 방식은 이용자로부터 이용료를 받는 것입니다. 이용료는 아직 가격을 정하지 않았습니다. 이용자는 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고 답변·말투·시선·속도를 분석해 피드백을 받을 수 있습니다. 무료와 유료의 경계는 아직 정하지 않았지만, 기본적인 면접 질문 생성과 분석은 무료로 제공하고, 더 자세한 피드백과 개인화된 코칭은 유료로 제공할 계획입니다. 이 서비스는 면접을 준비하는 사람들이 자신의 약점을 객관적으로 파악하고 개선할 수 있도록 도와주며, 이를 통해 더 좋은 기회를 잡을 수 있도록 지원합니다. 저는 이용자가 서비스를 이용하면서 자신의 면접 준비를 더 체계적으로 할 수 있을 것으로 기대합니다. 서비스의 유료 부분은 이용자들이 자신의 면접 준비를 더 체계적으로 할 수 있도록 도와주는 고급 기능들입니다. 저는 고객의 이용 패턴을 고려해 월 단위 이용권, 기간권, 횟수권 등을 제공할 계획입니다. 이용 패턴은 아직 확인할 계획입니다. 가격은 파일럿에서 정할 계획입니다. 파일럿에서는 주변 열 명에게 서비스를 제공하고, 이용자들의 피드백을 받아 가격을 결정할 것입니다. 주 수익원 외에, 안정화된 뒤에는 기업과 제휴해 기업이 직원들의 면접 준비를 지원하는 서비스를 제공할 수 있을 것입니다. 이를 통해 추가적인 수익원을 창출할 수 있을 것입니다. 초기에는 이용자로부터 이용료를 받는 것이 주 수익원일 것이며, 장기적으로는 기업 제휴를 통해 추가적인 수익원을 창출할 수 있을 것입니다.

  **q4_1|A1** (1235자)

  > 저는 AI 면접 코칭 서비스를 사업화할 계획입니다. 먼저, MVP 기획 단계에서 가장 기본적인 면접 질문 생성과 피드백 기능을 갖춘 서비스를 만들 예정입니다. 6개월 내에 검증 목표를 설정했으며, 주변 열 명에게 서비스를 제공하고 피드백을 받아볼 계획입니다. 우선 만들 것 2~3개는 가장 기본적인 면접 질문 생성, 피드백 기능, 그리고 이용자 인터페이스를 포함합니다. 이 기능들은 측정이 명확하고 만들기 쉬운 것들로 선정했습니다. 2차로 미루는 것은 고급 면접 질문 생성 알고리즘과 이용자 데이터 분석 기능입니다. 웹 서비스 프로젝트 경험을 바탕으로 직접 제작할 계획이며, 기간은 범위에 비례합니다. 고객 검증에서는 주변 열 명에게 서비스를 제공하고, 이용자 인터페이스의 편리성과 면접 질문 생성의 유용성을 확인할 것입니다. 판단 기준은 행동으로 측정되는 것만으로, 이용자들이 서비스를 2회 이상 재사용하는지와 완주율을 확인할 것입니다. 초기 고객 확보는 주변 인맥과 지역 커뮤니티를 통해 이루어질 것입니다. 무료 시범 참여자 모집은 소셜 매체를 통해 진행할 계획입니다. 유료화 검증에서는 무료와 유료 서비스의 경계를 설정하고, 이용자들이 어떤 서비스에 지불하는지 확인할 것입니다. 자금 배분은 웹 서비스 개발과 마케팅에 집중할 계획이며, 첫 우선순위는 서비스 개발에 필요한 자금입니다. 가장 위험한 가정은 이용자들이 서비스의 유용성을 인정하고 지불할 것이라는 가정이며, 이를 직접 검증하는 실험을 통해 확인할 것입니다. 확장 축은 지역 커뮤니티와 인맥을 통해 서비스를 널리 알리는 것입니다. 고객 검증에서 얻은 피드백을 바탕으로 서비스를 개선하고, 이용자들이 지불하는 서비스의 가치를 확인하기 위해 유료화 검증을 진행할 계획입니다. 예를 들어, 기본적인 면접 질문 생성 기능은 무료로 제공하되, 고급 면접 질문 생성 알고리즘이나 이용자 데이터 분석 기능은 유료로 제공하는 방식입니다. 이를 통해 이용자들이 서비스의 유용성을 인정하고 지불할 것이라는 가정을 검증할 수 있을 것입니다. 또한, 초기 고객 확보를 위해 지역 커뮤니티와 인맥을 통해 서비스를 널리 알릴 계획입니다. 이를 통해 서비스의 가치를 확인하고, 이용자들의 지불 의향을 확인할 수 있을 것입니다. 저는 6개월 계획을 세우고, MVP 기획과 제작, 고객 검증을 통해 서비스를 사업화할 계획입니다. 제작은 웹 서비스 프로젝트 경험을 바탕으로 직접 진행할 계획이며, 고객 검증에서는 주변 열 명에게 서비스를 제공하고 피드백을 받아볼 계획입니다.

  **q4_2|A1** (1180자)

  > 저는 컴퓨터공학을 전공하고 웹 서비스 프로젝트에 2회 참여한 경험을 바탕으로 AI 면접 코칭 서비스를 개발하고자 합니다. 이 서비스는 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고 답변, 말투, 시선, 속도를 분석해 피드백하는 기능을 제공합니다. 현재 혼자서 객관적으로 판단하고 반복하기 어려운 불편을 해결하고자 합니다. 저는 현재 가장 단순한 형태로 서비스를 만들고 주변 열 명에게 테스트해 볼 계획입니다. 또한, 이용료를 통해 수익을 창출할 계획입니다. 그러나 아직 가격을 정하지 않았으며, 고객 검증과 가격 결정에 대한 도움을 받고자 합니다. 저는 컴퓨터공학 전공과 웹 서비스 프로젝트 경험을 바탕으로 기술적인 측면에서 강점을 가지고 있습니다. 그러나 고객 검증과 시장 진입에 대한 경험은 부족하여, 책임멘토의 도움을 받고자 합니다. 또한, 가격 결정에 대한 경험을 공유받아, 적정한 가격을 설정하고자 합니다. 저는 현재 직접 확인한 근거를 바탕으로 서비스를 개발하고자 합니다. 책임멘토의 도움을 통해 이러한 부분을 보완하고, 고객이 실제로 사용하고 지불하는 서비스를 개발하고자 합니다. 저는 책임멘토의 도움을 받아, 고객 검증과 가격 결정에 대한 계획을 수립하고자 합니다. 또한, 시장 진입 전략을 수립하고, 적정한 가격을 설정하여, 고객이 실제로 사용하고 지불하는 서비스를 제공하고자 합니다. 현재 AI 면접 코칭 서비스를 개발 중이며, 가장 단순한 형태로 만들고 주변 열 명에게 테스트해 볼 계획입니다. 먼저, 고객 검증을 위해 책임멘토의 경험을 활용하고자 합니다. 멘토의 도움을 통해 고객의 실제 nhu cầu와 기대치를 확인하고, 서비스를 개선할 수 있는 방법을 찾고자 합니다. 예를 들어, 고객의 면접 경험과 기대치를 조사하고, 서비스의 피드백 기능을 개선하는 데 도움이 될 수 있는 지식을 얻을 수 있을 것입니다. 책임멘토의 도움을 통해 시장 조건과 고객의 지불 의사를 고려한 가격을 결정할 수 있을 것입니다. 예를 들어, 멘토의 경험을 통해 고객이 지불할 수 있는 가격의 범위를 파악하고, 서비스의 가치를 고려한 가격을 설정할 수 있을 것입니다. 또한, 시장 진입 전략을 수립하고자 합니다. 책임멘토의 도움을 통해 고객에게 서비스를 알리고, 고객의 nhu cầu를 충족하는 방법을 찾을 수 있을 것입니다. 이를 통해 고객이 실제로 사용하고 지불하는 서비스를 개발하고자 합니다.

  **q8|A1** (804자)

  > 저는 컴퓨터공학 전공과 웹 서비스 프로젝트 경험을 바탕으로 이 아이디어를 실행할 수 있습니다. 가장 관련이 있는 저의 강점은 프로그래밍 언어와 웹 서비스 개발에 대한 지식입니다. 이를 통해 직접 면접 코칭 서비스를 제작할 수 있으며, 웹 서비스 프로젝트 경험을 통해 사용자 친화적인 인터페이스를 디자인할 수 있습니다. 예를 들어, 저는 이전에 웹 서비스 프로젝트에서 사용자 데이터를 분석하고 시각화하는 기능을 개발한 경험이 있습니다. 이러한 경험을 바탕으로 면접 코칭 서비스에서 사용자의 답변과 말투, 시선, 속도를 분석하고 피드백하는 기능을 구현할 수 있습니다. 저는 직접 겪은 경험으로 면접 준비의 어려움을 이해하고 있습니다. 혼자서 객관적으로 판단하고 반복하기 어렵다는 점을 알고, 이에 대한 해결책을 제공하고자 합니다. 이를 통해 사용자들이 더 나은 면접 준비를 할 수 있도록 도와줄 수 있습니다. 저는 MVP 기획 단계에서 가장 기본적인 면접 질문 생성과 피드백 기능을 갖춘 서비스를 만들 예정입니다. 웹 서비스 프로젝트 경험을 바탕으로 직접 제작할 계획이며, 주변 열 명에게 서비스를 제공하고 피드백을 받아볼 계획입니다. 이를 통해 초기에 서비스의 품질을 높이고 사용자들의 요구를 더 잘 알 수 있을 것입니다. 저는 아직 직접 확인한 근거가 없으므로, 고객 검증과 가격 결정에 대한 도움을 멘토로부터 받고자 합니다. 또한, 관련 분야의 전문가와 협력하여 서비스의 품질을 높이고 사용자들의 요구를 더 잘 알 수 있도록 하겠습니다. 이를 통해 강점을 핵심으로 하고 부족한 부분은 보완하여 고객이 실제로 쓰는 제품으로 만들겠습니다.

  **q8|A-영업** (1387자)

  > 저는 5년간의 영업직 회사원 경험을 바탕으로 이 아이디어를 실행할 수 있습니다. 가장 큰 강점은 고객과의 소통과 문제 해결 능력입니다. 이를 통해 고객의 니즈를 정확하게 이해하고, 이를 해결하는 서비스를 개발할 수 있습니다. 예를 들어, 이전에 근무했던 회사에서 고객의 불만을 처리하는 업무를 맡았을 때, 고객의 상황을 이해하고 적절한 해결책을 제시하여 고객의 만족도를 높일 수 있었습니다. 이러한 경험을 바탕으로, 저는 고객의 면접 준비 과정에서 겪는 어려움을 이해하고, 이를 해결하는 서비스를 개발할 수 있습니다. 저는 컴퓨터공학을 전공하였고, 웹 서비스 프로젝트 경험을 바탕으로 직접 서비스를 제작할 계획입니다. 이를 통해, 고객의 면접 준비 과정을 분석하고, 맞춤형 면접 질문과 피드백을 제공하는 서비스를 개발할 수 있습니다. 예를 들어, 이전에 진행했던 프로젝트에서 데이터 분석을 통해 고객의 행동 패턴을 이해하고, 이를 바탕으로 맞춤형 서비스를 제공하는 경험을 했습니다. 이러한 경험을 바탕으로, 저는 고객의 면접 준비 과정을 분석하고, 맞춤형 서비스를 제공할 수 있습니다. 저는 이 서비스를 개발하고 고객에게 제공하는 과정에서 직접 실행 가능한 부분부터 빠르게 검증할 계획입니다. 먼저, 가장 기본적인 면접 질문 생성과 피드백 기능을 갖춘 서비스를 만들고, 주변 열 명에게 서비스를 제공하고 피드백을 받아볼 계획입니다. 이를 통해, 서비스의 적합성과 고객의 만족도를 확인할 수 있을 것입니다. 저는 현재 고객 검증과 가격 결정에 대한 부분에서 멘토의 도움을 필요로 합니다. 이를 통해, 고객의 니즈를 더 정확하게 이해하고, 서비스의 가치를 고객에게 효과적으로 전달할 수 있을 것입니다. 저는 강점을 핵심으로 하여 부족한 부분은 보완해 고객이 실제로 쓰는 제품·서비스로 개발할 계획입니다. 이를 통해, 고객의 면접 준비 과정에서 겪는 어려움을 해결하고, 고객의 만족도를 높일 수 있을 것입니다. 저는 컴퓨터공학 전공과 웹 서비스 프로젝트 경험을 바탕으로 이 아이디어를 실행할 수 있습니다. 가장 큰 강점은 데이터 분석과 맞춤형 서비스 제공 능력입니다. 현재 저는 고객의 면접 준비 과정에서 겪는 어려움을 이해하고, 이를 해결하는 서비스를 개발하는 데 집중하고 있습니다. 예를 들어, 고객의 면접 준비 과정에서 가장 어려운 부분은 객관적인 판단과 반복적인 준비입니다. 저는 이러한 어려움을 해결하는 서비스를 개발하여 고객의 면접 준비 과정을 분석하고, 맞춤형 면접 질문과 피드백을 제공할 수 있습니다. 저는 고객의 면접 준비 과정에서 겪는 어려움을 해결하는 서비스를 개발하는 데 필요한 역량을 가지고 있습니다. 컴퓨터공학 전공과 웹 서비스 프로젝트 경험을 바탕으로, 고객의 면접 준비 과정을 분석하고, 맞춤형 면접 질문과 피드백을 제공하는 서비스를 개발할 수 있습니다.

  **q8|A-없음** (1032자)

  > 저는 이 아이디어를 실행할 수 있는 역량을 가지고 있습니다. AI 면접 코칭 서비스를 개발하고 운영하기 위해 필요한 기술과 경험을 바탕으로, 이 프로젝트를 성공적으로 추진할 수 있을 것입니다. 저의 가장 큰 강점은 AI와 면접 준비에 대한 이해입니다. 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고 답변을 분석하는 알고리즘을 개발할 수 있습니다. 또한, 웹 플랫폼 구축과 AI 모델 개발에 필요한 기술을 가지고 있습니다. 이러한 기술을 바탕으로, 저는 MVP를 기획하고 제작할 수 있습니다. MVP의 기본적인 면접 질문 생성과 피드백 기능을 구현하고, 주변 열 명에게 서비스를 제공하여 피드백을 받을 수 있습니다. 저는 이 아이디어의 필요성을 직접 겪었습니다. 혼자서는 객관적으로 판단하고 반복하기 어렵다는 점을 느꼈고, 주변 세 명에게 물어봄으로써 이 문제가 많은 사람들에게 공통되는 것임을 확인했습니다. 이러한 경험을 바탕으로, 저는 이 서비스의 목적과 필요성을 명확하게 이해하고 있습니다. 저는 보완할 영역으로, 고객 검증과 가격 결정에 대한 전문적인 지식을 필요로 합니다. 이를 위해, 책임멘토에게 도움을 받을 계획입니다. 또한, 마케팅과 고객 관리에 대한 지식도 필요로 하므로, 외부 전문가의 도움을 받을 수 있습니다. 저는 강점을 핵심으로 하여 부족한 부분을 보완함으로써, 고객이 실제로 사용하는 제품으로 만들 수 있을 것입니다. 이를 통해, 저는 이 아이디어를 성공적으로 사업화할 수 있을 것입니다. 저는 MVP를 기획하고 제작하는 단계에서 직접 실행 가능한 부분부터 빠르게 검증할 계획입니다. 이를 통해, 실제 고객의 반응과 요구를 확인하고, 서비스를 개선할 수 있습니다. 또한, AI 모델 개발과 웹 플랫폼 구축에 필요한 기술을 바탕으로, 서비스의 안정성과 성능을 검토하고, 개선할 수 있습니다. 이러한 직접 실행 가능한 부분부터 빠르게 검증함으로써, 저는 이 서비스의 잠재력을 확인하고, 부족한 부분을 보완할 수 있을 것입니다. 저는 이 아이디어의 성공을 위한 직접 실행 가능한 부분부터 빠르게 검증할 계획입니다.

  **q1|A** (91자)

  > 면접을 준비하는 사람들에게 이력서와 지원 직무를 기반으로 맞춤형 면접 질문을 생성하고, 답변과 말투, 시선, 속도를 분석해 피드백하는 AI 면접 코칭 서비스입니다.

  **q10|A** (52자)

  > 면접을 준비하는 사람에게는 AI 기반의 맞춤형 면접 질문 생성과 답변 분석을 제공해 드립니다.

  **q1|C** (59자)

  > 저는 소규모 딸기 농가의 불편을 덜어주는 저온 저장과 직거래 택배 포장을 지원하는 센터를 만들 계획입니다.

  **q10|C** (49자)

  > 혼자 딸기 수확과 포장을 하시지 마세요. 저온 보관부터 택배 발송까지 제가 도와드립니다.
