# 작업 상태판 (WORKBOARD) — 2026-09-02

> 실시간 조율 문서. 규칙은 `docs/TEAMWORK.md`.
> 각 세션은 **자기 로그 절에만** 쓴다. 배정표·조율 필요 절은 세션1이 관리한다.

## 배정표

| # | 작업 | 담당 | 상태 | 근거 |
|---|---|---|---|---|
| 1 | **V1 — 프론트를 작업 큐 API 로 전환** | 세션2 | ✅ 완료 (`b4839a1`+`dae680c`, 08:57 배포) | PROGRESS "다음 순서" 1순위. V5·V6·V9 가 같이 풀림 |
| 2 | **V4 — 사용자별 동시 작업 제한** | 세션3 | ✅ 완료 (`1bb3ad0`) | 한 명이 27건으로 큐 독점 가능 |
| 3 | 모델 결정 (라마 이물 82%·출처 날조) | **사용자** | 결정 대기 | PROGRESS 2절 — 선택지 3개 |
| 4 | V7 — SSH 터널 NSSM 서비스화 | 보류 | 승인 대기 | 관리자 권한·키 인증 확인 필요 (PROJECT_CONTEXT 12절에 절차 있음) |
| 5 | ~~조사 재사용~~ → **작업 12 의 2단계로 흡수** | — | 통합 | |
| 6 | 모델 표기 → "백석대학교 로컬 LLM" | 세션3+세션2 | ✅ 완료 (공개 `/api/models` 확인) | 교수님 요청 (9/2). 작음 — 각자 자기 파일 1~3줄 |
| 7 | ~~근거 소스 다양화~~ → **작업 12 의 3단계로 흡수** | — | 통합 | |
| 8 | ~~검색 백엔드 추가~~ → **작업 12 의 3단계로 흡수** | — | 통합 | |
| 9 | LlamaIndex RAG — 조사 자료 벡터DB 축적·재사용 | **세션2**(모듈+테스트) → 세션3(파이프라인 연결) | 모듈 ✅ (`7b2a6e5`) / 색인 연결 ✅ (`12d44a5`, 기본 꺼짐) / **조회 단계 대기** — min_score 0.8→0.6 결정(세션1) | 교수님 요청 (9/2). PROGRESS 백로그 "공공데이터 색인" 확장. 설계: `docs/RAG_PLAN.md` |
| 10 | GPU 한계 측정 → 워커 수 상향 | 세션3 + 사용자 | ✅ 완료 — 09:3x 재시작으로 운영 반영 (`/api/health` `max_workers: 30`) | 교수님 요청 (9/2). 어제 부하테스트에서 병목은 vLLM 이 아니라 **우리 워커 12개**였음 |
| 11 | 로컬 트랙·스토리/간결 스타일 UI 제거 | 세션2 | ✅ 완료 (배포 번들 확인) | 사용자 결정 (9/2). 일반/기술 + 논리·근거형만. **프론트만, 백엔드 md 는 유지** |
| 12 | **자료조사 파이프라인 개편 (0~5단계)** | 세션3 | 0~3단계 ✅ — 정형 API 4종(KOSIS·ECOS·K-Startup·상권) + 도메인 상한 + 네이버 정렬 (`c747955`, 실호출 각 1건). 남은 것: 4단계 캐시 개편·5단계 진행 표시. **운영 반영은 재시작 대기** | 사용자 지시서 (9/2) — `docs/RESEARCH_PLAN.md`. 수치 검증 완료. 구 5·7·8 흡수 |
| 13 | **GPU 상한 실측 확대 → 워커 최대치 반영** | 세션3 + 사용자 | ✅ 완료 — 11:0x 재시작으로 운영 반영 (`max_workers: 36`) | 한계는 메모리(KV 45%@100)가 아니라 처리량. 워커를 더 올려도 총 처리량은 안 늘고 문항당 시간만 늘어남 |
| 14 | 프론트 UX 개선 A2~A5·B1~B4 (이어받기 후 자동 생성, 대기 카드, 공통조사 안내, 저장 용량, 모델 표기 등) | 세션2 | ✅ 커밋 (`c1dd0e4`), 세션1 dist-check 빌드 통과. dev :5173 **사용자 4항목 전부 통과(11:2x)** → 배포 진행 | 세션2 로그 325행에 항목별 근거. A1(조사·인테이크 큐 전환)은 세션3 `main.py:257` 정리 뒤 |
| 15 | **Q1 문체 획일화** — "저는 [대상]에게 [서비스]를 제공하는 앱입니다" 틀로만 나옴 | 세션3 | 대기 (사용자 지적 11:3x) | 원인은 `prompts/questions/q1.md:21` 의 고정 어미 지시 + "세 요소를 한 문장에" 규칙. 상세는 아래 |
| 16 | **프롬프트를 창업교육 MVP 8단계 프로세스에 맞춰 개선** | 세션3 | 대기 (사용자 지시 11:4x). 작업 15 뒤 | 기준·매핑·검증은 **`docs/MVP_PROCESS.md`** 가 단일 기준. 백업: 태그 `prompts-before-mvp-2026-09-02` + `docs/prompt_backups/2026-09-02/` |
| 17 | **KCI 학술 어댑터 + 출처 표기** | 세션3(어댑터) + 세션2(UI 문구) | 대기 (키 발급 11:5x) | RESEARCH_PLAN 의 KCI 준수 6항이 완료 조건. 세션3: `no_fetch`·벡터DB 제외·논문 인용 형식. 세션2: 푸터+근거 패널에 "KCI(한국학술지인용색인) 데이터 활용" |

### 작업 15 상세 — Q1 문체 다양화 (세션3, 프롬프트만)

- 증상(사용자, 실서비스): Q1 이 아이디어가 달라도 항상 "저는 20-30대 취업자와 가족 단위 캠핑객에게 캠핑장 빈자리 실시간 알림과
  당일 취소분 할인 예약 서비스를 제공하는 앱입니다." 꼴. 사람이 쓴 것처럼 문장 구조가 바뀌어야 함.
- 원인 (세션1 진단): ① `q1.md:21` "문장 끝은 '~하는 앱입니다', '~해 주는 서비스입니다'처럼 명사로 맺습니다" — 어미를 예시로 고정.
  ② `q1.md` "[필수 요소] 세 가지가 반드시 한 문장 안에" — 대상→가치→형태 순서로 채우는 틀이 됨.
  ③ `short_question.md:4` 가 "'저는 ~입니다' 로 시작하지 않는다" 인데 출력이 "저는" 으로 시작 — 라마가 이 규칙을 무시함 (①②가 더 강하게 작용).
  temperature 0.7 이라 샘플링 문제는 아님.
- 방향: `q1.md` 만 고친다. 어미 예시(①)를 지우고 **문장 구조 3~4가지를 제시해 아이디어에 맞는 것을 고르게** —
  예) 문제 먼저형("~때문에 곤란한 A 를 위해 …"), 장면형("주말 캠핑장 앞에서 발길을 돌리는 …"), 대비형("기존 X 대신 Y 로 …"), 결과형("… 을 30초 안에 끝내 주는 …").
  "저는 ~" 시작 금지와 "~에게 ~를 제공하는 앱입니다" 단일 패턴 금지를 q1.md 에 **직접** 명시 (short_question 에만 있으면 안 먹힘).
  필수 요소 3개는 유지하되 "순서는 자유" 를 붙인다.
- 검증: 같은 아이디어로 3회, 다른 아이디어 2개로 각 1회 실호출(라마, 한도 무관) → 5개 문장의 **첫 어절과 어미가 서로 다르면** 통과. 결과 문장 5개를 로그에 그대로 적기.
- `tests/test_prompts.py` 통과. 다른 문항 md 는 건드리지 않는다.

### 작업 9 상세 — RAG 벡터DB (세션2 모듈 → 세션3 연결)

- 설계·결정·**모듈 인터페이스는 `docs/RAG_PLAN.md` 5·5-1절이 기준** (서명 변경은 세션1 승인 후).
- **소유권 예외 (세션1 승인)**: 세션2 가 `backend/rag/vectorstore.py`, `tests/test_vectorstore.py` 를 새로 만들고,
  `backend/requirements.txt` 에 llama-index 2줄만 추가한다. 그 외 `backend/**` 는 여전히 세션3 구역 — `pipeline.py` 는 건드리지 않는다.
- 세션2 완료 조건: fake embedding 으로 upsert(중복 url 재색인 없음)·query(score 붙음)·신선도 컷·is_sufficient 가 mock 테스트로 통과.
  ✅ 10:3x `bge-m3` 로컬 Ollama 설치 완료(세션1) — 세션2 는 mock 통과 후 실호출 1건으로 실제 임베딩 차원·persist 확인 가능.
- 세션3 연결(1단계 색인만 → 2단계 조회)은 세션2 커밋 뒤, 작업 12 3단계 어댑터 다음에.

### 작업 13 상세 — GPU 상한 실측 확대 (세션3 + 사용자)

- 목적: "맥스로 하면 몇 개까지 병렬로 처리되나, 그때 GPU 를 얼마나 먹나" — 작업 10 의 45 는 한계가 아니었다
  (KV 캐시 7%, waiting 0, preemption 0). 한계가 보일 때까지 올려서 잰다.
- 방법 (`scripts/gpu_probe.py`, 코드 수정은 필요한 만큼만):
  1. `--levels 60,80,100,150` — **실사용 길이 프롬프트**로 (참고자료 facts 8건 붙인 문항 생성 프롬프트, `max_tokens 1200`).
     작업 10 은 짧은 프롬프트라 KV 를 실제보다 적게 썼다.
  2. 단계마다 vLLM `/metrics`: `num_requests_running`·`num_requests_waiting`·`kv_cache_usage_perc`·preemption 을 1초 샘플링.
     **`running` 이 동시성보다 작게 머무는 지점 = vLLM `--max-num-seqs` 상한** → 그 위는 GPU 서버 설정을 바꿔야 함(리포 밖, 사용자).
  3. 사용자: 같은 시간에 PuTTY 로 GPU 서버에서 `nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.total --format=csv -l 1`
     → 단계별 최대치를 세션3에 전달 (GPU 사용률·메모리).
  4. 판정: KV 캐시 90% 근접 / p95 가 직전 단계보다 1.5배 이상 꺾임 / preemption>0 / 실패 발생 — 넷 중 하나가 걸리는
     **직전 단계가 안전 상한**. 본문:조사 = 4:1 로 나눠 `max_workers`·`research.llm.max_workers` 반영 → 재시작(사용자).
- 결과 표는 작업 10 형식 그대로 세션3 로그에. 요약(안전 상한·GPU 사용률)은 세션1이 PROGRESS 로 이관.
- ✅ 10:4x **nvidia-smi 는 세션1이 SSH(`ssh gpu`, 키 인증)로 직접 샘플링한다** — 사용자 PuTTY 불필요. 세션3 은 측정 시작·각 단계 시각만 로그에 남기면 세션1이 맞춰 정리.
  유휴 상태 실측: RTX PRO 6000 Blackwell 96GB × 3장. GPU1 90.2GB 사용(=vLLM 라마 70B AWQ 추정), GPU0 14.7GB(다른 사용처), GPU2 유휴. 사용률 0%.
  → vLLM 은 **한 장**에서 도는 것으로 보임. KV 캐시 여유 = 96GB − 모델 가중치(~40GB) 근처라 작업 10 의 KV 7%@45 와 부합.
- ⚠️ 공유 GPU 라 실행 시점은 사용자 승인. 라마 실호출 ~400회(측정용, 한도 무관).

### 작업 12 상세 — 자료조사 개편 (세션3)

- 전체 설계·API 한도·완료 조건은 **`docs/RESEARCH_PLAN.md`** 가 단일 기준.
- **1단계(두 줄)는 키 없이 지금 바로 가능**: `pipeline.py:141` `* 2` 제거 + `research.py:179`
  `d.news()` 제거(뉴스는 3단계에서 네이버 API 로 이관). 500명 기준 10만 → 4만 요청.
- 0단계(검색어 중복도 분석)는 라마 실호출 필요 — 한도 부담 없음, 결과는 RESEARCH_PLAN 에 기록.
- 3단계 어댑터+mock 테스트는 키 발급 전에 선작업 가능. 실키 연결만 키 이후.
- 5단계의 프론트 진행 표시는 세션2 V1(큐 전환)과 맞물림 — 인터페이스 바뀌면 여기 공지.
- **세션3 진행 순서 (10:2x 사용자 "진행" 지시)**: ① 작업 13 GPU 실측(사용자 nvidia-smi 동행, 짧음) → ② 네이버 HUB 규격 수정(🔴)
  → ③ 정형 API 어댑터 KOSIS·ECOS·data.go.kr(상권정보·K-Startup) + 도메인별 상한 → ④ 작업 9 파이프라인 연결.
  목표는 RESEARCH_PLAN 완료 조건 그대로: 1인 시나리오 외부 요청 40건 이하, **ddgs 5건 이하**.
- 🔴 **3단계 정정 (09:5x, 세션1 웹 검증)** — 네이버 검색 API 가 **NAVER API HUB 로 이관**됐다(2026-06-25).
  `research.py` `NaverSearch`(`23ad8de`) 는 구 규격이라 실키 연결 전에 고쳐야 한다. 정확한 값은 RESEARCH_PLAN "API 한도" 절:
  ① `base_url` → `https://naverapihub.apigw.ntruss.com/search/v1`, 경로 `/{kind}` (`.json` 없음)
  ② 헤더 → `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` (env 이름 `NAVER_CLIENT_ID/SECRET` 은 그대로)
  ③ `NAVER_ENDPOINTS` 에서 `doc`(전문자료) 제거 — 2026-07-31 종료. 학술은 KCI → OpenAlex/Semantic Scholar 로 (KCI 키 `KCI_API_KEY`)
  ④ 오류 본문 2형태(`errorCode` 평면 / `error.errorCode` 중첩) 모두 파싱, 429 는 서킷브레이커 대상
  ⑤ 한도 상수·주석 25,000/일 → 월 775,000·50 RPS 로. 기존 mock 테스트는 새 규격으로 갱신.

### 작업 10 상세 — GPU 한계 측정과 워커 상향 (세션3 + 사용자)

**현황 근거** (어제 측정, PROGRESS·BOTTLENECKS):
- 백엔드 큐 `max_workers: 12` (본문) + 4 (조사) — 부하테스트 내내 `running=12` 고정, 대기 최대 90건.
  **대기(2분49초)가 실행(24초)의 7배 — 병목은 GPU 가 아니라 우리 쪽 워커 수.**
- vLLM 직접 45 동시 호출은 45/45 성공, 73초 — GPU 쪽에 여유가 있다는 뜻.

**측정 (코드 수정 없음, 세션3):**
1. vLLM `/metrics` 를 터널(30800) 경유로 조회 — `vllm:num_requests_running`,
   `vllm:num_requests_waiting`, KV 캐시 사용률이 핵심 — **이 vLLM 의 실제 지표명은 `kv_cache_usage_perc`** (9/2 스모크로 확인).
   SSH 필요 없음 — 백엔드가 쓰는 같은 포트다.
2. `scripts/load_test.py` 로 동시성을 **단계적으로** 12 → 20 → 30 → 45 올리며
   문항당 p50/p95 실행 시간과 `gpu_cache_usage_perc` 를 기록.
   **KV 캐시가 90% 근처로 가거나 p95 가 꺾이는 지점의 한 단계 아래가 안전 상한.**
3. `nvidia-smi` (GPU 사용률·메모리)는 사용자가 PuTTY 로 원격 서버에서 확인해 값만 전달.

**적용:** `backend/config.yaml` `max_workers` 를 측정된 안전 상한으로 (한 줄).
되돌리기도 한 줄이라 위험 낮음. 조사 워커(4)도 같은 방식으로.

**⚠️ 조율:** 부하 테스트는 공유 GPU 서버에 실부하를 건다 — **실행 시점은 사용자 승인 후**
(한가한 시간대 권장). GPU 서버 자체 설정(vLLM 재기동, `--max-num-seqs` 등)은 이 리포 밖 —
필요해지면 사용자가 직접.

### 작업 11 상세 — 로컬 트랙·스토리/간결 스타일 UI 제거 (세션2)

- 결정(사용자, 9/2): **일반/기술 트랙만, 논리·근거형 스타일만** 쓴다.
- 방법: **프론트에서만 제거** — `App.jsx` 의 `TRACKS`(6행)에서 local 항목,
  `STYLES`(23행)에서 story·plain 제거(또는 선택 UI 숨기고 logic 고정).
  트랙 선택 화면(509행)과 스타일 선택(597행)이 항목 1개가 되면 화면 자체를 접는 것도 고려.
- **백엔드 프롬프트(`tracks/local.md`, `styles/story.md`·`plain.md`)와 API 는 그대로 둔다** —
  테스트가 파일 목록을 검사하고, 되살릴 때 버튼 복원만으로 끝나게.
- 효과: 문항당 생성 호출이 1회로 고정 → 부하 계산 단순화 (V8 완화).

### 작업 6 상세 — 모델 표기 (세션3 + 세션2)

- 세션3: `backend/config.yaml:36` `name: Llama 3.3 70B (사내 GPU)` → `백석대학교 로컬 LLM`
  (/models 응답으로 프론트 선택 목록에 내려감. id 는 그대로 두기 — 실제 모델 지정에 쓰임)
- 세션2: `frontend/src/App.jsx` 의 사용자 노출 문구 3곳 — 156행("라마 70B"), 592행 등
  "로컬 라마 70B" → "백석대학교 로컬 LLM" 으로. 주석(45·152·231행)은 그대로 둬도 됨.
- 공개 반영은 dist 재빌드 필요 → 배포 시점은 "조율 필요" 참조.

### 작업 7 상세 — 근거 소스 다양화 (세션3)

- 증상: 어떤 때는 근거자료가 같은 신문사·같은 글에 편중됨.
- 방향: `backend/rag/pipeline.py` `collect_pages`/`extract_facts` 에서
  ① 페이지 수집 시 **도메인별 상한** (예: 같은 도메인 최대 2건)
  ② facts 선별 시 같은 url·publisher 편중이면 다른 소스 우선
  ③ 검색어 생성 프롬프트(`prompts/research/queries.md`)에 "뉴스·통계·보고서를 섞어라" 지시 검토.
- 검증: mock 테스트로 도메인 상한 동작 확인 (`tests/test_research.py` 에 추가).

### 작업 8 상세 — 검색 백엔드 추가 (세션3, API 키 나온 뒤)

- 현재: ddgs(덕덕고) 단독 — 무료·비공식이라 수백 명 규모에서 봇 판정·차단됨 (BOTTLENECKS 1층).
- 방향: `backend/rag/research.py` 의 `Researcher(backends=[...])` 가 이미 플러그인 구조.
  ① **네이버 뉴스/블로그 검색 API** — 공식 키, 일 25,000회 무료. 한국어 뉴스 품질도 ddgs 보다 좋음
  ② **공공데이터포털(data.go.kr)·KOSIS API** — 통계 근거용, 차단 걱정 없음
  ③ 순서: naver → ddgs(폴백) 로 두면 ddgs 의존이 자연히 줄어듦. 키는 `.env` 로 (커밋 금지).
- 키 없이도 어댑터+mock 테스트까지는 선작업 가능. 실키 연결만 키 발급 후.
- 작업 7(다양화)과 같은 파일을 만지므로 **세션3이 이어서 한 묶음으로** 처리 권장.

### 작업 1 상세 — V1 (세션2)

- 현재: `frontend/src/api.js` 의 `generate()`/`extend()` 가 동기 `POST /generate` 를 부르고 3분간 연결을 붙잡음.
- 목표: `POST /jobs/generate` → 응답의 job id 로 `GET /jobs/{job_id}` 폴링(2~3초 간격) 전환.
  - 큐가 주는 `position`(대기 순번)을 화면에 표시 (V5)
  - job id 를 `sessionStorage` 에 보관해 새로고침해도 이어받기 (V6)
- 건드릴 파일: `frontend/src/api.js`, `frontend/src/App.jsx`. 백엔드는 완성돼 있음 — **수정 금지**.
  응답 스키마는 `backend/main.py:261~284`, `backend/llm/jobs.py` 를 읽고 맞추기.
- **같이 고칠 버그 (2026-09-02 발견)**: 조사 결과 메모(`researchRef`, `App.jsx:305 ensureResearch`)가
  문항 id 로만 키가 잡혀 있고 localStorage 에서도 복원돼서, **아이디어를 바꿔도 옛 아이디어의
  조사 결과를 그대로 재사용**한다 (조사가 아예 생략되거나 엉뚱한 참고자료가 끼어듦).
  실측: 9/2 아침 생성 34건 동안 `/research` 호출 0회. 아이디어(또는 트랙)가 바뀌면
  `researchRef`·`researchState` 를 비우도록 수정. 참고: 조사 실패 시 `[]` 를 넣는데 JS 에서
  빈 배열은 truthy 라 재시도도 영영 안 된다 — 의도인지 확인하고 주석 남기기.
- 검증: `npm run dev` (:5173) 로 로컬 확인 → `npx vite build` 통과.
- ⚠️ **`frontend/dist` 재빌드는 즉시 공개 배포다** (nginx 가 dist 를 그대로 서빙).
  개발 중엔 dev 서버로만 확인하고, dist 빌드·반영은 사용자 확인 후.

### 작업 2 상세 — V4 (세션3)

- 목표: 클라이언트당 동시 작업 수 제한 (인증이 없으므로 우선 IP 기준, 예: 동시 3건).
  초과 시 429 + 안내 메시지.
- 건드릴 파일: `backend/llm/jobs.py`, `backend/main.py`, `tests/test_jobs.py`(테스트 추가).
- 제약: `POST /jobs/{kind}` / `GET /jobs/{job_id}` / `GET /jobs` **응답 필드 삭제·개명 금지** (세션2가 의존).
  필드 추가는 허용 — 추가하면 아래 "조율 필요"에 공지.
- 검증: `.venv/Scripts/python -m pytest -q` 전체 통과 (현재 87 passed). 실모델 호출 없이 mock 으로.
- 참고: 개발용 백엔드가 필요하면 **:8001** 로 (`uvicorn backend.main:app --port 8001`). :8000 은 운영 중.

## 조율 필요 / 결정 대기

- ⚠️ **프롬프트 md 는 저장 즉시 운영 반영** (요청마다 다시 읽음, 서비스가 이 폴더에서 돔) — 프롬프트 작업(15·16)은 **git worktree** 에서 하고 검증 후 merge. 절차 `docs/MVP_PROCESS.md` 4절. 원복: `git checkout prompts-before-mvp-2026-09-02 -- backend/prompts`
- [ ] **모델 결정** (사용자): ① OpenRouter $10 충전 ② 라마+후처리 ③ 하이브리드 — PROGRESS.md 2절 참고
- [ ] **SSH 터널 서비스화** (사용자 승인): nssm install 은 관리자 권한 필요, 키 인증 전제
- [x] dist 재빌드(=배포) 시점 — 08:57 사용자 승인으로 배포 완료 (`index-CG-vzjlN.js`).
  ⚠️ 09-02 08:18 세션2가 문법 검증용 `npx vite build` 로 미검증 작업본을 오배포 → 직전 커밋 빌드로 복구 진행.
  재발 방지: 문법 검증은 `--outDir dist-check` 로 (CLAUDE.md·TEAMWORK 규칙화 완료)
- [~] **API 키 발급** (사용자만 가능, 작업 12 3단계 전제 — 상세 한도는 RESEARCH_PLAN 참조):
  - ✅ **네이버 (API HUB) 09:4x 발급 완료** — `.env` 에 `NAVER_CLIENT_ID/SECRET` 들어감. 세션1이 파이썬으로 실호출 1건
    (`/search/v1/news` query=창업) → 200, UTF-8 한글 정상, total 478만. **세션3: 어댑터를 HUB 규격으로 고친 뒤 실키 검증 1~2건 가능.**
    운영 `mochang-api` 는 아직 재시작 전(구 어댑터라 재시작해도 네이버 호출은 실패함 → 어댑터 수정 후 재시작).
  - ✅ 10:0x `.env` 현황(값 확인 안 함, 이름만): `DATA_GO_KR_API_KEY`·`KOSIS_API_KEY`·`ECOS_API_KEY` 들어감.
    ~~`KCI_API_KEY` 승인 대기~~ → **11:5x KCI 발급 완료** (사용자가 `.env` 에 넣음). 이용 준수 사항 6개를 RESEARCH_PLAN "API 한도" 절에 코드 규칙으로 명시 —
    메타데이터만·벡터DB 제외·UI 출처 표기 필수. `KIPRIS_API_KEY` 미신청. data.go.kr 는 상권정보·K-Startup 활용신청 기준.
  - 네이버 → **NAVER API HUB** (ncloud.com 계정 → 콘솔 > Application Services > NAVER API HUB > Application 등록 > 검색 API).
    developers.naver.com 은 신규 신청 종료. 뉴스·블로그·웹문서 공용, 전문자료는 종료됨. 콘솔에서 한도 알림 설정
  - KCI OpenAPI (kci.go.kr) — 학술 근거용 키. OpenAlex·Semantic Scholar 는 키 불필요
  - 공공데이터포털(data.go.kr) → 활용신청 → 인증키 (상권정보·K-Startup 포함. **오픈 전 운영계정 전환 필수**)
  - KOSIS(kosis.kr), 한국은행 ECOS(ecos.bok.or.kr), KIPRIS(특허), 빅카인즈 — 각 자체 발급
  - 발급되면 `.env` 에 넣고 (커밋 금지) 세션3에 알리기. 키 복수 발급 우회 금지

## 세션 로그

> 형식: `- HH:MM 시작|진행|완료|막힘 — 내용 (커밋 해시)`

### 세션1 (조율)

- 2026-09-02 세션 시작. TEAMWORK.md·WORKBOARD.md 생성, 작업 1·2 배정.
- 세션3 커밋 4개(`1bb3ad0`~`23ad8de`) push 완료. `.env.example` 에 NAVER 2줄 추가(세션3 요청).
- 08:18 dist 오배포 사고 조율 — 직전 커밋 빌드로 복구 결정(사용자 승인), 문법 검증을 `dist-check` 로 규칙화 (CLAUDE.md·TEAMWORK 반영).
- 세션3의 0단계 실측 표는 세션3 로그가 원본 — RESEARCH_PLAN 이관은 2단계 설계 확정 때 함께.
- 09:2x 세션 비정상 종료 → 재시작. 파일 기준으로 복구, 유실 작업 없음.
- 09:3x **실서비스 검증 + 워커 반영 확인** (공개 URL `/api/health`·`/api/jobs`·`/api/models`).
  08:57 재시작이 `d7d34a9`(08:59) 보다 2분 먼저라 워커가 12 로 남아 있던 것 발견 → 큐 비어 있음(running 0) 확인 후
  사용자가 `nssm restart mochang-api` → `max_workers: 30`, `max_per_client: 3` 확인. 작업 1·6·10·11 배정표 ✅ 갱신.
  남은 브라우저 확인 1건(생성 중 F5 → 이어받기)은 사용자 몫.
- 세션3 📌 (`/research`=research_client, `/jobs/research`=client 불일치) 판단: **큐 경로도 `research_client` 로 통일**
  (`main.py:257` 한 줄). 지금은 같은 라마라 결과 동일 — 세션3 다음 작업 때 같이 처리하면 됨.
- 세션2 다음 배정: **프론트 UX 개선 후보 목록** (구현은 승인 후, 배포 금지).
- 09:5x 사용자 제보(네이버 API HUB 이관·전문자료 종료) 웹 검증 → 사실. RESEARCH_PLAN·.env.example 정정, 세션3에 `NaverSearch` 규격 수정 지시(작업 12 상세 🔴).
- 10:1x 사용자 지시 2건 접수: ① GPU 상한 실측 확대 → **작업 13** 등록(세션3). ② RAG 벡터DB(작업 9)·다양한 소스(12-3단계) 진행 확인 →
  둘 다 미구현. 작업 9 는 사용자 결정 3개(임베딩 bge-m3 / 임계 0.8·5건 / 신선도 2년) 대기, 결정되면 세션2에 `backend/rag/vectorstore.py` 독립 구현 배정 제안.
- 10:2x 사용자 "진행" — 작업 9 결정 3개 추천안 확정, RAG_PLAN 5-1절에 모듈 인터페이스 고정. 작업 9 세션2 배정(소유권 예외), 세션3 순서 ①13→②네이버→③정형API→④RAG 연결.
- 10:5x 세션2·세션3 결과 검수 — pytest **124 passed** 로컬 재확인, 커밋 전부 push 됨.
  작업 9 모듈(세션2) ✅, 작업 13 측정(세션3) ✅ 36+9, 네이버 HUB 어댑터+실키 검증(세션3) ✅.
  ⚠️ nvidia-smi 샘플링은 **못 했음** — 세션3 측정이 제게 알림 없이 먼저 끝남. KV 45%@100 이라 상한 판단엔 영향 없음.
  필요하면 60 단계 1회만 재측정하며 GPU 사용률 확인 가능(라마 60회). 운영 반영은 재시작 1회(36/9 + 네이버 검색 활성화).
- 11:0x **재시작(사용자) → 운영 검증**: `/api/health` `max_workers: 36`. 공개 URL 로 조사 1건(새 아이디어, 라마 실호출 1건):
  60초, `backend: naver`, 검색어 4개(공통 조사만, 보완 0), 페이지 6건 전부 네이버 뉴스(아이뉴스24·뉴스핌·머니투데이·노컷·메트로 등 6개 매체),
  facts 4건 — **ddgs 0건, 소스 편중 해소**. 관찰(세션3 참고): 같은 기사(100세 이상 고령자 8,604명)가 3개 매체로 3건 중복 —
  도메인 상한으로는 못 거르니 3단계에서 **제목 유사도 중복 제거**도 같이 검토.
- 11:3x 세션3 커밋 2개(`c747955`·`12d44a5`) 검수 — pytest **152 passed** 재확인, push 완료. 작업 12 3단계·작업 9 색인 연결 ✅.
  세션3 제안(min_score 0.8 → 0.5~0.6) 수용 → **0.6** 으로 결정(RAG_PLAN 5절 갱신). 색인은 지금부터 쌓이도록 `enabled: true` 로 켜기로.
- 11:3x 사용자 지적 "Q1 문장이 늘 같은 틀" → 원인 진단(`q1.md:21` 고정 어미) → **작업 15** 세션3 배정.
- 11:4x 사용자 지시 "프롬프트 백업 + 창업교육 MVP 8단계에 맞춰 개선" → 백업(태그+사본), `docs/MVP_PROCESS.md`(매핑·개선 기준·검증) 작성, **작업 16** 세션3 배정.
  발견: 프롬프트가 요청마다 디스크에서 읽혀 **편집 즉시 운영 반영** → 프롬프트 작업은 worktree 규칙 추가.
- 11:5x KCI 키 발급 → 이용 준수 사항을 RESEARCH_PLAN 에 코드 규칙 6항으로 번역, **작업 17** 배정(세션3 어댑터·세션2 표기).

### 세션2 (프론트)

- 시작 — 작업 1(V1 큐 전환 + researchRef 버그), 작업 11(로컬 트랙·스토리/간결 제거), 작업 6 프론트 몫.
  건드릴 파일: `frontend/src/api.js`, `frontend/src/App.jsx`. dist 는 건드리지 않음(사용자 확인 후).
- 08:18 사고·복구 — 문법 검증으로 `npx vite build` 를 outDir 없이 돌려 **`frontend/dist` 가 재빌드되며
  작업본이 그대로 공개**됐다. 08:26 직전 커밋(HEAD) 소스를 스크래치패드에서 빌드해 dist 를 원상 복구
  (`index-CS-PX4XH.js` 로 확인, 공개 사이트는 이전 버전으로 돌아옴). 이후 검증은 `--outDir dist-check` 로만.
  `frontend/.gitignore` 에 `dist-check` 추가. (규칙은 세션1이 TEAMWORK 4절에 반영함)
- 완료 — **작업 1(V1) + 작업 11 + 작업 6 프론트 몫** (`b4839a1`). `vite build` 통과, dist 미변경.
  - `api.js`: `runJob(kind, body, {onSubmit,onTick})` = 제출 → 2.5초 폴링, `followJob(jobId)` = 이어받기,
    `JobExpiredError`(404). `generate()`/`extend()` 가 이걸 쓴다. 제출 429 는 5초 뒤 재시도(최대 12회),
    폴링 중 네트워크 오류는 연속 3회까지 무시.
  - `App.jsx`: 대기 순번 표시(`JobProgress`), job id 를 `sessionStorage`(`modoo-writer-jobs-v1`)에 보관 →
    마운트 시 남은 작업을 이어받음. 조사 결과에 `(트랙|아이디어)` 서명을 붙여 아이디어가 바뀌면 폐기.
    조사 실패 문항은 ref 에 안 남겨 다음 생성에서 재시도.
  - 실호출 1회(라마, 무료 한도 아님): `POST /jobs/generate` → 폴링 → done 계약 확인.
    응답 필드가 프론트 기대와 일치(`job_id/position/status/queued_seconds/elapsed_seconds/result.text/model`),
    없는 job 은 404. **운영 :8000 백엔드는 아직 구코드** — `GET /jobs` 에 `max_per_client` 가 없음(=V4 미반영).

#### 사용자 검증 체크리스트 (dev 서버 :5173 띄워둠, 백엔드는 운영 :8000)

`http://localhost:5173` 에서 확인. 처음 열면 예전 저장본은 자동 정리된다(로컬 트랙·옛 스타일 제거).

1. **대기 순번** — 아이디어 입력 → "신청서 초안 만들기". 문항 카드 안에
   `작업을 대기열에 넣는 중… → 대기 중 — 다음 차례입니다 → 작성 중… (보통 30~60초)` 순서로 뜨는지.
   ⚠️ 혼자 쓰면 큐가 비어 있어 **"앞에 N건"은 안 뜨는 게 정상**이다(운영 워커 12개, 브라우저는 2건씩만 제출).
   숫자를 직접 보려면 dev 백엔드에서 `max_workers` 를 1~2로 낮춰야 하는데 `config.yaml` 은 세션3 파일 → 조율 필요.
2. **새로고침 이어받기** — "작성 중…" 이 떠 있을 때 F5. 그 문항이 다시 "작성 중…"으로 살아나고
   잠시 뒤 글이 채워지면 성공. (개발자도구 Application → Session Storage 에 `modoo-writer-jobs-v1` 확인,
   작업이 끝나면 키가 사라진다.) 서버가 그 사이 재시작됐다면 "이어받지 못했어요 (서버에서 만료됨)" 안내가 정상.
3. **조사 재사용 버그** — 초안이 다 나온 뒤 "입력 수정"에서 **아이디어를 다른 내용으로 바꾸고** 다시 생성.
   문항마다 `웹에서 근거 자료를 조사하는 중… (백석대학교 로컬 LLM)` 이 다시 뜨고, 조사 근거 패널의
   **검색어가 새 아이디어 기준으로 바뀌면** 수정된 것. (네트워크 탭에 `/research` 호출이 다시 보임.
   고치기 전에는 호출이 0회였다.) 아이디어를 그대로 두고 다시 생성하면 조사는 건너뛰는 게 정상.
4. **429 안내** — 생성이 도는 중에 다른 문항의 "새로 생성"을 3~4개 연달아 누르면 4건째부터
   `먼저 넣은 작업이 끝나기를 기다리는 중… (한 번에 3건까지)` 이 뜨고, 앞 작업이 끝나면 저절로 진행돼야 한다.
   ⚠️ **운영 :8000 은 아직 V4 구코드라 429 가 안 난다.** 확인하려면 dev 백엔드가 필요하다(PowerShell, 한 줄씩):
   ```
   .\.venv\Scripts\python -m uvicorn backend.main:app --port 8001
   ```
   ```
   "VITE_API_BASE=http://localhost:8001" | Out-File -Encoding utf8 frontend\.env
   ```
   그 뒤 dev 서버 재시작(:5173). 확인이 끝나면 `frontend\.env` 를 지워 운영 :8000 으로 되돌린다.

- **사용자 브라우저 검증 결과 (dev :5173)** — ②③④ 통과, ⑤(429)는 운영 백엔드 구코드라 미확인.
  - ② 새로고침 이어받기: F5 후 다시 "작성 중"으로 살아나 결과까지 도착 — 정상.
  - ③ 조사 재사용: 스탬프투어 → 음식 칼로리계산앱 으로 아이디어를 바꾸자 **조사가 새로 돌았다** — 버그 수정 확인.
  - ⑤ 트랙 선택 없음 + 스타일 1종 고정 — 정상.
  - 지적받아 고친 것 (`dae680c`): 이어받는 동안 "작업을 대기열에 넣는 중"으로 보이던 것 →
    "새로고침 전에 맡긴 작업을 이어받는 중…". 헤더의 "여러 스타일로 받아서 고르고" → "받아서 다듬고".
- **막힘/조율 필요 — 모델 드롭다운이 아직 "Llama 3.3 70B (사내 GPU)"** (사용자 지적).
  프론트는 `/models` 응답을 그대로 뿌리는 구조이고, `config.yaml` 은 이미 `백석대학교 로컬 LLM`(세션3 `1bb3ad0`).
  **운영 :8000 서비스가 그 커밋 이전 코드로 돌고 있는 게 원인** — `GET /jobs` 에 `max_per_client` 가 없는 것과 같은 이유다.
  → `mochang-api` 재시작이면 해결되지만 공개 서비스 중단이라 **사용자 승인 필요**. 이름을 프론트에 하드코딩하지 않았다.
  재시작하면 작업 6(프론트+백엔드)과 ⑤(429) 확인이 같이 풀린다.
- **08:57 공개 배포 (사용자 승인)** — `npx vite build` 로 dist 재빌드 → `index-CG-vzjlN.js` 서빙 중.
  `b4839a1`+`dae680c` 가 공개됨. 배포 번들은 `.env.production` 대로 API 주소가 `/api`(같은 오리진 → nginx 프록시).
  같은 시점에 사용자가 `mochang-api` 를 재시작해 백엔드도 최신 코드로 올라옴(`/jobs` 에 `max_per_client:3` 확인).

#### 실서비스 검증 결과 (https://bustartup.kr:50001)

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | 모델 선택에 "백석대학교 로컬 LLM" | ✅ | 공개 URL `GET /api/models` → `"name":"백석대학교 로컬 LLM"`. 프론트는 이 목록을 그대로 렌더 |
| 2 | 로컬 트랙·스타일 선택 UI 제거 | ✅ | 배포 번들 문자열: `로컬 분야` 0건, `스토리텔링형` 0건, `간결·실무형` 0건 / `지원 분야` 1건 |
| 3 | 새 아이디어 생성 → 순번 → 결과 | ✅(API 레벨) | 새 아이디어(고령자 스마트 약통)로 `POST /api/jobs/research` → `position=0` → running → **113.9초** done, 이어서 `jobs/generate` → **27.7초** done, 1447/2000자 |
| 4 | 생성 도중 새로고침 → 이어받기 | ⏸ 미확인 | 브라우저 조작이 필요. dev :5173 에서는 사용자가 이미 통과 확인 |
| 5 | 결과에 조사 근거(출처) | ✅ | 검색어 6개 새로 생성(`cached:false`), 페이지 9건 → 근거 **8건 전부 URL 보유**, 그 facts 를 references 로 넘겨 생성 |

- 검증에 쓴 실호출: 라마 조사 1건 + 생성 1건 (무료 한도와 무관).
- ⚠️ **관찰(제 구역 아님, 세션3 작업 12-3단계 소관)**: 이번 조사에서 근거 8건 중 상위 4건의
  publisher 가 전부 `한국전자통신연구원` 이었다 — 소스 편중이 실서비스에서도 재현됨.
- 1·2·5 는 배포 산출물·공개 API 로 확인한 것이고, **화면으로 눈으로 본 확인과 4번은 브라우저 도구가 켜져야 가능**.
- 09:1x 사용자가 `/chrome` 실행·확장 설치했으나 **이 세션에는 브라우저 도구가 등록되지 않음**
  (`Skill(claude-in-chrome)`→Unknown skill, `ToolSearch` 로 `mcp__claude-in-chrome__*` 조회 실패).
  세션 시작 후에 붙은 도구라 등록이 안 된 것으로 보임 → **세션 재시작(`claude --continue`) 후 재시도**하거나
  사용자가 직접 화면 확인. 스크린샷 검증은 **미수행**(없는 결과를 기록하지 않음).


#### 프론트 UX 개선 후보 (세션1 배정, 2026-09-02 — **구현·배포 전 승인 필요**)

`App.jsx`(944행)·`api.js`(147행) 전수 검토. 취향 항목은 뺐고, 재현 경로가 있는 것만 적는다.

**A. 결함급 — 사용자가 멈춘 줄 알거나 결과를 잃는다**

| # | 문제 | 근거 | 고치는 법 |
|---|---|---|---|
| A1 | **조사·인테이크가 아직 동기 호출**이라 큐를 안 탄다 | `api.js:39·45·53` 이 `POST /intake`·`/intake/regenerate`·`/research` 를 직접 호출. 백엔드엔 이미 `/jobs/intake`·`/jobs/research` 가 있다(`main.py:251~258`). 실측 조사 1건 **113.9초** — 그동안 HTTP 연결을 붙잡고, 새로고침하면 통째로 유실되며, `max_jobs_per_client` 집계에도 안 잡힌다 | V1과 같은 `runJob("research"/"intake", …)` 로 전환. **선행**: 세션1이 적은 `/jobs/research`→`research_client` 통일(`main.py:257`) 뒤에 붙이는 게 안전 |
| A2 | 새로고침하면 **아직 제출 안 된 문항은 영영 안 만들어진다** | 이어받기(마운트 useEffect)는 `sessionStorage` 의 job id 만 복원한다. `generateAll` 의 `runLimited` 큐(아직 차례가 안 온 6~7문항)는 복원 대상이 아니다 → 9문항 중 2~3개만 채워진 채 멈춘다 | 이어받기가 끝난 뒤 "본문이 빈 문항"을 이어서 생성하거나, 상단에 `남은 N개 계속 만들기` 버튼 |
| A3 | 조사 차례를 기다리는 문항 카드가 **완전히 빈칸** | `generateAll` 은 조사 3병렬. 아직 차례가 안 온 문항은 `researchState[qid]`·`status[qid]` 둘 다 없어 `ResearchPanel`=null, 본문 블록 3개가 전부 false → 제목만 있는 빈 카드가 6~7개 늘어선다 | `running` 중 상태 없는 문항에 "차례를 기다리는 중" 한 줄 |
| A4 | **첫 문항 조사만 4배 느린데** 표시가 같다 | 세션3 2단계: 공통 조사(모델 4회)는 첫 문항에서만 돈다. 화면 문구는 모든 문항이 `웹에서 근거 자료를 조사하는 중…` 하나뿐 | 첫 문항에 "아이디어 공통 조사 중 — 처음 한 번만, 1~2분" 별도 문구 |
| A5 | 조사 결과 `pages` 를 **쓰지도 않으면서 localStorage 에 저장** | `App.jsx:393` 이 `pages` 를 넣는데 렌더에서 참조 0회(`ResearchPanel` 은 facts/queries/cached 만 씀). 9문항×수 KB → 용량 초과 시 `saveState` 가 통째로 실패해 **초안까지 저장이 안 된다** | 저장 직전에 `pages` 제외 |

**B. 공개 서비스에 안 맞는 문구·설정**

| # | 문제 | 근거 | 고치는 법 |
|---|---|---|---|
| B1 | 백엔드 다운 시 일반 사용자에게 **개발자 명령어**를 보여준다 | 헤더 하단 `uvicorn backend.main:app --port 8000 을 실행해 주세요` | "서버와 연결이 끊겼어요. 잠시 후 다시 시도해 주세요" |
| B2 | 모델이 1개인데 **드롭다운**이 있다 | `/models` 목록이 지금 1개 | `models.length <= 1` 이면 텍스트로 |
| B3 | 브라우저 동시 제출 `PARALLEL = 2` 가 서버보다 보수적 | `App.jsx:41`. 서버는 워커 30, IP 당 3건 허용 | 2 → 3. 9문항 기준 5배치 → 3배치 |
| B4 | 복사 실패가 조용하다 | `copy()` 가 `navigator.clipboard?.` — 없으면 아무 반응 없음 | 실패 시 "복사 실패 — 직접 선택해 복사하세요" |

**C. 있으면 좋은 것**

- C1 초안 화면 상단 **진행 바**(`3/9 작성됨`) + 미완성 문항으로 점프. 지금 `doneCount` 는 "선택됨" 기준이라 진행률과 다르다.
- C2 조사 패널에 세션3 신규 필드 활용 — `shared_queries`(공통) / `followup_queries`(이 문항 보완) 분리 표시. A4 가 자연히 설명된다.
- C3 `q.limit` 초과분을 `slice` 로 말없이 자른다(`runQuestionJob`) → "2000자에서 잘렸어요" 안내.
- C4 진행 표시에 `aria-live`, 아이콘 버튼에 `aria-label` (공공 서비스 접근성).
- C5 `새로 시작` 의 `confirm()` → 인앱 확인 UI.

**권장 순서**: A5·B1·B2·B4 (각 1~3줄, 위험 0) → A3·A2 → B3 → A1(백엔드 선행 필요) → C.
**A1 은 조율 필요** — 세션3의 `main.py:257` 정리 뒤에 붙는다.


- 완료 — **UX 개선 A2~A5 + B1~B4** (`c1dd0e4`). `vite build --outDir dist-check` 통과, **dist 미변경(배포 안 함)**.
  - A2 `generateMissing()` 신설. 마운트 이어받기가 **끝난 뒤** 본문이 빈 문항을 이어서 만든다.
    최신 `texts` 를 봐야 해서(마운트 클로저는 낡는다) `textsRef` 를 따로 둔다. 초안 화면 상단에
    `남은 N개 계속 만들기` 버튼도 추가 — 이어받기 말고 다른 이유로 빠진 경우까지 덮는다.
  - A3 `!st && running` 인 문항에 회색 자리표시 + "차례를 기다리는 중…" (조사 중이면 파란 패널이 대신 설명).
  - A4 `ResearchPanel({ warm })` — 이 아이디어로 조사 결과를 아직 하나도 못 받았으면
    "아이디어 공통 조사부터 합니다 — 처음 한 번만 1~2분". `dropStaleResearch` 에서 같이 초기화된다.
  - A5 저장 직전 `pages` 제거(`leanResearch`). 예전 저장본에 있던 pages 는 다음 저장 때 자연히 빠진다.
  - B1 `uvicorn …` 명령 제거 → "서버와 연결이 끊겼어요. 잠시 후 새로고침해 주세요." 주소·원인은 `title` 로만.
  - B2 `models.length <= 1` 이면 드롭다운 대신 글자.
  - B3 `PARALLEL 2 → 3`. 서버 워커 30 · IP 당 3건이므로 429 는 안 난다(제출 429 재시도 로직은 그대로).
  - B4 `copy()` 가 `navigator.clipboard` 부재·거부를 잡아 버튼 문구를 "복사 안 됨 — 직접 선택하세요"로.
  - **A1(조사·인테이크 큐 전환)은 안 했다** — 세션1이 적은 `main.py:257`(`/jobs/research` → `research_client`)
    정리 뒤에 붙이는 게 안전. 그때 `api.js` 에 `runJob("research"/"intake", …)` 추가하면 된다.

#### 사용자 검증 체크리스트 2차 (dev :5173, HMR 로 이미 반영됨 — 백엔드는 운영 :8000)

1. **A2** 생성이 도는 중에 F5 → 이어받은 문항이 끝난 뒤 **나머지 문항도 저절로 이어서** 만들어지는지.
   (전에는 2~3개만 채워지고 멈췄다.) 중간에 멈춰 있으면 상단 `남은 N개 계속 만들기` 가 보여야 한다.
2. **A3** 초안 화면에 들어간 직후, 아직 차례가 안 온 문항 카드에 **회색 상자 + "차례를 기다리는 중…"** 이 보이는지.
   (전에는 제목만 있는 빈 카드였다.)
3. **A4** 새 아이디어로 처음 만들 때 첫 문항에 **"아이디어 공통 조사부터 합니다 — 처음 한 번만 1~2분"**,
   두 번째 문항부터는 원래 문구로 바뀌는지.
4. **B2** 헤더에 드롭다운 대신 `모델 백석대학교 로컬 LLM` 글자로 보이는지.
5. **B1** (확인 어려움) 백엔드를 끄면 개발자 명령 대신 "서버와 연결이 끊겼어요" 만 보인다.
6. **A5** 개발자도구 Application → Local Storage → `modoo-writer-state-v1` 안 `researchState` 에
   `pages` 키가 없는지 (한 번 새로 조사한 뒤).

⏸ **배포(=`npx vite build`)는 승인 대기** — 승인 전까지 `dist` 는 `index-CG-vzjlN.js` 그대로 둔다.


- 완료 — **작업 9: 벡터DB 모듈** (`7b2a6e5`). `RAG_PLAN` 5-1절 서명 그대로, **pytest 전체 124 passed, 실호출 0**.
  - `backend/rag/vectorstore.py` (신규) + `tests/test_vectorstore.py` (신규 18건) + `requirements.txt` 2줄.
    **`pipeline.py` 등 기존 백엔드 파일은 하나도 안 건드렸다** — 파이프라인 연결은 세션3 몫.
  - `upsert_pages` 는 `collect_pages` 의 page dict 를 그대로 받는다. url 해시로 upsert(프래그먼트·끝
    슬래시 무시), 본문 40자 미만이면 색인 안 함(`snippet` 폴백은 있음), 청크 512토큰.
    메타: `url/title/publisher/date/fetched_at/source_kind/query` — `publisher` 없으면 도메인
    (`pipeline.py:271` 과 같은 규칙), `source_kind` 는 검색 결과의 `source_type` 을 옮긴 것.
    메타는 임베딩 본문에서 제외했다(제목·URL 이 섞이면 유사도가 주제가 아니라 형식에 끌린다).
  - `query` 는 같은 문서의 청크를 **최고점 하나로 합쳐** 페이지 목록으로 준다. 신선도 컷은
    `date` 가 `max_age_days`(기본 730) 초과면 제외, **날짜를 모르는 문서는 남긴다**.
  - **📢 세션3 참고 — 고정 서명 외에 추가한 것 3개** (서명 변경 아님, 안 써도 그만):
    ① `indexed_urls() -> set[str]` — `collect_pages(skip_urls=…)` 에 바로 넘길 수 있다.
    ② `ollama_embedding(model_name="bge-m3", base_url=…)` — 운영용. **지연 import 라 Ollama 없어도
       모듈 import 는 된다.** `VectorStore(persist_dir, embed_model=ollama_embedding())` 로 넘기면 됨.
    ③ `query()` 결과에 `from_vectorstore: True` — 웹에서 새로 받은 페이지와 구분용.
  - `embed_model=None` 기본값은 `OfflineEmbedding`(글자 2-gram 해시 + L2 정규화). LlamaIndex
    `MockEmbedding` 을 안 쓴 이유: 모든 문장에 같은 벡터를 줘서 유사도가 항상 1.0 → `min_score`
    동작을 테스트로 지킬 수 없다. **운영에서는 반드시 `embed_model` 을 넘겨야 한다** (안 넘기면 조회 품질이 없다).
  - ⚠️ **공유 `.venv` 에 패키지를 설치했다** — `llama-index-core 0.14.24`,
    `llama-index-embeddings-ollama 0.10.0` + 의존성 45개(numpy·aiohttp·sqlalchemy·nltk 등).
    `setuptools 65.5.0 → 84.0.0` 만 업그레이드됐고 pydantic·httpx·fastapi 는 그대로다.
    설치 직후 기존 테스트 106건 통과를 먼저 확인했다. 다른 PC 는 `pip install -r requirements-dev.txt` 필요.
  - 아직 안 한 것(세션3 소관): `config.yaml research.vectorstore.{enabled,min_score,min_hits,max_age_days}`
    설정 항목, `collect_pages` 연결, `asyncio.to_thread` 래핑. Ollama 실호출(`ollama pull bge-m3`) 검증도 미수행.


- **공개 배포 (사용자 승인, 2026-09-02)** — `npx vite build` 로 `frontend/dist` 갱신.
  **새 번들: `index-vkBBBKoM.js` / `index-Da5jo6yn.css`** (이전 `index-CG-vzjlN.js` → 교체됨).
  배포 내용은 `c1dd0e4` (UX 개선 A2~A5 + B1~B4). 빌드 전 워킹트리에 프론트 미커밋 변경 없음,
  `frontend/.env` 없음(= `.env.production` 의 `VITE_API_BASE=/api` 로 빌드됨) 확인 후 실행.
  - 공개 URL 확인: `https://bustartup.kr:50001/` 이 `assets/index-vkBBBKoM.js` 를 참조 → nginx 가 새 번들 서빙 중.
  - 번들 문자열 검증 — 들어간 것: `차례를 기다리는 중`(A3) · `남은 N개 계속 만들기`(A2) ·
    `아이디어 공통 조사부터`(A4) · `복사 안 됨`(B4) · `서버와 연결이 끊겼어요`(B1).
    빠진 것(0건): `uvicorn`(B1 제거 확인) · `로컬 분야` · `스토리텔링형`(작업 11 유지 확인).
  - `frontend/dist` 는 `frontend/.gitignore` 로 추적 제외라 **커밋할 산출물은 없다** (배포 = 파일 교체).
  - ⏸ 남은 브라우저 확인은 이제 공개 URL 에서 하면 된다: A2(생성 중 F5 → 나머지 문항까지 이어서 생성),
    A3(진입 직후 대기 카드), A4(첫 문항 문구), B2(모델이 드롭다운 아닌 글자).
  - 📌 세션1/세션3: 워커 36+9(`054a51f`)는 **아직 운영 미반영** — `mochang-api` 재시작이 필요하다.
    이번 배포는 프론트 정적 파일만 바꾼 것이라 백엔드는 그대로다.

### 세션3 (백엔드)

- 시작 — 작업 2(V4), 작업 6 백엔드 몫, 작업 12 1단계.
  건드릴 파일: `backend/llm/jobs.py`, `backend/main.py`, `backend/config.yaml`, `backend/rag/*`, `tests/`, `scripts/load_test.py`.
- 완료 — **작업 2(V4) + 작업 6 백엔드 몫** (`1bb3ad0`). 테스트 90 passed.
  - IP 당 동시 작업 3건(`config.yaml max_jobs_per_client`), 초과 제출은 `429` + `Retry-After: 10`,
    detail 은 한국어 안내문("동시에 진행할 수 있는 작업은 3건까지입니다 …").
  - 클라이언트 식별은 `X-Forwarded-For` 첫 항목 우선 → nginx 50001 뒤에서도 사용자별로 구분됨.
  - **📢 세션2 참고 (응답 필드 추가만, 삭제·개명 없음)**: `POST /jobs/{kind}` 가 429 를 낼 수 있음
    (본문 `{"detail": "…"}`) — 프론트에서 429 는 오류가 아니라 "잠시 후 재시도" 안내로 다루면 좋음.
    `GET /jobs` 에 `max_per_client`, `your_active`(내 IP 가 점유 중인 작업 수) 추가.
  - `config.yaml` 모델 표기 `백석대학교 로컬 LLM` (id 는 그대로) → `/models` 로 내려감.
  - `scripts/load_test.py` 는 요청마다 다른 IP 를 흉내내도록 수정(안 하면 부하가 아니라 429 를 재게 됨).
    `--same-ip` 옵션으로 제한 동작 확인 가능.
- 완료 — **작업 12 1단계** (`8da2f0a`). 테스트 91 passed. 조사 1회 외부 요청 **20건 → 10건**.
  - `collect_pages` 가 `max_pages_to_extract * 2` 를 받아 절반 버리던 것 제거 (fetch 12 → 6).
    여유가 필요하면 `config.yaml research.max_pages_to_fetch` 로만 올린다 (기본 6, 실패 잦으면 8).
  - `DDGSearch.include_news` 기본 `False` — DDG 호출 8 → 4. 뉴스 기능은 삭제하지 않았고
    `DDGSearch(include_news=True)` 로 즉시 되살아난다 (3단계에서 네이버 뉴스 API 로 이관 예정).
- 완료 — **작업 12 0단계: 검색어 중복도 실측** (`ca8e71e`). 라마 실호출 20회 (아이디어 2개 × 10문항).
  재현: `.venv/Scripts/python -m scripts.query_dump --ideas 2 --json out.json` (검색·fetch 없음 = 외부 요청 0건).

  | 지표 | 아이디어 A(냉장고 관리 앱) | 아이디어 B(전통시장 배달 중개) |
  |---|---|---|
  | 총 검색어 (아이디어 조사 1 + 문항 9, 각 4개) | 40 | 40 |
  | 문자열 중복 제거 후 | 37 (**중복률 7%**) | 39 (**3%**) |
  | 사실상 같은 검색어 쌍(토큰 50% 이상 겹침) | **49쌍** | 17쌍 |
  | 아이디어 검색어가 토큰으로 덮는 문항 검색어 | **21/36 (58%)** | 13/36 (36%) |
  | 문항쌍 유사도 평균 / 최대 | 0.42 / **0.75 (q3_1↔q8)** | 0.30 / 0.48 (q2↔q4_2) |

  **해석 (2단계 설계 근거)**: 글자 그대로 같은 검색어는 3~7% 뿐이라 **문자열 캐시로는 거의 못 줄인다**.
  반면 토큰 기준으로는 절반 가까이("… 현황"/"… 통계"/"… 시장 규모") 사실상 같은 검색이라 검색 결과 페이지가 크게 겹친다.
  → 2단계는 "아이디어 단위 공통 조사 1회(검색어 4개)를 먼저 하고, 문항별로는 그 결과로 못 채우는
  각도만 1~2개 추가"가 맞다. 문항당 4개 고정을 없애는 것이 핵심 (조사 10회 → 1회 + 보완).
  아이디어별 편차가 크므로(58% vs 36%) 문항 보완 검색어 수는 고정하지 말고 상한만 두는 쪽.
- 완료 — **작업 12 3단계 선작업: 네이버 검색 API 어댑터** (`23ad8de`). 테스트 96 passed, 실호출 0. *(구 규격 — 아래 정정으로 대체됨)*
  - `NaverSearch` — kinds(news/blog/doc/webkr) 각각 요청 1회, `<b>` 태그·엔티티 제거, originallink 우선,
    유형 하나가 실패해도 나머지는 살림. `_norm_date` 가 네이버 날짜 2종(pubDate, postdate) 인식.
  - **키가 없으면 백엔드 목록에 아예 안 들어간다** → 지금은 예전과 100% 동일하게 ddgs 단독.
    `.env` 에 `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` 이 들어오는 순간 `naver → ddgs` 순서로 자동 전환.
  - 📌 세션1에게: `.env.example` (루트, 제 구역 밖이라 안 건드림)에 아래 2줄 추가해 두면 좋겠습니다.
    `NAVER_CLIENT_ID=` / `NAVER_CLIENT_SECRET=`
- 완료 — **작업 12 2단계: 아이디어당 1회 조사** (`0c1f125`). 테스트 103 passed, 실호출 0.
  - `run_research` 가 아이디어 **공통 조사 결과를 물려받고**, 그 사실로 못 채우는 각도만 보완 검색한다.
    공통 조사는 그 아이디어의 첫 문항에서만 실제로 돌고, 나머지 8문항은 캐시 적중 = 외부 요청 0건.
  - 보완 검색어는 새 프롬프트 `research/followup_queries.md` 가 만든다 —
    **"이미 확보한 사실로 충분하면 빈 배열"이 최선의 답**이라고 지시하고, 개수는 고정하지 않고 상한만 둔다.
    모델 응답이 깨지면 폴백 검색어를 만들지 않는다(요청을 늘리지 않는 게 목적).
  - `collect_pages(skip_urls=…)` — 공통 조사에서 이미 본문을 받은 페이지는 다시 안 받는다.
  - **외부 요청 실측 (mock 카운터, `test_one_person_scenario_external_requests_before_and_after`)**

    | 1인 시나리오 (인테이크 + 문항 9개) | 검색 | 페이지 fetch | 합계 | 모델 호출 |
    |---|---|---|---|---|
    | 2단계 전 (문항마다 처음부터 조사) | 40 | 60 | **100** | 20 |
    | 2단계 후 (보완 2개 = 최악의 경우) | 22 | 33 | **55** | 20 |
    | 2단계 후 (모델이 "충분" 판단) | 4 | 6 | **10** | 11 |

    0단계 실측(공통 조사가 문항 검색어의 36~58% 커버)을 감안하면 실제 값은 55와 10 사이,
    대략 **30~40건**으로 예상. 1단계까지 합치면 개편 전 200건 → 지금 30~55건.
  - 📢 **세션2 참고 (응답 필드 추가만, 삭제·개명 없음)**: `POST /jobs/research` · `POST /research` 응답에
    `shared_queries`(공통 조사 검색어) · `followup_queries`(이 문항에서만 추가한 검색어) ·
    `shared_facts_used`(facts 중 공통 조사에서 물려받은 개수) 3개 추가. `queries` 는 둘을 합친 값이라
    기존 화면은 그대로 두면 된다. `facts`/`references`/`pages`/`cached` 는 의미·형태 그대로.
  - ⚠️ 체감 변화: **첫 문항 조사만 느려진다** (공통 조사 + 보완 = 모델 호출 4회). 2~9번째 문항은
    모델 호출 1~2회로 오히려 빨라진다. 인테이크를 거친 사용자는 공통 조사가 이미 캐시에 있어 첫 문항도 빠르다.
  - 되돌리기: `config.yaml research.share_idea_research: false` 한 줄이면 예전 동작(문항마다 처음부터 조사)으로
    복귀한다. 예전 경로도 테스트로 계속 지키고 있다.
  - 📌 세션1에게(코드 아님, 확인만): `/research` 는 `research_client`(조사용 모델)로, `/jobs/research` 는
    `client`(작성 모델)로 조사를 돌린다 — 2단계 전부터 있던 불일치다. 지금은 둘 다 같은 로컬 라마라 결과는 같지만,
    나중에 모델을 나누면 조사 품질이 경로마다 달라진다. 정리 여부는 세션1 판단.
- 완료 — **작업 10: GPU 한계 측정 + 워커 상향** (`d7d34a9`) *(1차 — 짧은 프롬프트 기준. 작업 13에서 실사용 프롬프트로 재측정해 값이 36/9 로 바뀜)*. 사용자 승인 후 실측, 라마 실호출 **107회**(측정용).

  측정 조건: `scripts/gpu_probe.py`, vLLM 에 **직접** 동시 호출(우리 큐 우회), 문항 생성 길이 프롬프트,
  `max_tokens 600`, 단계마다 `/metrics` 1초 간격 샘플링. 2026-09-02 저녁.

  | 동시성 | 성공/실패 | p50 | p95 | 최대 | 전체 소요 | 생성 tok/s | KV캐시 최대 | 서버 running 최대 | 서버 waiting 최대 | preemption |
  |---|---|---|---|---|---|---|---|---|---|---|
  | 12 | 12 / 0 | 8.6s | 9.6s | 10.0s | 10.2s | 311 | **1.9%** | 12 | 0 | 0 |
  | 20 | 20 / 0 | 10.7s | 12.1s | 12.9s | 13.2s | 423 | **3.1%** | 20 | 0 | 0 |
  | 30 | 30 / 0 | 11.4s | 13.1s | 14.7s | 14.9s | 580 | **4.6%** | 30 | 0 | 0 |
  | 45 | 45 / 0 | 14.3s | 15.9s | 17.9s | 18.1s | 706 | **7.0%** | 45 | 0 | 0 |

  **분석**
  - **한계점이 측정 구간 안에 없다.** KV 캐시는 45 동시에서도 7%(기준 90%), 서버 waiting 0, preemption 0.
    45개를 전부 동시에 처리했다 = vLLM 의 `--max-num-seqs` 도 45 이상.
  - p95 는 9.6 → 12.1 → 13.1 → 15.9초로 **완만히만** 증가(45에서도 어제 실사용 대기 2분49초보다 훨씬 짧다).
    처리량은 311 → 706 tok/s 로 계속 늘지만 증가폭은 둔해진다(20→30 +37%, 30→45 +22%).
  - 어제 진단(“병목은 GPU 가 아니라 우리 워커 12개”)이 숫자로 확인됐다. 워커 12 단계에서 서버 waiting 이 0이라는 것은
    **GPU 가 놀고 있었다**는 뜻이다.

  **채택값과 근거**: 판단 기준(KV 90% 근접·p95 꺾임·preemption)이 하나도 걸리지 않았으므로
  “측정된 최대(45)의 한 단계 아래”인 **30** 을 안전 상한으로 잡았다. 45 를 그대로 쓰지 않은 이유 3가지 —
  ① 측정용 프롬프트는 실사용보다 짧다(실사용은 참고자료가 붙어 KV 를 더 쓴다) ② 조사 워커와 GPU 를 나눠 쓴다
  ③ 공유 GPU 서버라 다른 사용처를 위한 여유가 필요하다.
  - `max_workers: 12 → 30` (본문 생성)
  - `research.llm.max_workers: 4 → 8` (조사) — 같은 근거. **본문 30 + 조사 8 = 38 ≤ 측정 확인된 45.**
  - 되돌리기는 두 줄. 더 올리려면 `--levels 60,80` 으로 한 번 더 재면 된다(측정 자체는 5분).
  - 측정 원자료 JSON 은 세션 임시 폴더에만 두었다(리포는 깨끗하게). 필요하면 위 명령으로 재현 가능.

  ⚠️ **운영 반영은 아직 안 됐다 — `mochang-api` 재시작이 필요하다.**
  `config.yaml` 은 프로세스가 시작할 때 한 번만 읽는다(`load_config()` at import). 지금 커밋된 30/8 은 파일에만 있고,
  :8000 에서 도는 서비스는 여전히 12/4 로 동작한다. TEAMWORK 4절대로 **세션3은 NSSM 서비스를 건드리지 않았다.**
  - 필요한 조작(사용자): 관리자 PowerShell 에서 `nssm restart mochang-api` (또는 stop → start).
  - **권장 시점**: ① 세션2 의 V1(큐 전환) + 작업 11 프론트 변경이 끝나 **dist 배포와 한 번에 묶어서**,
    ② 사용 없는 시간대(심야/이른 아침). 재시작 중 진행 중이던 생성 작업은 **큐가 메모리에만 있어 전부 사라진다**
    (작업 ID 폴링이 404 가 된다) — 사용자가 붙어 있는 낮 시간대는 피할 것.
  - 재시작 뒤 확인: `GET /jobs` 의 `max_workers` 가 30 인지. 그 다음 부하 체감은
    `scripts/load_test.py --n 100 --jobs` 로 큐 쪽만 따로 볼 수 있다(모델은 가짜라 GPU 부하 없음).
- 완료 — **작업 12 3단계 정정: NaverSearch 를 NAVER API HUB 규격으로** (`6c7e9bc`). 테스트 106 passed, **실호출 0**.
  상태판 "🔴 3단계 정정" 5개 항목 전부 반영:
  - ① `base_url` → `https://naverapihub.apigw.ntruss.com/search/v1`, 경로 `/{kind}` (`.json` 없음)
  - ② 인증 헤더 → `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` (구 `X-Naver-Client-*` 삭제).
    **`.env` 이름은 `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` 그대로** — NCP 콘솔의 Client ID/Secret 을 그 이름에 넣으면 된다.
  - ③ `doc`(전문자료) 제거(2026-07-31 종료). 대신 `webkr`(웹문서)·`cafearticle`(카페 글, 고객 불만 근거)을 넣었다.
    학술 근거는 KCI(`KCI_API_KEY`) → OpenAlex/Semantic Scholar(키 불필요) 자리로 비워 둔다.
  - ④ 오류 본문 2형태 모두 파싱(`naver_error_message`): 평면 `{"errorCode","errorMessage"}` /
    중첩 `{"error":{"errorCode","message"}}`. **429 는 남은 유형을 더 쏘지 않고 중단**하고
    `SearchRateLimited` 로 올려 ddgs 폴백 — 5단계 서킷브레이커가 그대로 잡을 신호 타입이다.
    429 가 아닌 오류(500 등)는 유형 하나만 건너뛰고 나머지는 살린다. 오류 문자열은 `NaverSearch.last_error`.
  - ⑤ `display` 1~100 로 제한, 한도 주석 25,000/일 → **월 775,000건·키당 50 RPS** 로 정정 (config.yaml 포함).
  - 동작 변화 없음 확인: **키가 없으면 백엔드 목록에서 빠져 지금도 ddgs 단독**이다. 키가 들어오면 `naver → ddgs`.
  - 남은 3단계 작업(키 필요 없음): KCI/OpenAlex 어댑터, 정형 API(KOSIS·ECOS·data.go.kr) 별도 경로 설계.
- 📌 세션1에게 — 키가 발급되면 `.env` 에 이 이름으로 넣어 주세요 (코드가 이 이름을 읽습니다):
  `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`, `DATA_GO_KR_API_KEY`, `KOSIS_API_KEY`, `ECOS_API_KEY`,
  `KCI_API_KEY`, `KIPRIS_API_KEY`. 지금 코드가 실제로 쓰는 건 네이버 2개뿐이고 나머지는 어댑터를 만들 때 씁니다.
  `.env.example`(루트, 제 구역 밖)에도 같은 줄을 넣어 두면 다른 PC 에서 헤매지 않습니다.
- 완료 — **작업 13: 실사용 길이 프롬프트로 고동시성 실측 → 워커 36+9** (`d3d26a2`, `054a51f`).
  라마 실호출 **약 500회**(측정용). 사용자 지시로 진행.

  **1차(09-02 저녁)와 조건이 다르다**: 1차는 한 줄짜리 짧은 프롬프트 + `max_tokens 600` 이었다.
  이번엔 `--prompt real` — 실제 생성 경로와 같은 프롬프트(system.md + 트랙 + 문항 md + 참고자료 컨텍스트,
  **3,370 토큰**)에 `max_tokens 4096`, 실제 생성량 **795 토큰**. 1차 수치는 prefill 비용과 KV 점유를 과소평가한 값이다.

  | 동시성 | 성공/실패 | p50 | p95 | 생성 tok/s | 평균 프롬프트/생성 토큰 | KV캐시 최대 | 서버 running | waiting | preempt |
  |---|---|---|---|---|---|---|---|---|---|
  | 20 | 20 / 0 | 36.5s | 40.1s | 379 | 3370 / 798 | 11.0% | 20 | 0 | 0 |
  | 30 | 30 / 0 | 42.0s | 44.6s | 509 | 3370 / 789 | 15.3% | 30 | 0 | 0 |
  | 45 | 45 / 0 | 54.6s | 58.5s | 574 | 3370 / 795 | 21.6% | 45 | 0 | 0 |
  | 60 | 60 / 0 | 65.1s | 70.1s | **650** | 3370 / 795 | 28.9% | 60 | 0 | 0 |
  | 80 | 80 / 0 | 90.8s | 97.6s | 627 | 3370 / 791 | 36.5% | 80 | 0 | 0 |
  | 100 | 100 / 0 | 114.5s | 122.2s | 640 | 3370 / 800 | 45.1% | 100 | 0 | 0 |
  | ~~150~~ | 150 / 0 | 137.3s | 194.9s | 594 | — | 47.1% | **100** | 0 | 0 |

  ⚠️ **150 행은 무효다 — 측정기 쪽 함정.** httpx 기본 커넥션 풀이 100이라 나머지 50건이 **서버가 아니라
  클라이언트에서** 줄을 섰다(그래서 서버 running 이 100 에서 멈추고 waiting 은 0). 풀을 `n+20` 으로 열도록
  고쳤다(`054a51f`). **운영 코드는 무관** — `AsyncOpenAI` 는 풀이 1000이다. 같은 함정을 다시 밟지 않도록 주석으로 남겼다.

  **분석 — 한계는 메모리가 아니라 처리량이고, 포화점은 60이다.**
  - 처리량은 379 → 509 → 574 → **650(60)** 까지 오르고 그 뒤로 평평하다(80: 627, 100: 640).
    60 이상에서는 **동시성을 올려도 전체 처리량이 늘지 않고 문항당 대기만 비례해서 길어진다**(65s → 122s).
  - KV 캐시는 100 동시에서도 45% 라 90% 기준까지 한참 남았고, 실패·서버 대기·preemption 은 전 구간 0.
    즉 vLLM 은 더 받아 줄 수 있지만 **받아도 더 빨리 처리하지는 못한다**.
  - 1차 결론(“45 까지 여유”)과 모순되지 않는다. 1차는 짧은 프롬프트라 60까지 못 가 봤을 뿐이다.

  **채택: 총 동시 호출 45** = `max_workers 30 → 36`(본문) + `research.llm.max_workers 8 → 9`(조사).
  - 규칙(꺾이는 지점의 한 단계 아래)대로 60 이 아니라 45. 60 으로 올려도 처리량은 같고 문항당 p50 이
    55초 → 65초로 늘 뿐이다. **남는 요청은 vLLM 이 아니라 우리 큐가 잡는 편이 낫다** — 우리 큐는
    대기 순번(`position`)을 화면에 보여줄 수 있지만, vLLM 안에서 기다리는 요청은 그냥 느린 요청으로만 보인다.
  - 조사 워커를 낮게 두는 이유: 조사 호출은 프롬프트가 훨씬 길다(페이지 6개 ≈ 2만 토큰) — 슬롯당 GPU 비용이 크다.
  - 시스템 처리 능력 자체는 워커 수와 무관하게 **≈ 650 tok/s ≈ 문항 0.8건/초 ≈ 분당 49문항**이다.
    더 필요하면 워커가 아니라 GPU·모델 쪽(양자화·서버 증설)을 봐야 한다.
  - 📌 **`nvidia-smi` 값은 아직 못 받았다.** 다만 KV 캐시가 100 동시에서도 45% 라 메모리는 제약이 아니어서
    상한 판단이 바뀌지는 않을 것으로 본다. 값 주시면 로그에 붙이고, GPU 사용률이 100% 에 붙어 있다면
    “처리량 포화” 해석을 한 번 더 확증하는 자료가 된다.
  - vLLM `--max-num-seqs` 의 실제 값은 아직 모른다(≥100 인 것만 확인). 우리 상한이 45라 지금은 알 필요가 없어
    150 재측정은 하지 않았다 — 공유 GPU 에 3분 넘게 부하를 다시 거는 값어치가 없다.

  ⚠️ **운영 반영은 여전히 `mochang-api` 재시작 필요** (앞 항목의 재시작 안내 그대로. 값만 30/8 → **36/9** 로 바뀜).
  재시작 뒤 `GET /jobs` 의 `max_workers` 가 36 이면 반영된 것이다.
- 완료 — **네이버 API HUB 실키 검증** (`2f3d66b`). `.env` 에 키가 들어온 것을 확인하고 실호출 **2건**(news·blog).
  - `scripts/naver_check.py` 로 언제든 재확인 가능 (`--query`, `--kinds`). 캐시를 거치지 않는다.
  - 결과: 경로·헤더·응답 파싱 전부 정상. **뉴스는 품질이 좋다**(2026년 1인 가구·식품 폐기 기사가 최신순으로).
  - ⚠️ **blog 는 `sort=sim` 이라 2023년 무관한 글이 섞여 나온다.** `sort: "date"` 로 바꾸거나 blog 를 빼고
    `webkr` 을 쓰는 게 나아 보인다 — 3단계 라우팅에서 유형별로 `sort` 를 다르게 주는 설계로 정리하겠다.
  - 키가 있으므로 **재시작하는 순간부터 검색이 `naver → ddgs` 순으로 나간다**(지금 :8000 은 아직 ddgs 단독).
    한도는 월 775,000건이라 여유가 크지만, 재시작 후 첫날은 NCP 콘솔에서 사용량을 한 번 봐 두면 좋다.
- 다음(세션3 소관, 세션2 작업 9 인계분) — `backend/rag/vectorstore.py` 를 파이프라인에 연결:
  `config.yaml research.vectorstore.{enabled,min_score,min_hits,max_age_days}` 추가, `collect_pages` 연결
  (`indexed_urls()` → `skip_urls`), `asyncio.to_thread` 래핑, Ollama `bge-m3` 실호출 검증.
- 완료 — **작업 12 3단계: 정형 공공 API 어댑터 4종 + 도메인별 상한 + 네이버 정렬** (`c747955`). 테스트 147 passed.
  각 소스 **실호출 1건씩** 확인 (KOSIS 는 검색+수치라 2건). 재확인: `.venv/Scripts/python -m scripts.opendata_check`

  | 소스 | 하는 일 | 요청 수 | 실호출 결과 (2026-09-02) |
  |---|---|---|---|
  | **KOSIS** | 통계표 검색 → 이름이 검색어와 겹치는 표의 최신 수치 | 2 | 「1인가구비율」 **전국 1인가구 8,244,308가구 / 비율 36.6% (2025)** |
  | **ECOS** | 100대 지표 1회 조회 후 검색어와 맞는 지표만 | 1 (이후 캐시) | **기준금리 3%**, 원/달러 1,370.4원, 원/엔 855.21원 … |
  | **K-Startup** | 공고명 LIKE 검색, 공고 본문이 그대로 근거 | 1 | 청년창업 공고 3건 (지원대상·본문 포함) |
  | **상권정보** | 업종 대분류 매칭 → 전국 상가업소 수 | 2 (업종목록 캐시) | **전국 '음식' 업종 840,255개 (2026-06 기준)** |

  - **폴백이 아니라 덧붙이는 소스**다. `Researcher(extras=[...])` 로 늘 같이 물어보고 웹 결과 **앞에** 붙인다.
    웹 검색이 전부 막혀도 정형 결과만이라도 나오고, 정형 소스가 죽어도 웹 검색은 그대로 간다(양방향 격리, 테스트로 지킴).
  - **검색어에 그 소스 키워드가 없으면 HTTP 요청 자체를 하지 않는다**(`TRIGGERS`). 예: "음식 업종 점포 수" 는
    상권정보만 부르고 K-Startup 은 안 부른다. 한도 절약 + 로그 가독성. 모델로 유형 분류하는 원안은 호출이
    한 번 더 늘고 디버깅이 어려워 보류 — 실사용 로그를 본 뒤 필요하면 바꾼다.
  - 결과에 `no_fetch` 플래그 → `collect_pages` 가 **fetch 없이 snippet 을 본문으로** 쓴다. 정형 소스는
    받아올 페이지가 없고 이미 사실 문장이라, 외부 요청이 늘지 않는다(오히려 웹 fetch 자리를 대체한다).
  - ⚠️ **KOSIS 는 수치를 못 가져오면 아무것도 주지 않는다.** 축이 여러 개인 표는 KOSIS 가 '40,000셀 초과'로
    거절하는데, 그때 "이런 통계표가 있다"는 문장을 근거랍시고 넣으면 진짜 근거 자리만 차지한다
    (첫 실호출에서 관련 없는 관광 통계표가 그렇게 1위로 끼어들었다 — 표 이름 매칭까지 넣어 해결).
    표별 축·항목 조합 목록을 갖추면 더 많은 표에서 수치를 뽑을 수 있다 → **후속 과제**(자주 쓰는 표부터).
  - **도메인별 상한** `max_pages_per_domain: 2` (구 작업 7) — 같은 신문사 글이 근거를 독식하지 못하게.
    정형 결과는 상한 대상이 아니다(공식 통계는 소스가 하나뿐이라 쏠림이 문제되지 않는다).
  - **네이버 유형별 정렬**: 블로그·카페는 `date`(최신순), 뉴스·웹문서는 `sim`. 지난 실호출에서 블로그를
    `sim` 으로 부르면 3년 전 무관한 글이 섞여 나온 문제를 이렇게 정리했다. `research.naver.sort` 에 값을 주면
    전 유형에 강제된다(비워 두는 게 기본).
  - 키 없는 소스는 목록에서 자동으로 빠진다 — 키를 지워도 예전처럼 돈다.
- 완료 — **작업 9 연결(색인 단계)** (`12d44a5`). 테스트 152 passed. Ollama 실호출 1건.
  - `collect_pages` 가 만든 page dict 를 그대로 `upsert_pages` 로 넘긴다. `asyncio.to_thread` 로 감쌌고,
    **색인 실패는 조사를 막지 않는다**(부가 기능이라 조용히 넘어간다 — 테스트로 지킴).
  - `config.yaml research.vectorstore.{enabled,dir,embed_model,ollama_url,min_score,min_hits,max_age_days}` 추가.
    **`enabled: false` 가 기본** — 켜면 페이지마다 임베딩 호출이 붙고 디스크가 쌓이므로 사용자 결정 뒤에 켠다.
    켜져 있을 때만 저장소를 열고, `llama_index` import 도 그때 처음 일어난다(꺼져 있으면 기동 비용 0).
  - 세션2 인계 노트대로 **조회(웹 검색 대체)는 붙이지 않았다** — 이번엔 색인까지만.
  - 실호출 검증: bge-m3 로 페이지 1건 색인 → 조회 적중, 저장소 파일 5개 생성 확인.
    📌 **세션1·2 참고 — `min_score: 0.8` 은 너무 빡빡해 보인다.** 같은 주제 문장으로 조회했는데
    **score 0.668** 이 나왔다(1인가구 통계 본문 ↔ "1인 가구 비율"). 지금은 색인만 해서 영향이 없지만,
    조회 단계를 붙일 때 이 값을 그대로 두면 벡터DB 가 사실상 안 쓰인다. 실측 몇 건 더 뽑아 0.5~0.6 선으로
    조정하는 걸 권한다(조회 단계 작업자가 정할 문제라 지금은 안 건드림).
- 완료 — **작업 12 3단계: 정형 공공 API 어댑터 4종 + 도메인별 상한 + 네이버 정렬** (`c747955`). 테스트 147 passed.
  각 소스 **실호출 1건씩** 확인 (KOSIS 는 검색+수치라 2건). 재확인: `.venv/Scripts/python -m scripts.opendata_check`

  | 소스 | 하는 일 | 요청 수 | 실호출 결과 (2026-09-02) |
  |---|---|---|---|
  | **KOSIS** | 통계표 검색 → 이름이 검색어와 겹치는 표의 최신 수치 | 2 | 「1인가구비율」 **전국 1인가구 8,244,308가구 / 비율 36.6% (2025)** |
  | **ECOS** | 100대 지표 1회 조회 후 검색어와 맞는 지표만 | 1 (이후 캐시) | **기준금리 3%**, 원/달러 1,370.4원, 원/엔 855.21원 … |
  | **K-Startup** | 공고명 LIKE 검색, 공고 본문이 그대로 근거 | 1 | 청년창업 공고 3건 (지원대상·본문 포함) |
  | **상권정보** | 업종 대분류 매칭 → 전국 상가업소 수 | 2 (업종목록 캐시) | **전국 '음식' 업종 840,255개 (2026-06 기준)** |

  - **폴백이 아니라 덧붙이는 소스**다. `Researcher(extras=[...])` 로 늘 같이 물어보고 웹 결과 **앞에** 붙인다.
    웹 검색이 전부 막혀도 정형 결과만이라도 나오고, 정형 소스가 죽어도 웹 검색은 그대로 간다(양방향 격리, 테스트로 지킴).
  - **검색어에 그 소스 키워드가 없으면 HTTP 요청 자체를 하지 않는다**(`TRIGGERS`). 예: "음식 업종 점포 수" 는
    상권정보만 부르고 K-Startup 은 안 부른다. 한도 절약 + 로그 가독성. 모델로 유형 분류하는 원안은 호출이
    한 번 더 늘고 디버깅이 어려워 보류 — 실사용 로그를 본 뒤 필요하면 바꾼다.
  - 결과에 `no_fetch` 플래그 → `collect_pages` 가 **fetch 없이 snippet 을 본문으로** 쓴다. 정형 소스는
    받아올 페이지가 없고 이미 사실 문장이라, 외부 요청이 늘지 않는다(오히려 웹 fetch 자리를 대체한다).
  - ⚠️ **KOSIS 는 수치를 못 가져오면 아무것도 주지 않는다.** 축이 여러 개인 표는 KOSIS 가 '40,000셀 초과'로
    거절하는데, 그때 "이런 통계표가 있다"는 문장을 근거랍시고 넣으면 진짜 근거 자리만 차지한다
    (첫 실호출에서 관련 없는 관광 통계표가 그렇게 1위로 끼어들었다 — 표 이름 매칭까지 넣어 해결).
    표별 축·항목 조합 목록을 갖추면 더 많은 표에서 수치를 뽑을 수 있다 → **후속 과제**(자주 쓰는 표부터).
  - **도메인별 상한** `max_pages_per_domain: 2` (구 작업 7) — 같은 신문사 글이 근거를 독식하지 못하게.
    정형 결과는 상한 대상이 아니다(공식 통계는 소스가 하나뿐이라 쏠림이 문제되지 않는다).
  - **네이버 유형별 정렬**: 블로그·카페는 `date`(최신순), 뉴스·웹문서는 `sim`. 지난 실호출에서 블로그를
    `sim` 으로 부르면 3년 전 무관한 글이 섞여 나온 문제를 이렇게 정리했다. `research.naver.sort` 에 값을 주면
    전 유형에 강제된다(비워 두는 게 기본).
  - 키 없는 소스는 목록에서 자동으로 빠진다 — 키를 지워도 예전처럼 돈다.
- 완료 — **작업 9 연결(색인 단계)** (`12d44a5`). 테스트 152 passed. Ollama 실호출 1건.
  - `collect_pages` 가 만든 page dict 를 그대로 `upsert_pages` 로 넘긴다. `asyncio.to_thread` 로 감쌌고,
    **색인 실패는 조사를 막지 않는다**(부가 기능이라 조용히 넘어간다 — 테스트로 지킴).
  - `config.yaml research.vectorstore.{enabled,dir,embed_model,ollama_url,min_score,min_hits,max_age_days}` 추가.
    **`enabled: false` 가 기본** — 켜면 페이지마다 임베딩 호출이 붙고 디스크가 쌓이므로 사용자 결정 뒤에 켠다.
    켜져 있을 때만 저장소를 열고, `llama_index` import 도 그때 처음 일어난다(꺼져 있으면 기동 비용 0).
  - 세션2 인계 노트대로 **조회(웹 검색 대체)는 붙이지 않았다** — 이번엔 색인까지만.
  - 실호출 검증: bge-m3 로 페이지 1건 색인 → 조회 적중, 저장소 파일 5개 생성 확인.
    📌 **세션1·2 참고 — `min_score: 0.8` 은 너무 빡빡해 보인다.** 같은 주제 문장으로 조회했는데
    **score 0.668** 이 나왔다(1인가구 통계 본문 ↔ "1인 가구 비율"). 지금은 색인만 해서 영향이 없지만,
    조회 단계를 붙일 때 이 값을 그대로 두면 벡터DB 가 사실상 안 쓰인다. 실측 몇 건 더 뽑아 0.5~0.6 선으로
    조정하는 걸 권한다(조회 단계 작업자가 정할 문제라 지금은 안 건드림).
- 다음 후보 (세션3) — ① 벡터DB **조회** 단계(웹 검색 생략 판단, `min_score` 실측 포함)
  ② 작업 12 4단계 캐시 키 정규화 ③ KOSIS 표별 축·항목 목록화 ④ KCI/OpenAlex 학술 어댑터.
