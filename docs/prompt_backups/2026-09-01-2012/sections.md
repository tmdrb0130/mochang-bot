---
# user 프롬프트에 들어가는 섹션 머리말 모음. 코드(assemble.py, rag/pipeline.py)가 key 로 읽는다.
# 문구를 바꾸려면 여기만 고친다.
facts: "[지원자가 확인한 사실 — 그대로 사실로 써도 됨]"
unknowns: "[지원자가 아직 모른다고 한 것 — 확정된 것처럼 쓰지 말고 '가정'·'계획'으로 표현합니다. 멘토 도움(Q4-2)에서 다룹니다]"
references: "[웹 참고자료 — 아래 사실만 인용할 수 있습니다. 목록 밖의 수치·통계는 쓰지 않습니다. 전부 쓸 필요는 없고, 이 아이디어의 문제·고객·시장과 직접 연결되는 것만 고릅니다. 출처는 문장 안에 자연스럽게 녹입니다(예: 2025년 뉴스핌 보도에 따르면 …). '출처:' 라는 말을 본문에 쓰지 않습니다. 같은 출처를 이어 인용할 때는 두 번째부터 출처를 반복하지 않습니다. 연도가 적혀 있지 않은 항목은 연도를 쓰지 않습니다]"
extend_current: "이미 작성된 글:"
---
이 파일의 본문은 쓰지 않는다. frontmatter 의 값만 사용.
